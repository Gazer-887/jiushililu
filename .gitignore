# —— 开发过程笔记与计划（内部留痕，不入库）——
NOTEBOOK/
PLAN/

# —— 一次性交接文档（用户 2026-09-13 定：留在 docs/ 里给人读，但不入仓库）——
# 为什么单独一条：`docs/` 本身是**入 git** 的（对外文档），而交接文档是**给下一个会话读的**
# 一次性件（本机路径、临时结论、待办口吻），不该跟着公开仓库走。
# 按文件名前缀匹配 —— 将来新的交接文档自动适用，不用每次再想一遍。
docs/交接*.md

# —— 个人学习日记（用户强制约定，不入库）——
DIARY/

# —— WorkBuddy 内部数据（含 memory/）——
.workbuddy/

# —— Python ——
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.local

# —— Node ——
node_modules/
out/
dist/
build/
*.tsbuildinfo
electron.vite.config.*.mjs

# —— 测试与覆盖率 ——
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# —— 编辑器与系统 ——
.vscode/
.idea/
.DS_Store
Thumbs.db

# —— 本地运行时数据（会话、记忆库、日志）——
*.db
*.sqlite3
*.log
runs/
out-stale-*/
verify-*.png
.verify-userdata/
.git-commit-msg.tmp
dist-stale-*/

# —— 校准 harness 的现场（plan8 R9.1）——
# ⚠️ 第一条是**硬安全要求**，不是习惯：
#   `.bench-tool-window/userdata/` 里会出现**用户密钥保险箱的副本**（哪怕只是密文），
#   任何情况下都不许进仓库；`bench/` 里是真实调用的原始报告（含模型回答内容）。
#   将来要公开数字，请另写一份**人工整理的摘要**进 docs/，不要直接提交原始报告。
.bench-tool-window/
bench/

# —— 图片归档（Photo 目录）——
# 验证脚本的截图每次跑都会重新生成，属"本地归档"不入库；
# 需要随仓库分发的图片资产（应用图标等）放 resources/ —— 那里是入库的。
Photo/
