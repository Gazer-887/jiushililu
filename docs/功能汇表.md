# 功能汇表：已实现 与 待实现

> 对账基线：**0.13.90**（2026-09-26 定版：**plan56 记忆页只做审批三片 + 独立复查轮**（片① 已生效列表折起 `8bc4bfc`、片② 「需你过目」给处置出口 `468e241`、片③ 一键拒绝未成簇 + 可恢复回收站 `85fc161` + 门禁跟桩 `8960c4d`、复查九条对账含 1 条 P1 `0e79a71`）。本批是 0.13.89 被真人点验打回来的**形态返工**，不是新功能。上一版 0.13.89（09-25 17:33 本机出双包）**未打 tag、未发 GitHub Release**；⚠️ `dist/` 现只剩 0.13.89 一套双包 —— **09-25 17:46 按用户指令清掉了 87 / 88 两副（含 blockmap 共 418 MB），"87 那份内容的唯一副本"本句已失效**，复现 = `git checkout <对应提交>` 后 `npm run dist`。**发版即动表**：本行版本必须等于 `package.json`，由 `npm run check:docs` 机器核（`scripts/check-docs-fresh.cjs`）。）
>
> **本次复验方法（与以往不同）**：以往是「PLAN/ 状态行 × progress.md × src/ 三方交叉」——
> 读的是**计划自己怎么说**。本次改为**不读计划，直接去源码找符号**（类型定义 / 导出函数 / IPC 通道 / 引用点），
> 计划与实践不一致的**一律以源码为准**。结果：19 项标记完成的能力**全部验证通过**，
> 同时查出 **5 处台账滞后**（详见文末 §四）。
>
> **2026-09-21 那次沿用同一方法再做一遍**：`CHANGELOG.md` 的 0.13.52–0.13.83 共 **32 个版本条目**逐条对到源码符号，
> 提交面用 `git rev-list --count c7e94b0..HEAD` = **84 个提交**（`c7e94b0` 为 0.13.51 收口提交）交叉；
> 远端 tag 在 0.13.52 之后只有 13 个，**版本归属一律以 CHANGELOG 为准**（细则见 §四 末「更新惯例」）。
>
> 状态符号（`AGENTS.md` §四③ 的四值口径，2026-09-21 起本表按此对齐）：
> ✅ 已完成（**附版本与提交号**）· 🚧 进行中（部分完成）· ⏳ 未开始 · ❌ 不做（终态，划掉留痕不删行）。
> 本表旧版把 ❌ 用作「未立项（仅愿景）」，属存量用法——本次新立的未完成条目一律写 ⏳，❌ 只留给「不做」。

---

## 一、已实现

### 会话与模型

| 功能 | 状态 | 出处 / 说明 |
|---|:--:|---|
| 多模型接入（OpenAI 兼容协议 + Anthropic 原生，providers/ 抽象层） | ✅ | plan3；Key 走 safeStorage 加密，不硬编码 |
| 模型目录与管理界面（增删改 / 连通性测试） | ✅ | plan7 F5 / F5.1 |
| 流式输出 + 唯一发送口 | ✅ | `chat-emitter.ts` |
| 多会话并发（一窗口多会话各跑各的，上限 3） | ✅ | plan11 六步全完成（0.13.13） |
| 会话持久化分层存储（索引 + 正文，读盘足迹不随正文增长） | ✅ | plan10 0/A 批 |
| 会话回滚（N 轮 → 原前缀）+ 确认桥 | ✅ | plan10 B 批 |
| 检查点（跨轮 runs）+ 文件变更记录 + revert-hunk | ✅ | plan8 R4 |
| Token Saver（省 token 档位） | ✅ | plan8 R-10；四档 `rich/ultimate/balanced/light` 见 `shared/token-tier.ts` 的 `TokenSaverTier`；8k/12k/0.72 为保守默认。**「校准待做」已过期**：校准方案 v2 于 2026-09-13 两轮实测收口（见下方「Token Saver 校准 v2」行），原写「校准待做」、实际做的是 `--repeat≥3` 取中位数并带工具调用次数，结论=保持现状 |
| 上下文压缩/收窄提示 | ✅ | plan8（撤指引 v2 校准同样已做完，见下表） |
| **Token Saver 校准 v2（每臂 ≥3 次比中位数 + 报告轮数 / 工具调用次数）** | ✅ | plan8 R9.1 §七⑤（`scripts/bench-tool-window.cjs` 的 `REPEAT` 与 `median`，`npm run bench:window`）；两轮实测记录在 `PLAN/plan8_健壮性与技术债.md` §七⑤：窗口化在大输出下省 94%，三档「调不出可测差别」（方差 > 效应）⇒ **8k/12k/0.72 维持不动**。原列 §二「未做」，2026-09-21 反向验证改正 |
| 会话大纲（各轮提问悬浮列表 + 点击跳转） | ✅ | 0.13.46；**点开式浮层自 0.13.60 起被右缘常驻刻度条取代**（见下行），点击跳转与悬浮列表语义保留在刻度条上 |
| **会话刻度条（右缘常驻等宽刻度 + 滚动联动高亮 + 拖动擦洗预览卡 + 滚轮跳轮）** | ✅ | plan41 S1（`bb33716`，0.13.60）+ S2（`ed0e719`，0.13.61）+ 手感修正（`45a0699`，0.13.72 热区 24×12px、滚轮跳轮）；**09-22 反转**：预览卡由「hover 即出 + 激活常显」改成**只有按住拖动时出、且内容跟随滚动**（旧行为停在正文上挡字，用户截图报）；`views/ChatView.tsx` 的 `outlineItems` / `tickTopsRef` / 擦洗 effect，样式 `.chat-outline-tick` / `.chat-outline-peek` / `.is-scrubbing` |
| 模型按端点分组切换（设置页 + 输入框下拉两处） | ✅ | 0.13.45；选择粒度下沉到模型条目，`models:set-entry` 补广播（D-045） |
| 模型自视配置报告（每轮向模型报告当前运行配置） | ✅ | 0.13.45 |
| **免保存拉取可用模型 + 每端点各持一把 Key** | ✅ | plan47 S1/S2（`c4b3783`，0.13.68）：IPC `models:fetch-available` → `store/models.ts · fetchAvailableModels`，Key 解析链为纯函数 `shared/models.ts · resolveFetchApiKey`（表单明文 → 该端点已存密文 → 空串即拒，不静默发请求）；Key 本体按端点分把存于 `store/settings.ts` 的 `apiKeysEncrypted`（`getProfileKey` / `setProfileKey`，档案形状自 0.13.17 F5.1 起就是「一端点一把 Key + 一串模型」）；拉取失败分三档给人话 |
| **baseURL 版本段通用识别（不再写死 `/v1`）** | ✅ | plan47 S2（`c4b3783`，0.13.68）：`providers/url.ts` 的 `TRAILING_VERSION_SEGMENT`——已带版本段（`/v3` `/v4` `/v1beta` …）的地址原样拼接，智谱 / 火山方舟一类非 `/v1` 端点不再被再插一层而 404 |
| **消息分段化：思考段 / 工具卡 / 正文按真实顺序交错，且随会话落盘** | ✅ | plan36 S1–S4（`addabba`，0.13.56，D-100）：纯函数在 `shared/message-segments.ts`（`appendSegmentThinking` / `upsertSegmentTool` / `ensureSegments`），渲染器 `components/MessageSegments.tsx`，落盘字段 `shared/ipc.ts` 的 `segments`；分段过长时**舍分段不舍正文**，该降级路径的非法内容缺陷在 0.13.58 修（`fba57d8`） |
| **消息操作条（复制正文 / 重新生成 / 编辑提问）+ 消息时间戳** | ✅ | plan46（`4ac2cb6`，0.13.59）：`views/ChatView.tsx` 的 `copyMessage` / `requestRegenerate`，编辑提问走 `store.ts · rollbackTo(index, { viaEdit: true })` + `rollbackNotice`；`createdAt` 缺则不显示（不编造）；回退提示改两段式说清「文件与 git 提交未退」 |
| **模型切换永不改名** | ✅ | plan39（`addabba`，0.13.56，D-101）：`store/models.ts · setModel` 只按名字精确命中 `shared/models.ts · findModelEntry` 的已有条目，找不到就明确报错、配置零改动；改名只保留在设置页显式编辑 |
| **语音输入（点按录停、转写插到光标处、端点自配）** | ✅ | plan45（`fe02663`，0.13.64）：`src/main/voice/transcribe.ts · transcribe` / `testVoiceEndpoint`，IPC 四通道 `voice:get-config` / `set-config` / `transcribe` / `test`，界面 `components/VoiceButton.tsx` + `VoiceSettingsPanel.tsx`；一次性隐私披露 + 仅放行纯音频权限 |

### Agent 内核

| 功能 | 状态 | 出处 / 说明 |
|---|:--:|---|
| 工具系统（命令 / 文件读写 / 检索） | ✅ | 代码检索走 ripgrep agentic search（不用向量，plan4 结论） |
| 子代理（spawn_agents + 调度器 + 执行进度面板） | ✅ | plan6；`.agents/*.md` 定义 |
| 任务清单（update_todos + 输入框上方可折叠面板） | ✅ | plan7 批 D |
| 后台任务（background=true + check/kill + 列表回显） | ✅ | plan7 批 D |
| Agent 向用户提问（ask_user 带选项，超时绝不代答） | ✅ | plan7 批 D-提问（`2bfb090`） |
| 危险操作确认（R5 确认桥：待决请求 + IPC + 超时 + 关窗中止） | ✅ | `confirm.ts` |
| 权限档（只读 / 逐次授权 / 信任目录，唯一用户档位） | ✅ | plan8 D-032；**plan29 S1（`34a39c2`）名实相符**：完全访问 = Agent 线无边界 + 界外回显绝对路径（界面线仍锁工作区，结构性分界由源码级断言钉死）；越界写入回报「不在本轮回滚覆盖范围内」 |
| **计划批准**（planner 出方案 → 停下等用户点头 → executor 续跑） | ✅ | plan27（`196ab66`）：零 electron 桥 `plan-approval.ts`（600s 超时）；闸在 `runAgent` 不进 loop——子代理天然不闸（守卫丁防回退）；frontmatter 新字段 approval/executor |
| **edit 工具**（精确字符串替换） | ✅ | plan28 S1（`25ce074`）：非唯一 → 拒绝；任一处不成立 → 整次不做（原子）；CRLF/LF 按文件口径归一 |
| **命令超时可调 + 输出上限** | ✅ | plan28 S3/S4（`25ce074`）：timeoutMs 模型可设（默认 120s / 上限 600s，越界拒绝不夹取）；maxBuffer 1M→8M；失败三分型（输出超限 / 超时 / 真错误） |
| **流守卫（四层计时器）** | ✅ | plan29 S2（`25ce074`）：首包 60s + 分片间隔 90s 取代整轮墙钟（**按用户决议彻底删除**，不保留开关）；报错分「首包超时 / 流中断」，子代理父停子停 |
| 目标（Goal，跨轮次存活的长期意图） | ✅ ①–⑥ 全交付 | plan12；**⑤ Agent 自建、⑥ 待办联动曾长期标"未做"，实际 0.13.37（`ec10294`）已交付** —— `set_goal` @ `tools/goal-tools.ts:16` 且已进 `runner.ts:69` 白名单，联动 @ `TodoPanel.tsx:18`；2026-09-15 源码复验改正 |
| **执行事件流**（六 kind 落盘 `<userData>/exec-events.jsonl`，子代理标 `sub`；时间线页签数据源） | ✅ | plan26 S1（`ec91855`）；与记忆事件流平行，落盘/轮转/失败容错同套机制 |
| **智能标题**（首答落盘后一次轻调用；只覆盖机械标题 + 原子条件更新防冲用户改名 + fail-soft） | ✅ | plan26 S3（`369d3e6`）；`conv:changed` 广播，侧栏实时更新 |
| **滚动摘要**（裁剪时调模型摘要被裁段，缓存复用；双层 fail-soft 退机械占位） | ✅ | plan26 S3；进程内会话级缓存（重启重摘，已登记取舍） |
| 子 Agent 管理界面（CRUD + 主 Agent 切换） | ✅ | plan17 / plan7 F8（`b5e9942`）；`AgentManager.tsx` 已接线进设置页「子 Agent」分区（`SettingsView.tsx` 中 `section === 'agents'` 分支）。**愿景表曾标 ❌ 未立项，已改正**。⚠️ **分层口径已变**：原写「三层 CRUD（内置 / 用户 / 项目）」，0.13.60 起 **项目级取消、回归两层**（D-103 ②，`175dac7`）——`shared/agents.ts` 的 `AgentSource = 'builtin' \| 'user'`，工作区 `.agents/` 旧定义改由加载警告提示迁移 |
| **子代理工具集按自己的声明装配 + 角色天花板** | ✅ | plan51 F1–F4（`99dad28`，0.13.78）：装配口径见 `agent/runner.ts`（0.13.77 之前是「复用主代理那份已过滤工具集」，`def.tools` 对子代理完全不生效）；上限改为「权限档 ∩ 自己的声明」，故带计划批准闸的 Agent **拿不到 `spawn_agents`**（防写操作绕闸落地，见 `runner.ts` 派发口摘除）；`check_command` / `kill_command` 与 `run_command` 同进同退；`self-view.ts` 补「缺什么 + 谁能给」；一次性任务入口 `IPC.agentRun` 的 `permission` 与对话线同源（复查抓出，此前恒按可写档）。plan6 D4「审查型只给读、执行型才给写」自此第一次成立 |
| **子代理吃到做事纪律，且只吃它该有的那几条** | ✅ | K6（`77f90e5`，0.13.80）：`agent/conduct-rules.ts · composeConductRules` 按**实收工具表**取舍（工具没拿到的承诺不写），子代理与主代理吃同一份模块；自视段**不给**子代理（实测该段约 140 token，无理由才是给的理由）；会话级面板工具 `update_todos` / `set_goal` 从子代理收回（并发各推一份会互相覆盖） |
| **确认卡按发起者署名** | ✅ | K5（`b242244`，0.13.79）：`agent/runner.ts · buildCommandTools`——命令类工具**按发起者各造一份实例**（`agentLabel` 在建实例时定格，复用主代理那份会让并发派发的几张卡同署名）；界面出口 `components/ConfirmDialog.tsx` 的 `req.agent`；后台任务记录同样按发起者落账。**权限档 / 确认桥 / 检查点口径未变** |
| **同一个 shell 会话内命令串行执行** | ✅ | K5（`b242244`，0.13.79）：`agent/tools/shell-session.ts` 的 `chain` 字段（`run()` 串行化到 `runNow`）——此前两条并发命令往同一 stdin 写、同一 stdout 收且无互斥，一方会收到另一方的输出、超时连带杀对方进程；0.13.79 以真并发用例复现后修复 |
| **空正文助手轮的出境整形（停止生成不再作废会话）** | ✅ | K8（`3a7898a`，0.13.81，D-125）：发送侧 `src/main/schemas.ts` 按角色放行（助手轮允许空正文，不再 `min(1)` 硬拒）；「不把空串送上线」由 `agent/context.ts · historyForModel` 负责，接线**两处**（主循环 + `memory/reflection.ts · reflect`——反思不经主循环，第一轮复查抓出的漏口）；渲染端不再吞掉主进程原文（此前空 `catch {}` 把定位信息丢了） |
| **反思失败留痕（不再与「确实无内容」同形）** | ✅ | plan8 R17（`7c3af06`，版本号记 0.13.83、**未单独发布 ⇒ 随 0.13.85 发出**，`git tag --contains` 核过）：`memory/reflection.ts · reflect` 只咽「模型输出形状不对」这一种，其余异常抛出，由 `store/memory-store.ts · runReflection` 落主进程日志；判据在 `tests/unit/memory-queue.test.ts` 的 R17 组（含阳性对照） |
| **web_search 工具（默认零密钥 + 可选 Firecrawl + 失败熔断）** | ✅ | plan31 / plan32（`738951d` + `7dfbaa1` + `4587050`，0.13.52）：`agent/tools/web-tools.ts · createWebTools`，默认源解析 `parseDuckResults` / `unwrapDuckHref`，密钥源 `parseFirecrawlResults`；熔断常量 `FIRECRAWL_COOLDOWN_MS = 10 * 60_000`（402/429 等失败后 10 分钟内直接走默认源，首行注明结果来源） |
| **fetch_url 受控跟随重定向 + 错误页反假成功** | ✅ | plan38 S1–S3（`3a0337b`，0.13.55，D-099）：`web-tools.ts` 的 `fetchFollowingRedirects` / `MAX_REDIRECT_HOPS = 3`（**逐跳**重过内网拦截与协议白名单，https 不许降级），`detectErrorPage` 把 HTTP 200 的提示页识别出来；SSRF 扩面在 `isBlockedHost`（`.local` / `.internal` / 100.64.0.0/10 / IPv6 映射与 NAT64 展开）；`browser_read_page` 读页上限 20K → 512KB（`93b04e3`，0.13.53） |
| **run_command 持久 shell 会话（cd / 环境变量跨步存活）** | ✅ | plan28 S2（`f46d6a6`，0.13.53）：`agent/tools/shell-session.ts · createShellSession`，前台命令跑在常驻会话里、起不来时自动回落一次性执行；同 run 从工作区根重新开始，回收与并发上限见该文件的 `IDLE_MS`（10 分钟空闲回收）与 `MAX_LIVE_SESSIONS`（同活会话上限 4）。原列 §二「⏳ 未开工（需 shell-integration 层）」，2026-09-21 反向验证改正 |
| **命令结果机器可判尾标（exit_code / error_class / elapsed_ms）** | ✅ | plan31（`738951d`，0.13.52）：`agent/tools/system-tools.ts` 在结果尾部统一附三字段，失败分型 `ok` / `error` / `timeout` / `output_exceeded` 由尾标给出，不再靠读文本猜 |

### 工作台（右抽屉页签）

| 功能 | 状态 | 出处 / 说明 |
|---|:--:|---|
| 文件页签：目录树 + 写操作 + 右键菜单 + 拖拽上传 + 二进制/图片/Markdown 预览 | ✅ | plan7 批 A/A2/A3 |
| Office 内嵌预览：docx（mammoth）/ xlsx·xls·xlsm（SheetJS）转沙箱 iframe；多 sheet 切换 + 截断保护 | ✅ | 0.13.36（用户反馈「docx 看不懂十六进制」） |
| 二进制出口：pptx 等不支持内嵌的格式 → 十六进制头 + 「用系统程序打开」（工作区边界内） | ✅ | 0.13.36 |
| 编辑器页签：Monaco（代码编辑）+ Diff 审查 + 未保存守卫 | ✅ | plan7 批 B（`75ea027`）/ plan13。**⚠️ 2026-09-15 澄清：Monaco 与 Diff 是两个独立组件** —— `CodeEditor.tsx`(269 行，**零 diff 字样`) 与 `DiffView.tsx`(325 行，**jsdiff + 统一视图**) 不同源；原写法连着写易读成"Monaco 的 diff"。不并排的理由写在 `DiffView.tsx:19-20`：Monaco 与 jsdiff 各一套切块算法，并用会「点第 2 处、写盘第 3 处」静默对不上 |
| 终端页签：node-pty 真终端（重大转向后落地） | ✅ | plan7 批 C / plan14（0.13.25） |
| **页签保活 + 渲染性能修复**（plan30，0.13.51） | ✅ | 失活页签隐藏不卸载（browser 例外失活即卸载）+ MessageMarkdown LRU 解析缓存 + 九面板 memo——真机实测会话切换 765→281ms、卡顿帧 87→2；包装层 `display:contents` 保高度链（0.13.51 修 docx 预览卡尺寸回归） |
| 任务页签：子代理进度 + 后台任务列表 | ✅ | plan7 批 D |
| 源代码管理：变更列表 / 勾选暂存 / 差异 / 提交 / 待推送计数 / 外部刷新 | ✅ | plan16（内联着色 Diff，Monaco 复用未接） |
| 检查点页签：runs 列表 + 正文 Diff + 回滚 | ✅ | plan8 R4 / plan10 B |
| **时间线**：执行事件流回放（工具调用/审批/裁剪/起止，按会话过滤，元数据摘要行） | ✅ | plan26 S2（`c67109b`）；事件只含元数据（payload 白名单，入参/正文不落盘）；真渲染门禁 verify-shot 338/338。**位置已变**：0.13.74（`e7606b9`）从工作台页签移进**主对话头部开关**（`views/ChatView.tsx` 引 `components/TimelinePanel.tsx`），不再占一栏 |
| 面板宽度拖拽 + 分栏引擎 | ✅ | plan7 A0 / plan9 |
| **页签栏滚轮横滚** | ✅ | plan42（`ddc88e6`，0.13.58）：`components/Pane.tsx` 以 `{ passive: false }` 手动接管 `wheel`（React 的 `onWheel` 是 passive，`preventDefault()` 会静默失效）；未溢出时不劫持滚动 |
| **栏折叠语义改「留页签条」+ 栏数下降 WARN 哨兵** | ✅ | 0.13.70（`9567ad1` + `d4f1285`）：`Pane.tsx` 折叠只收标题栏、页签条常驻展开钮；栏数在 `store/ui-prefs.ts` 侧对「读回来的栏数比上次少」记 WARN（P0 右栏塌缩的回归哨兵），门禁同批补「每栏 ≥120px」渲染断言 |
| **右栏 / 主列比例制布局（65 / 35 + 主列地板 360px）** | ✅ | 0.13.73（`38bd5ff`）：`shared/splitter.ts` 的 `DOCK_RATIO_MAX = 0.65` / `MAIN_RESERVE = 360` / `dockMaxWidth(available)`，窗口尺寸变化时已存宽度自动重钳（含窄窗启动读档） |
| **右栏工作台减负 9 → 7**（Playbook 并入设置分区） | ✅ | 0.13.74（`e7606b9`）：内置面板集合见 `shared/workbench.ts` 的 `BUILTIN_TYPES` / `BUILTIN_LABELS`（2026-09-21 计数 = **7 项**：资源管理器 / 文件变更 / 源代码管理 / 终端 / 浏览器 / 任务管理 / 记忆）；Playbook 改在设置页分区（`components/PlaybookManager.tsx`），时间线移进主对话 |
| **弹出层裁切两条修复：ⓘ 气泡视口感知翻转 + 「＋」菜单限高内滚** | ✅ | H4（`3d673b1`，0.13.77）：`components/FieldNote.tsx`（下方放不下就翻上、两侧都不够则收缩高度并在气泡内滚、滚动中重判方向）+ `components/PlusMenu.tsx`（上限高度 + 内部滚动）；门禁同批补矮窗档（`scripts/verify-shot.cjs`） |
| **内置终端可选加载 PowerShell profile（默认关）+ 状态栏就地开关** | ✅ | E5（`daae32c`，0.13.71；就地入口 `d401c1b`，0.13.75）：`store/settings.ts` 的 E5 开关 → `terminal-session.ts` 起 shell 时决定是否带 `-NoProfile`；`components/TerminalPanel.tsx` 状态栏的 shell 名可点即切换并重启终端（提示与入口同位） |
| **外部 MCP 工具来源徽标 + 桌面动作参数明文回显** | ✅ | plan44 S2 之一/三（`d05ad13`，0.13.67）：`components/MessageSegments.tsx` 的 `.tool-mcp-badge`（`MCP·<服务器名>`）+ `shared/tool-detail.ts · toolCallDetail`（字符串兜不住时拼至多三个标量 `k=v`，80 字截断） |
| **MCP 截图真的上屏：工具卡里的缩略图** | ✅ | plan44 S2b（未发版）：`image` 块不再被压成"[image 类型内容]"。引用（受限文件名 + 类型 + 字节数）随工具事件进 `segments`，正文落 `userData/mcp-artifacts/`，界面按引用经 `mcp:artifact-read` 取回；**不进模型上下文** —— 与"记忆正文不进 prompt、要细节用 recall"同一条口径（进了既烧 token 又每次存档重写一遍图）。留最新 40 张按 mtime 清；类型白名单 png/jpeg（SVG 能带脚本）；取不到那张明说"截图已不在"，不留空白格 |

### 生态与扩展（P3：技能 / MCP / 规则）

> 本节 **2026-09-21 新增** —— 原表**整层没有**：技能系统（F6）、MCP 客户端（F7）、规则系统（plan24）
> 随 0.13.50 出包（开发提交 `fb6d5e1` / `5a9fdbc` / `e6040c8`），0.13.60 又补齐全套管理 GUI 与开关（plan34），
> 而本表连一行都没有。与 2026-09-15 那次「P4 整层缺失」是同一类失真，量级相当。

| 功能 | 状态 | 证据 / 说明 |
|---|:--:|---|
| 技能系统（渐进式披露的流程知识包） | ✅ | plan22 F6（`fb6d5e1`，0.13.50 随包）：`skills/loader.ts` 的 `parseSkillDefinition` / `loadSkillsFromDir` / `loadSkillEntries`（分层 `SkillLayerSource = 'builtin' \| 'user'`），模型侧工具 `tools/skill-tools.ts · use_skill` |
| 内置技能随包 + 中英双语简介 | ✅ | plan34 S3（`1bdc18f`，0.13.60）：`resources/skills/*.md`（2026-09-21 计数 = **9 个**），双语字段 `description_zh` / `description_en` 见 `skills/loader.ts`；S2a 的「›」下拉展开中文简介在 `SkillsPanel.tsx`。**i18n（plan52）就绪前界面取 zh** |
| MCP 客户端（stdio / SSE 接外部工具） | ✅ | plan23 F7（`5a9fdbc`，0.13.50 随包）：`mcp/mcp-manager.ts · createMcpManager`，工具桥 `tools/mcp-tools.ts · createMcpTools`（前缀 `mcp__<name>__`）；IPC `mcp:list` / `mcp:save` / `mcp:delete` / `mcp:reconnect` / `mcp:changed`；服务器配置含 token 时住 userData、不入 git |
| 规则系统（无条件注入的约束层） | ✅ | plan24 P3 收口（`e6040c8`，0.13.50 随包，D-068）：`rules/rules.ts · collectRuleFiles` / `composeRulesBlock`，字节上限常量 `RULES_BLOCK_MAX_BYTES`；注入点在 `agent/runner.ts` 的规则段，超限时 `droppedFiles` 如实上报 |
| 技能 / MCP 的管理 GUI 与「真禁用」 | ✅ | plan34 S1–S4（0.13.60：`6f335b5` / `2d8bde1` / `466dd92` / `180e01f` / `07bc8b9`）：技能写路径 `skills/skills-write.ts · composeSkillFile` / `saveSkillFile` / `deleteSkillFile`（删除走回收站，**内置技能不可删**、只用开关关）；技能禁用名单走 IPC `skills-disabled:get` / `set`；**MCP 不设第二份名单**——开关走配置 `cfg.enabled`（`mcp-manager.ts`，关掉即断进程、工具不再下发，单一真相源）；「+」面板同步（`PlusMenu.tsx`）与输入框「/」技能浮层（`InputConsole.tsx` 的 `slash` 状态） |
| 桌面操作（computer-use）能力接入：门控 + 白名单 + 推荐卡片 | ✅ | plan44 S1+S3（`f38df9c`，0.13.66）：`shared/computer-use.ts` 的 `COMPUTER_USE_ALLOWLIST`（仅屏幕/键鼠/UI 树类工具）、`gateComputerUseTool`（返回 `keep` / `drop-server-off` / `drop-not-allowlisted`，**未知新工具名默认屏蔽并记日志**）、`forceMainDisplay`、风险标注 `PROCESS_RISK_NOTE`；推荐卡片 `components/ComputerUseRecommend.tsx`（仅写配置，不代装不代开权限）。**两道闸独立**：添加 MCP 配置 + 通用设置「电脑控制」开关 |
| MCP 桌面派工具装配层修复 | ✅ | D-119（`1b59b2a`，0.13.76 未发布 / 随 0.13.77 出包）：装配口补回 `computerControl` 与门控回调两个漏传参数——面板显示「已连接」而模型说没有桌面能力的那条路径，并补三条装配口回归测试 |

### 记忆与进化（P4，自进化闭环）

> 本节 **2026-09-15 新增** —— 原表完全没有这一层（基线停在 0.13.35，而 P4 自 0.13.41 起四环全闭）。
> 这是本次复验补上的**最大缺口**，不是"补几行"的量级。

**① 语义记忆（0.13.41 批 1）**

| 功能 | 状态 | 证据 / 说明 |
|---|:--:|---|
| 两条写入通路（模型 `remember` / 用户选中即记） | ✅ | 选中即记**不经过模型**，记的就是选中的原话 |
| 影响面三分类 + 注入与预算截断 | ✅ | 满载 100 条时 8KB 字节预算先到 → 只注入 81 条，**如实标注"未生效"** |
| 事件流 `memory/events.jsonl` | ✅ | 写入（含被拒）/ 召回 / 删除 / **归档** / 标记 / 注入 |
| 三条架构守卫 | ✅ | 记忆层物理碰不到权限设置；授权语义拒写并指路；凭据形状硬拒。**有单测钉死** |
| 右抽屉记忆页签 + 「本次新增」巡检区 + 归档区 | ✅ | 删除立即生效，下一轮起不再出现；归档区默认折起只报条数，展开可逐条恢复（plan53 片 1，**0.13.87**）；★ K28「清空归档」已实现（带确认框，逐条记 `delete`），**未随 0.13.87 出包，随下一包** |
| 注入预算读数「已用 N / 上限 M 字节」 | ✅ | plan53 片 0；数由 `buildIndex` 逐行算出后随索引返回，界面只读这一个数（抄来的上限或漏上报都有判据抓）。**0.13.87** |
| 用量牌「记忆税 N(估)」 | ✅ | 占多少上下文是**看得见的数**，不是感觉 |
| 完全访问档下开启记忆的红色告警 | ✅ | 每次重开都会再提醒一次 |
| **审批门：模型写的长期记忆默认不生效，先进候选** | ✅ | plan53 片 2（D-131「默认开」）。闸口在 `createMemoryRepo` 的 `save()` 而不是工具层 —— 以后多一条模型通路会自动被罩住，不靠每个调用点记得判一次。画像仍**只对反思开放**（产品里没有产出"模型来源画像候选"的通路：工具 enum 不含 profile、save 层在路由前就拒、预筛把**输出分类**与来源同判，见 plan55 片④ 那节；它出现在盘上只有手改/伪造两种可能）。逃生开关在设置页「写入审批」，关掉即回直写、**下一轮生效**（每次写现读，不做重启才生效的假开关） |

**② 自动捕获 / 反思（批 2）**

| 功能 | 状态 | 证据 / 说明 |
|---|:--:|---|
| 会话切换时异步反思 → 产出候选 | ✅ | D-040：关窗只记队列、启动补跑 |
| 候选**物理隔离**在 `memory/candidates/` | ✅ | 用户批准前**不进注入索引段** |
| 批准 / 拒绝 / 冲突标记 + 前置门 2KB | ✅ | 日上限与队列 |

**③ Playbook（批 3）**

| 功能 | 状态 | 证据 / 说明 |
|---|:--:|---|
| `evolution/playbooks/` 布局 + tags 条件召回 | ✅ | 预算与语义记忆**分开** |
| `save_playbook` / `recall_playbook` 已注册下发 | ✅ | 0.13.48 补齐接线（`6a620ac`） |
| 右抽屉 Playbook 页签 | ✅ | |

**④ 遗忘与度量（批 4）**

| 功能 | 状态 | 证据 / 说明 |
|---|:--:|---|
| LRU 遗忘 + style 豁免 | ✅ | 遗忘**不是硬删**（plan53 片 1）：条目移进 `memory/archived/`，界面上折叠显示条数、可一键放回生效集合。归档记 `archive` 事件、不记 `delete`，存活率不被自己的安全网压低。**0.13.87** |
| 描述相似度警告 | ✅ | |
| 纠正通路 + 重复纠正率 / 误伤率 | ✅ | 0.13.48 补齐两个指标的数据源（`6a620ac`）；**口径 v2（plan53 §四之二，未发版）**：`correct` 的记账时刻从"模型写入成功"推到"**用户批准了那条撞名提案**"，公式与分子分母一行不动。三条件缺一不可（撞名 + 那一轮命中否定词 + 被批准），事件里的会话/轮次取**来源那一轮** |

**④ 用户画像 + eval 集（plan25，**实际随 0.13.58 发出**，`4a979fc`/`0e481e8`/`a523ecd`；09-23 核：原写"0.13.50 未发版"两处都错 —— 三个提交 `git tag --contains` 最早都落在 v0.13.58）**

| 功能 | 状态 | 证据 / 说明 |
|---|:--:|---|
| 用户画像（`class='profile'` 特殊记忆：固定 slug、注入正文置顶、独立 4KB 预算） | ✅ | D-071~073；四层写入防线（save 层拒 model 直写 / 工具 enum 排除 / UI 无选项 / approve 覆盖断言） |
| 反思产出画像候选（跨场景特征 → user-profile；撞名原地覆盖） | ✅ | 反思 prompt 独立模块（判据可断言）；手改防线（重复画像取新 + warnings） |
| L1 场景回归 16 条（CI 零模型，桩 chat 走完整记忆链路） | ✅ | `tests/evals/`；定性如实登记为"场景级回归测试"，不冒充 eval（盲审修正） |
| L2 在线 eval（electron 子进程 safeStorage 解密真跑；全代码判分 pass@k） | ✅ | `npm run evals`；**实测 deepseek-flash 3/3 通过 5s** + repeat=3 9/9；报告本地留档 bench/（不入 git） |
| 记忆 import 拒绝画像导入（明确话术） | ✅ | memory-import toClass |

**⑤ 去重与注入热路径（plan33 / D-106，2026-09-21 新增）**

| 功能 | 状态 | 证据 / 说明 |
|---|:--:|---|
| 写入相似度闸门 + 面板「疑似重复」区（一键合并 / 忽略） | ✅ | plan33（`beddb6a`，0.13.52，问题四）：判定核心是纯函数 `memory/similarity.ts` 的 `MEMORY_SIMILAR_THRESHOLD` / `normalizeSimilarityText` / `charBigrams` / `similarityScore` / `findSimilarEntry`（五条写入通路汇聚到 `memory-core.ts · save`，闸门收一个口）+ `findDuplicatePairs`（存量扫描）；界面 `components/MemoryManager.tsx` 的疑似重复区，合并方向由主进程按创建时间重判（正文并入较新那条、删旧条）。原列 §二「📋 只登记」，2026-09-21 反向验证改正——**交付口径是「同文换名 / 包含 / 近乎相同」的字面相似，不含语义级同义归一** |
| 疑似重复扫描签名缓存（热路径 O(n²) 摘除） | ✅ | 0.13.62（`ece9b13`）：比对结果按内容签名缓存，记忆没变不重算。实测（满载 100 条、同机对照、含系统实时防护底噪约 49ms）：单轮读取中位数 **139ms → 73ms** |
| 记忆读盘按 (mtimeMs, size) 判新鲜 | ✅ | D-106（`6421bf1`，0.13.63）：`store/memory-fs.ts · createFsMemoryBackend` 的 `readCache`——stat 命中即免「读盘 + 重复解析」，适配器不提供 `mtimeMsBytes`（注入式测试桩）时自动回退直读；实测（满载 100 条、`bench:all` 三跑、Windows Defender 实时防护环境下）**基准耗时 −63%** |

**⑥ 候选区治理：预筛与按簇批准（plan55 / D-139 / D-140，2026-09-25 新增，全部为未发版增量）**

缘起是用户 dogfood 报的那一条：待批准队列积了 71 条、其中 40+ 条在说同一件事，
"人工审核不仅视感差，而且操作繁重，需逐条管理"。判据口径统一一句话：**形状可测、分组质量不可测**。

| 功能 | 状态 | 证据 / 说明 |
|---|:--:|---|
| 「未能加载」与「需你过目」分家（K36，片①-a） | ✅ | 未发版。`memory-core.ts · loadAll` 把"读不出来/解析失败"送 `warnings`、把"已生效但守卫要看一眼"送 `needsReview`，界面两格各自可数；真渲染判据各钉一条（同一文件混装是用户报"2 条未加载却还在生效"的成因） |
| 个人信息与环境矛盾断言硬拒（片①-b） | ✅ | `shared/memory.ts · guardMemoryText` 加两档 reject（身份字段按**形状**匹配，仓库里不留任何具体值；「运行环境为 X」与注入的 `hostPlatform` 矛盾即拒）。⚠️ 判定按**最严重的一档**取，不按先命中的一档 —— 顺序曾经让标记档抢在身份档前面，条目照常入库（交叉验证 R-A3/B3，已修 + 变异证据） |
| 反思输入带「已生效索引 + 当前待批候选」（片②） | ✅ | `reflection-prompt.ts · composeReflectionContext` 产只读的 known-memories 块；判据钉"这一轮内容不变 ⇒ 出境文本逐字节相同"，防的是把候选正文喂回去 |
| 候选互检 / 同名不覆盖 / 条数上限（K29，片③） | ✅ | 三道闸全装在**唯一落盘口** `saveCandidateFile`（反思链与提案链共用；挂在调用点上会给最忙的那条留空档 —— 交叉验证 R-A3 实测即此形状）。合并稿按设计豁免于"自己并掉的来源" |
| 模型预筛 + 合并稿 + 按簇批准（片④） | 🚧 | `memory/prescreen.ts`（句柄制，路径不出境）+ `memory-store.ts · runPrescreen` + `MemoryManager.tsx` 簇视图；判据分两层：形状层 **28 个 `it`（含一处 6 例参数化 ⇒ 运行时 33 条）单测** + 真渲染 5 条（整理走 IPC、簇成行、看来源、批准一簇把来源一起收掉、覆盖提示可见），**分组质量层零判据**（plan55 §六）。合并稿批准只写 `delete(candidate: true)` 不动存活率账。剩余 🚧 的那半是：拿盘上 71 条存量做一次真人验收（#82） |
| 来源候选读侧留痕 / 上限不锁死整理入口 / `conflictWith` 只认 notes 区（交叉验证 R-A1/A3/A4） | ✅ | 同批补：候选解析失败也进「未能加载」；队列满 50 时合并稿免计上限（否则整理出不来、只能逐条手删）；指向归档件或越界的 `conflictWith` 不认指针、按新条目另存 |
| `npm run bench:all` 聚合基准（plan26 S4 兑现） | ✅ | 0.13.62（`ece9b13`）：`scripts/bench-all.cjs` + `package.json` 脚本；JSON + Markdown 双报告、与上次读数对比、首跑标 baseline、随报告输出环境对照（裸同步读 ms/文件）。**§四 旧补记把「bench:all 聚合报告」登记为 P5 未做，2026-09-21 改正为已完成** |

**⑦ 记忆页只做审批：折叠 / 提示出口 / 可恢复批量拒绝（plan56 / D-142 / D-143 / D-144，2026-09-26 新增）**

缘起是用户装上 0.13.89 点验后的原话："还是很麻烦……需要你过目的条目会冒红色，但我却没有什么交互的选项……已经存入的记忆，没必要直接显示出来。" 三条根因回代码复验成立，指向同一件事：**这一屏兼了审批工作流与库浏览器两个职**。判据口径与 ⑥ 同一句：形状可测、手感不可测。

| 功能 | 状态 | 证据 / 说明 |
|---|:--:|---|
| 已生效列表默认折起（片① / `8bc4bfc`） | ✅ | `MemoryManager.tsx` 与归档区同标准（此前同页两套：已生效 25 条全平铺、归档区有折叠）。**收起的是列表，读数不收起** —— 条数与「本次新增」巡检区常驻。真渲染判据 +2（417→419） |
| 「需你过目」每行三动作 + 顶部「全部看过」（片② / `468e241`） | ✅ | 消提示落 `review_dismissed` 事件，**不改条目、不落 `delete`**；「全部看过」先弹确认并写明条目仍生效。这一格在 0.13.89 里只有文字、零按钮（K36 那次只做了分家、没做出口 ⇒ "更准"变成"更刺眼"，教训已进 `learnings.md`）。真渲染判据 +4（→423） |
| 看过判据 = **内容指纹**不是 `updatedAt`（复查轮 P1 / `0e79a71` / D-144） | ✅ | `reviewSeenStamp`：守卫所见的四段文本 + 是哪个文件，FNV-1a + 长度（不引 `node:crypto`，因 `src/shared` 被渲染层 import）。**判据配成互反的一对**：只改正文不动时间戳 ⇒ 必须重新出现；只换时间戳正文没动 ⇒ 不再打扰。原按 `updatedAt` 记 = 另一种静音键（手改/Agent 改文件都不刷新它，塞凭据进正文后守卫重新命中却不冒头） |
| 一键拒绝未成簇 + `memory/rejected/` 可恢复（片③ / `85fc161` + `8960c4d`） | ✅ | **逐字节搬进**、四处互斥、不并入 `insideMemory`、不占 `maxCandidates`、不落事件；「最近拒掉 M 条 ▾」逐条放回，放回时若队列已有同名需先处理那一条；清空回收站二次确认并说清不可恢复。合并稿与其来源**不受该键影响**。真渲染判据 +6（→429）+ 桩路径同步改造（见下 ⚠️） |
| 单条「看过」失败时的提示文案 | 🚧 | **已写修复、未验证其强度**：要测它得造出"主进程说这条没了、界面上还挂着"的形状，而门禁是隔离进程、状态由桩自己发 ⇒ 造不出来。**不许当成"有判据"** |

> ⚠️ **本批三条流程结论（判据工程侧，登记于此防止重走）**
> 1. **预登记判据必须写明验证层**（单测=主进程语义 / 门禁=界面接线 / 真人=手感）。`plan56` §四 曾把"让看过顺带删条目 ⇒ 判据必须红"指望在门禁上，实测单测 4 红而门禁 429 全绿 —— **门禁不加载 `src/main`，对主进程语义变异结构性免疫。不是判据弱，是挂错了层。**
> 2. **桩不许保留原路径**：门禁把候选"搬进回收站"却在桩里留 `/mem/candidates/...`、恢复时按 name 反推目标 ⇒ 越界那道闸在门禁层一步没走，界面判据全绿、测的是桩的想象。**桩是契约的复制品，形状变了要跟着变，路径也要跟着变。**
> 3. **判据会跟着实现一起错**：那条"改过正文要重新出现"的测试，改正文时顺手换了 `updatedAt`，于是实现按 `updatedAt` 记的 P1 谁都没抓到。⇒ 写判据前先问"这个缺陷在现实里怎么走"，能配成**互反的一对**就配一对。

> ⚠️ **安全边界（照抄 README 的如实声明，这里不打折）**
> 三道防线是**必要非充分条件，不是"已解决"**：判定是关键词与形状匹配，**可被同义改写绕过**；
> 只读档也具备**出境与操作远端页面**的能力；记忆文件是 userData 下的**明文 markdown**，本机任意进程可读。
> 自动捕获会**读会话正文**，且反思本身是一次**出境的模型调用**。巡检区是给用户的习惯，护栏不是承诺。

### 设置与系统集成

| 功能 | 状态 | 出处 / 说明 |
|---|:--:|---|
| 外观：主题 + 字号四档（rem 基缩放）+ 系统字体选择（注册表枚举 + 白名单清洗） | ✅ | plan7 F3（0.13.33）；四档源码核实：`shared/splitter.ts` `sm/md/lg/xl` = **87.5 / 100 / 112.5 / 125%**（小档取 87.5% 而非 85%，因为 14px 换算 12.25px 会发虚） |
| 电脑控制开关（允许模型操控鼠标键盘与桌面应用） | ✅ | 0.13.45；自视段会向模型报告该状态 |
| 导入其他记忆（把别的 AI 工具的长期信息搬进来，两步） | ✅ | 0.13.42；设置 → 记忆 |
| 系统集成：锁屏运行 / 熄屏保持唤醒 / 开机自启 | ✅ | plan7 F1 / plan15 |
| 网络代理：三档 + 11 处 fetch 出口收口（可注入） | ✅ | plan7 F2（0.13.32） |
| 存储位置：更改 / 下次启动迁移 / 恢复默认 / 撤销 / lastEvent 提示 | ✅ | plan10 C（0.13.35，`9a3af4d`） |
| 设置窗口出口收敛：删自绘 ×（与系统标题栏 × 重复易误关应用），保留标题锚点 + Esc 关窗 | ✅ | 0.13.36（用户反馈双 X） |
| 默认工作空间（新任务落点，可恢复内置默认） | ✅ | plan7 F4① |
| 界面文案专业化（两轮 39 文件 206 条） | ✅ | plan7 批 E-文案 |
| **开发环境：运行时探测 + 选中项注入 Agent 执行环境** | ✅ | plan43 S1+S2（`c8e913d`，0.13.65）与 S3（`63b5e92`，0.13.77）：`dev-env/runtime-detect.ts · detectRuntimes` / `filterNoise`（六类来源 PATH / conda / nvm / volta / pyenv / uv 并发发现，版本一律 `--version` 实测，商店占位符排除，证明不了来源的归「其他」默认折叠），注入走 `dev-env/runtime-bin.ts · syncRuntimeBin` 的 shim 目录（S3a 纯函数层 `shared/runtime-path.ts`，S3c 三条线接入：`terminal-session.ts` / `shell-session.ts` / `background-tasks.ts`，组合根 `index.ts · resolveRuntimeEnv` 单点共享）；IPC `dev-env:detect` / `dev-env:select` / `dev-env:active`，界面 `components/DevEnvSettings.tsx` + `RuntimeSelect.tsx`。**没有「＋」按钮是立场不是缺陷**（探测是唯一入口，不代装）。✅ **作用域已定（`plan43` S3-3，2026-09-24 D-136）**：跟随的是**本项目要跑的代码**那四条通路（内置终端 / `run_command` / 后台任务 / 子 Agent），**MCP server 不跟随**（终态：shim 目录里没有它能用的命令，换了只会在它身上换走现有环境，而它断连在对话现场不说一句），LSP **当前是空集**（主进程无该进程）只预登记"引入时建议跟随"；另有三处**故意的不跟随**（探测层跟随会让配置自我锁死 / git 是工具链 / ripgrep 走捆绑二进制本就无可跟随），判定表 9 行在 `plan43` §8.2b，设置页 `.de-scope` 那句披露有真渲染判据。**计划与源码现已同步**（此前本行那句"计划滞后、状态行仍写 🚧 S3 实施中"已失效——`plan43` 09-24 起标 ✅，只剩 S3-2「存储双轨」另开一批） |
| **权限档变更即时广播到界面** | ✅ | plan40 S3（`ea4d13d`，0.13.57）：`shared/ipc.ts` 的 `SettingsChangedKind` 增 `'permission'`（现为 `'settings' \| 'ui-prefs' \| 'models' \| 'permission' \| 'devEnv'`，跨窗广播单一真相源），`components/TerminalPanel.tsx` 收到即重取权限并重 boot——消掉「横幅写着只读、界面仍显示运行中」的窗口期 |
| **设置窗口独立命名与出口收敛（禁最小化 / 最大化）** | ✅ | 0.13.75（`1113b3b` + `d401c1b`）：窗口标题锁为「设置」（渲染入口是单一 HTML，文档标题会覆盖窗口标题声明，两窗在任务栏必须可区分）；只保留系统关闭按钮——「-」会让任务栏缩略按钮滞留在屏幕左下角遮挡内容。应用内「返回」与 Esc 出口不变 |
| **侧栏设置入口由齿轮改文字框** | ✅ | 0.13.72（`45a0699`）：`components/Sidebar.tsx` 的设置入口；刻意不做 `.active` 选中态（设置是独立窗口，「已打开」由窗口自己表达） |
| **「电脑控制」设置文案与现状对齐** | ✅ | 0.13.69（`053ec31`）：删掉「当前版本尚未搭载电脑控制工具」的失真表述，如实写明工具由外部 MCP 服务（windows-mcp）提供、**开关是权限闸**，未添加服务时当场提示 |

### 数据与健壮性

| 功能 | 状态 | 出处 / 说明 |
|---|:--:|---|
| 数据目录可配置：排除法迁移 + 三段式校验（SHA-256 + 独立清点）+ 回退反向迁移 + restore-backup 保全 | ✅ | plan10 C |
| location.json 配置自举（损坏回退默认绝不启动失败；指针失效回退保留指针） | ✅ | plan10 C |
| 数据目录锁（wx 独占 + pid 存活 + 陈锁自愈），与单实例锁语义分离 | ✅ | plan10 C + plan8 R13 |
| 崩溃守卫 / 进程树管理 / URL 防护 / 原子写 + fsync | ✅ | plan8 |
| 会话绑定工作区 + 工作区授权边界（resolveInsideWorkspace） | ✅ | plan1 P1 |
| **卡顿治理 S0–S2：事件循环看门狗 / 内置搜索异步化 / 关窗先隐藏** | ✅ | plan37（`99c6031`，0.13.54）：`src/main/watchdog.ts · startWatchdog`（单任务占主进程 > 1s 即记停滞时长与所处阶段，落 `app.log` 的「事件循环停滞」）；内置降级扫描器 `retrieval/search.ts · searchBuiltin` 全程异步、按 `yieldEveryFiles` 用 `setImmediate` 让出；关窗 `index.ts · installFlushBeforeClose` 先 `win.hide()` 再后台 flush，flush 期间双击图标由 `show()` 接回 |
| **卡顿归因：同步块探针 + IPC 面包屑 + 探针心跳** | ✅ | plan49 §11（`38429e4` 埋点 + `80b0f25` 心跳，0.13.77）与 0.13.74（`e7606b9` 面包屑）：`src/main/sync-trace.ts · installFsSyncTrace` 在组合根一处 monkeypatch `node:fs` 的同步函数（`index.ts` 启动时装，110 处调用点逐点打点必漏）；告警额外报 `block`（横跨停滞起点的嫌疑区间）/ `traced` / `tracedMs` / `dropped`；`watchdog.ts` 的 `breadcrumb` 记每通道进出，`HEARTBEAT_MS` 每 5 分钟落一条 INFO，**自启动以来零记账则改报 WARN**——「这次没抓到」与「探针没生效」从此可分。**真凶仍未定位**，本轮交付的是下一轮抓不抓得到（见 §二） |
| **渲染层回调引用稳定性守卫（防自激重绘循环）** | ✅ | plan49 L1+L3（`bb1f79d`，0.13.76 提交 / 0.13.77 出包）：静态扫描测试 `tests/unit/render-callback-stability.test.ts`（含定义点回查，堵住用变量名包装的绕过），最小复现 `tests/bench/render-callback-loop.mjs`（实测 0.3 秒 200 次 vs 稳定引用 1 次）；对话页内联箭头回调改稳定引用（CHANGELOG 记 0.13.76 真机日志：12 分钟 166 次卡顿告警、累计占死 635 秒、单次最长 9.8 秒） |
| **会话运行态收口：存档旗标落回 / 删除会话不留收尾 / 撤销回滚硬闸** | ✅ | K9（`5dbc5ab`，0.13.82）：`store.ts · settleRuntime` 在三条结束路径（`markDone` / `markError` / `stopStreaming`）落回该条的 `streaming`，`stopStreaming` 用函数开头捕获的 conversationId（中间有 await，期间可能已切会话）；同批堵掉 `newSession` 换 `activeId` 前不收档导致**已输出回答被覆盖成空**的数据丢失。K10（`7c3af06`，随 0.13.85 发出）：`store.ts · removeConversation` 摘掉 `runtimes` 条目，`ipc.ts · IPC.convDelete` 先 `chatGate.abort(id)` 再删（只 abort 不 end，位子由那一轮的 `finally` 归还）。K11（同批）：`ipc.ts · IPC.convUndoRollback` 补上与 `convRollback` 同一道 `chatGate.isRunning` 硬闸，`sendMessage` 清 `rollbackNotice` |
| **空正文出境的发送侧与渲染侧双防线** | ✅ | K8（`3a7898a`，0.13.81）+ plan36 坑 3（`fba57d8`，0.13.58）：`schemas.ts` 只在「空正文**且**无分段」时拒（分段在场的轮次合法），预算降级时**分段不参与丢弃**以免整批保存被拒；渲染层过滤历史消息与落盘侧同判据（0.13.59 `4ac2cb6` 一批修掉「只有思考/工具、无正文」的中间轮被界面过滤 → 下标与磁盘错位） |
| **e2e 主干冒烟 + Linux headless 独立 CI job（R6 可行性量完）** | ✅ | plan48 S0–S2（`5669f91` 可行性骨架、`939b9a9` CI、`7888ce8` 主干 4+1 条，0.13.75）：`tests/e2e/boot.spec.ts` + `smoke.spec.ts` + `helpers.ts` 驱动真入口 `out/`，配置 `config/playwright.config.ts`，CI job 见 `.github/workflows/ci.yml` 的 `e2e`（xvfb + 不跳 Electron 二进制），脚本 `npm run test:e2e` / `test:e2e:xvfb`。headless 可行性结论：Linux 上跑得通 |
| **Linux CI 跨平台测试红清零** | ✅ | 0.13.77（`83ee58a`，plan43 S3 尾巴）：11 条用例把 Windows 平台与系统临时目录混在同一次调用拼出非法路径——修的是测试本身，产品代码两平台本就正确；修完 Linux 反而真正覆盖了原本跳过的 POSIX 分支 |
| **安装包产物名 ASCII 化** | ✅ | `6dadeaa`（0.13.80 出包）：`config/electron-builder.yml` 的 `artifactName` 改 `Jiushililu-Setup-${version}.${ext}` / `Jiushililu-Portable-${version}.${ext}`——中文产物名会被 GitHub Release 剥前缀成坏下载链接 |
| **单会话巨型文件基准档（plan10 R1）** | ✅ | 2026-09-14（`963eed9` + `da5fcd7`）：`tests/bench/conversations.bench.ts` 的「巨型 1MB / 5MB / 10MB」三档，**判定阈值跑前预登记**（10MB 档 save P95 > 500ms 或堆增量 > 6 倍文件 ⇒ 分片追加立项）。原列 §二「🚧 未开始 / TaskList #5 挂起中」，2026-09-21 反向验证改正；处置结论见 `plan8`：**分片追加不因性能立项** |
| **问题一「粘滞」量测三件套（plan29 S4，只测不改）** | ✅ | `fd9563a`（0.13.50 批）：`scripts/bench-switch-costs.mjs`（atomicWrite 四步复刻微基准）+ `scripts/bench-live-cdp.mjs`（CDP 驱动端到端）+ `scripts/bench-live-decompose.mjs`（逐 IPC 计时 + longtask 观察器 + resize 洪水）。净室读数：会话切换端到端中位 749ms，**渲染层 ≈97%**、IPC 串行链 ~2%、磁盘 I/O ~1%（50KB 含 fsync）——该读数即 plan30 的立项判据。原列 §二「🚧 进行中」，2026-09-21 改正 |

---

## 二、待实现（按计划台账）

> **2026-09-21 本节做了一遍反向验证**（`AGENTS.md` §四：「标未做的真的没做吗」才抓得出漏报）。
> 结果：本节原有 **14 行，其中 5 行失真**——4 行整条早已交付（plan10 R1 / plan28 S2 / plan29 S4 / Token Saver 校准 v2），
> 1 行部分交付（问题四记忆去重）。漏报比虚报多，与 09-15 那次复验的分布规律一致。
> 完成的上移 §一、原地留一行改正痕迹（不静默删行）；确实没做的把状态位改准
> （旧写法「🚧 未开始」与「📋 只登记」都不在四值口径内，一律改正并留原状说明）。

| 来源 | 条目 | 状态 | 备注 |
|---|---|:--:|---|
| plan12 | ~~⑤ Agent 自建目标 + ⑥ 与目标/待办联动~~ | ✅ **已交付** | **原写"未开始 / set_goal 全仓零命中"，实际 0.13.37（`ec10294`）已落地** —— 本行 2026-09-15 移出待实现，见 §一 Agent 内核节 |
| plan8 | ~~Token Saver 校准方案 v2（每臂 ≥3 次比中位数 + 报告轮数/调用次数）~~ | ✅ **已交付** | **原写「🚧 未做」并注「n=1 A/B 对照组抖动 3.3 倍，量不出结论」，实际 harness 与两轮实测 2026-09-13 就收了口**（`scripts/bench-tool-window.cjs` 的 `REPEAT` / `median`，读数与结论在 `PLAN/plan8_健壮性与技术债.md` §七⑤）。2026-09-21 移出待实现；「8k/12k/0.72 保持现状」正是这次校准的结论，不是没做 |
| plan10 | ~~R1 单会话巨型文件测量（分层方案前最后一次机会）~~ | ✅ **已交付** | **原写「🚧 未开始 / TaskList #5 挂起中」，实际 2026-09-14（`963eed9` + `da5fcd7`）已补测** —— `tests/bench/conversations.bench.ts` 有专门的「巨型 1MB / 5MB / 10MB」档且阈值跑前预登记。2026-09-21 移出待实现，见 §一 数据与健壮性节 |
| plan28 | ~~S2 run_command 会话复用~~ | ✅ **已交付** | **原写「⏳ 未开工（需哨兵 EOF / 退出码提取 / ANSI 清洗 / 树杀一整层 shell-integration）」，实际 0.13.53（`f46d6a6`）走的是另一条路** —— 没有做哨兵协议，而是 `agent/tools/shell-session.ts · createShellSession` 起常驻真 shell 会话并按 run 复用，起不来即回落一次性执行（`IDLE_MS` 10 分钟回收、`MAX_LIVE_SESSIONS` 4）。计划表达不成不等于能力没落地，2026-09-21 改正 |
| plan29 | ~~S4 问题一「粘滞」量测（只测不改）~~ | ✅ **已交付** | **原写「🚧 进行中（2026-09-16）· 结果回写 plan29 §七」，实际 `fd9563a` 已定案**（渲染层 ≈97% / IPC ~2% / 磁盘 ~1%，净室中位 749ms），结论已回写 `plan29` §八 并直接导出 plan30。2026-09-21 改正 |
| 问题四 | ~~记忆库去重 / 同义归一（批准列表刷屏）~~ | 🚧 部分交付 | **原写「📋 只登记」——`📋` 不属四值口径，且登记状态本身失真**：plan33 已于 0.13.52（`beddb6a`）交付**写入相似度闸门 + 面板「疑似重复」一键合并**（`memory/similarity.ts`）。字面这一层判据是 bigram 相似（`MEMORY_SIMILAR_THRESHOLD` 卡 0.7，换措词即漏）；**候选区内的语义级同义归一已由 plan55 片④ 的模型预筛接住**（分组 + 合并稿 + 按簇批准，未发版）；**已生效条目库的语义归一仍无**（面板「疑似重复」走的还是字面 bigram），
  但那份残余保持 🚧 的理由换成实的：**分组质量没有判据可测**（单测与门禁只测形状），且存量队列仍待一次真人验收 |
| — | **i18n 全量抽词（中 / 英双语 + 设置页可切换）** | 🚧 进行中 | **原写「🚧 单独评估」，现况：已立项为 `PLAN/plan52_中英双语.md`（待办总览 G3）**，缘起是用户 2026-09-17 10:33 明示「要做双语言版本的（中/英）软件，通用设置页可选择」。三问已于 2026-09-19 20:11 答完（界面文案与报错必翻 / 日志不翻；内置技能双语字段照做；第三语言留接口不留实现）。**工作区进度**：S1 已落 `src/renderer/src/i18n.ts · ensureI18n` + `src/shared/i18n/zh.ts`（真源在 shared，主进程产出用户可见串要复用同一张表）、语言偏好键在 `shared/splitter.ts` 的 `LOCALE_KEYS` / `sanitizeLocale`；缺译不静默（`parseMissingKeyHandler` 打警告并回退中文）。⚠️ **截至 2026-09-21 本端核对，该批仍是未提交改动**（`src/renderer/src/i18n.ts` 与 `src/shared/i18n/` 在 `git status` 里是 untracked，HEAD 停在 `7c3af06`），**故无版本与提交锚点**；另一端报过两个提交号，在本仓库对象库里 `git cat-file` 都查不到（`2026-09-21` 实测不可解析，故此处**不落号**——按 `AGENTS.md` §四「引用提交号前先确认它可解析」），**等它落到本端可解析、且 CHANGELOG 出现对应版本条目时再补**。全量抽词（S2 起）未开始 |
| plan7 A3 | PDF 与音视频预览**评估** | ✅ **评估已做完**（2026-09-24）· 功能本身按结论不排期 | **两个状态要分开**：① **评估**已做（见 `待办总览` E1 / E2）—— PDF：实测 `pdfjs-dist@6.3.289` 解包 **33.2 MiB** vs 安装包 109 MB ⇒ **不引**，而 `.pdf` 本就不在三张白名单表里、已落在二进制出口（十六进制头 + 「用系统程序打开」），零新依赖零改动；音视频：卡点是 CSP `media-src 'self'` 与"视频不能整读成 dataUrl"的**合取**，要先有一条能响应 `Range` 的本地协议（一条新的资源加载面），故挂起并写死触发条件。② **预览功能**确实仍未做 —— 本行 09-21 那句"零命中，确实没做"**至今成立**，只是它回答的是②不是①，当时把①一并标成 ⏳ 是**把"没人评估过"和"没人做过"混成一格**。PDF **编辑**属 plan21（A2）长期备做（D-112，2026-09-18 裁决：只改排期不改方向） |
| plan7 E | UI 精修剩余：**消息气泡 / 空状态 / 加载态 / 焦点态**（纯视觉） | 🚧 | **2026-09-15 拆分**：原条目里的「间距节奏」「字号层次」两项**已随 R7 收口完成**（`6d3a899` 363 处 + 四档缩放），移出本行；剩下的四项属 P5 "纯视觉"，在 R7 结束后才轮得到。**2026-09-21 补读数**：`styles.css` 现有 `focus-visible` 规则 5 处、`FieldNote.tsx` 已带 `:focus-visible` 键盘可达——焦点态**有零散收口但未成体系**，不因此判完成 |
| plan16 后续 | SCM Diff 复用 Monaco 编辑器（当前为内联着色渲染） | 🚧 | 理由见 plan16 §七。**2026-09-21 复核未变**：`components/ScmPanel.tsx` 顶部注释仍写「本批用内联着色渲染 unified 文本，没有复用批 B 的 Monaco 差异视图」 |
| plan8 | 「重复执行的同一命令」更明确提示（需跨轮状态） | ⏳ 未开始 | 原写「🚧 保留」。2026-09-21 全仓 grep「重复执行」零命中，确实未做；要做先想清楚状态放哪层 |
| plan8 | 「本会话总是允许」 | ⏳ 未开始 | 原写「🚧 保留」；等实际反馈，做也应为「记住这条具体命令」。2026-09-21 全仓 grep 该话术零命中 |
| plan8 | ~~设计 token 色值定稿（朱砂深浅 / 水印大字）~~ | ✅ **已了结**（一半定稿、一半取消） | **朱砂深浅已定稿** `#a8342c`（`styles.css:98`，墨色层次 + 朱砂点睛的用色体系早已兑现）；**水印大字用户 2026-09-15 明示「不做」**（D-049），从待办移除。此条 2026-09-15 关闭 |
| ~~plan44 S2b~~ | 桌面动作**截图缩略图回显** | ✅ **已交付**（2026-09-24 02:57 `dd75518`，未发版） | 本表 §工具与生态 已有一行 ✅ 记其形态（引用进 `segments` / 正文落 `mcp-artifacts/` / **不进模型上下文**）。此行原标「⏳ 未开始」是**该批收口时漏清**——同一张表里 ✅ 与 ⏳ 并存，读下面的人会以为还有一批没做。端到端仍待一个会返图的真 MCP server（已并入出包前真人清单 D8） |
| plan49 | **主进程卡顿真凶未定位** | 🚧 | 探针与心跳已就位（§一 数据与健壮性），但 0.13.77 的 20.5 分钟真机跑**停滞 0 条**、`traced` 有记账 ⇒ 现口径是「没抓到」而非「没发生」。判据读法见 `plan49` §11.3，盲区扩面走 §11.4；真人式实测报告 `docs/实测-web-chat-lab-26.09.20.md` |
| K12 | 界面标记类判据（侧栏「正在生成」/ 并发提示）只有单测在守 | ⏳ 未开始 | 触发条件写死在 `PLAN/待办总览.md`：下一次再动 `Sidebar` / `runtimes` 相关代码时**同批**把判据补进 `scripts/verify-shot.cjs`，别第三次补 |
| K13 | 隔离验证进程缺 `skills-disabled:get` 桩 | ⏳ 未开始 | **2026-09-21 复核仍未做**：`grep skills-disabled scripts/verify-shot.cjs` 零命中，该通道在真渲染门禁里拿 undefined ⇒「被停用的技能」这一维从未被测过。与 `AGENTS.md` §多窗口架构「新增 IPC 通道 = 门禁清单里的一步」同源 |
| plan35 | RULES 分层规则机制（A4） | ⏳ 未开始 | 开工序 S3 软版本 → S2b → **A4** → A1（D-112，2026-09-18 裁决）；动工前置=理清与 plan24 / plan29 的边界 |
| plan20 | IDE 能力（A1：多页签分屏 / LSP / DAP 调试 / AI 内联编辑） | ⏳ 未开始 | 压批待稳定期（D-112）：稳定基线跑过体验期 + 外部反馈收拢后再做，届时开分支。**这是 §三「运行 / 调试」那一行的实际归属** |
| plan21 | Office 能力（A2：Word / Excel / PPT / PDF 编辑） | ⏳ 未开始 | 长期备做，挂 V 储备区；触发条件=真实 Office 编辑需求出现，或 plan20 四批全闭 |
| 记忆去重（判据 10 环境漂移） | ~~满载 100 条时基准超线是否调阈值~~ | ✅ **已裁决** | 2026-09-18 00:55 用户裁决「先裁判据 10」→ **D-106 已登记并落地**（`6421bf1`，0.13.63 的 (mtimeMs, size) 读盘缓存），未调阈值。**此条 2026-09-21 销项**，实现见 §一 记忆⑤ |

---

## 三、长期愿景（D-034③，2026-09-12 用户明示）

| 愿景 | 拆解 | 现状 |
|---|---|---|
| 像 VS Code 一样写代码并运行代码 | 编辑器 | ✅ Monaco（批 B）+ 页签保活（plan30，0.13.51） |
| | 运行 | ✅ **大半**：真 PTY 终端（批 C / plan14）+ `run_command` 持久 shell 会话（plan28 S2，0.13.53）+ 选中运行时注入 Agent 执行环境（plan43 S3）——"改完就跑、跑完看输出"这条链路是通的 |
| | 调试（断点 / DAP） | ⏳ **未开始，但已不是"未立项"**：2026-09-21 校正——原写「❌ 未立项」，实际已登记为 **plan20 E3（DAP）= 待办 A1**，用户 2026-09-18 裁决 D-112 **压批待稳定期**（触发条件=稳定基线跑过体验期 + 外部反馈收拢，届时开分支）。四值口径下这是 ⏳，不是 ❌ |
| | Git 集成 | ✅ 大半（SCM 基础闭环已交付：变更列表 / 暂存 / 提交 / 待推送计数）；Diff 复用 Monaco 仍 🚧（见 §二） |
| 能自定义子 agent 并切换 | 内核（`.agents/*.md` 定义 + 调度器） | ✅ **口径更新**：子代理的工具集现在**真按各自声明装配**（plan51，0.13.78），plan6 D4 的「审查型只给读、执行型才给写」自此成立 |
| | 管理与切换界面 | ✅ **plan17 已交付**（`AgentManager.tsx` → 设置页分区，2026-09-15 改正：原标 ❌ 未立项是滞后）；**分层自 0.13.60 起为两层（内置 / 自定义），项目级已取消**（D-103 ②，`175dac7`） |

---

## 四、对账说明

- 「目标（Goal）」与「批 A3」「plan11」曾出现**台账滞后**（实际已交付但计划标待做），2026-09-13 全项目对账（`docs/交接-全项目对账-26.09.13.md`）已改正——本表的 ✅ 均以计划状态行 + 源码双证为准。
- **2026-09-15 源码级复验补记**（本次与前几次的关键差别见表头）：查出 **5 处滞后**，均已就地改正并在对应行留痕 ——
  1. **Goal ⑤⑥**：谓"未做 / `set_goal` 全仓零命中"，实际 0.13.37 已交付
  2. **子 Agent 管理界面**：愿景表谓"❌ 未立项"，实际 plan17 已落地并接入设置页
  3. **水印大字**：谓"待用户圈定"，用户已明示**不做**（D-049）
  4. **朱砂深浅**：谓"待定稿"，实际早已定稿 `#a8342c`
  5. **P4 记忆与进化整层缺失**：原表停在 P2，缺 0.13.41–0.13.48 的整个记忆闭环（已新增专节）

  **教训**：这批失真全部集中在「**等某人拍板的条目**」与「**别的文档会顺带更新的条目**」——
  前者因为没有明确归属而漂着（水印漂了三天），后者因为有人改 README / CHANGELOG
  就默认被覆盖（其实每张表各管各的）。**凡写着"待圈定 / 待用户决定"的行，
  应当自带一个"问过没有"的时间戳**，否则它会永远挂在那里装作还在评估。
- **2026-09-16 追平补记**（P4/P5 增量）：新增「用户画像 + eval 集」专节（记忆④）、
  时间线页签、执行事件流 / 智能标题 / 滚动摘要三行（Agent 内核）。同批清偿的历史欠账：
  plan8:550「裁剪没有可见性」→ trim 事件带 droppedCount/bytes/summarized；plan25 判据 10
  「真渲染门禁未跑」→ plan26 S2 以 verify-shot 338/338 兑现。
  **P5 余项**（bench:all 聚合报告）登记为未做（按协作裁决 S3 即停挪后）。
- **2026-09-16 二次追平**（分支 `feat/p5-tooling-ux`，未合并）：Agent 内核表新增
  计划批准（plan27）/ edit 工具与命令超时（plan28）/ 流守卫与档位名实相符（plan29 S1/S2）四行；
  待实现表新增 plan28 S2 会话复用、plan29 S4 量测、问题四（记忆去重）三行。
  同日裁决：**AGENTS.md / CLAUDE.md 纯本地化不入 git**（D-093/D-094）——本表其余行号引用
  若指向 AGENTS.md 章节仍有效（文件只在本地）。
- **2026-09-16 三次追平（0.13.51 收口）**：工作台表新增「页签保活 + 渲染性能修复」行（plan30 两批 +
  布局回归修复）；同日量测定案问题一占比（渲染 97%）并出 0.13.50/0.13.51 双包。
- **2026-09-21 追平（0.13.51 → 0.13.83）**：本表基线当时停在 0.13.49 / 正文覆盖到 0.13.51，
  与 `package.json` 的 0.13.83 **相差 34 个修订号**。方法照表头那套（不读计划怎么说，直接去源码找符号），
  逐条走 `CHANGELOG.md` 的 **0.13.52–0.13.83 共 32 个版本条目**，提交面用 `git rev-list --count c7e94b0..HEAD`
  = **84 个提交**交叉（`c7e94b0` 是 0.13.51 的收口提交）。
  ⚠️ 远端 tag 在 0.13.52 之后只有 13 个（v0.13.58 / 59 / 68 / 69 / 70 / 71 / 74 / 75 / 77 / 78 / 80 / 81 / 82），
  **版本归属一律以 CHANGELOG 为准**——`git tag --contains` 会把早期提交算到很晚的 tag 上，不能用它反推。

  **这次查出的滞后，按 `AGENTS.md` §四 四条规律归类**：

  1. **整层缺失（P3 生态）** —— 技能系统（plan22）、MCP 客户端（plan23）、规则系统（plan24）随 0.13.50 出包，
     0.13.60 又补齐全套管理 GUI 与「真禁用」（plan34），而本表**连一行都没有**。与 09-15 的「P4 整层缺失」同类，
     已新增「生态与扩展（P3）」专节。
  2. **★ 漏报（标未做、其实做了）共 6 条** —— 全部出自反向验证（把「未开始 / 进行中」逐条拿去源码找证据）：
     plan10 R1 巨型文件测量（`963eed9` + `da5fcd7`，2026-09-14）、plan28 S2 命令会话复用（`f46d6a6`，0.13.53）、
     plan29 S4 粘滞量测定案（`fd9563a`）、问题四记忆去重（`beddb6a`，0.13.52）、
     Token Saver 校准 v2（`scripts/bench-tool-window.cjs`，2026-09-13 两轮中位数实测）、
     `bench:all` 聚合基准（`ece9b13`，0.13.62，而 09-16 的补记把它写成「登记为未做」）。
     **这 6 处全部出自反向验证**；正向一面（"标完成的真完成了吗"）本次**零查出**——无一处虚报，
     失真的方向仍然是「落后的未做标记」而不是「冒充已做」。
  3. **口径变更没回写到旧行** —— 子 Agent 管理界面原写「三层 CRUD」，0.13.60 起项目级取消、回归两层（D-103 ②）；
     时间线原写「工作台页签」，0.13.74 起在主对话头部（`e7606b9`）；会话大纲原写「点开式悬浮列表」，
     0.13.60 起被右缘常驻刻度条取代（`bb33716`）。三处都在原行内就地改正并保留原描述。
  4. **「等某人拍板」的条目漂着** —— i18n 那行自 09-15 起写「单独评估」，实际用户 2026-09-17 10:33 已明示要做双语、
     09-19 20:11 三问答完、现已立项 `PLAN/plan52_中英双语.md`（待办 G3）且 S1 在工作区进行中，照实改标 🚧。
  5. **状态位与四值口径不齐** —— 旧写法「🚧 未开始」（plan10 行）、「📋 只登记」（问题四行）、
     愿景表的「❌ 未立项」都不属 `AGENTS.md` §四③ 的四值；本次统一为 ✅ / 🚧 / ⏳ / ❌ 不做，
     被改动的行都留了「原写什么」的说明，表头图例同步改写。
  6. **顺带把规律 1 用回本表** —— §二 里凡"待用户裁决 / 待开工"的行，本轮起带登记日期与触发条件
     （K12 / K13 / plan44 S2b / A1 / A2 五行按此写）。

  **本表的实测规模变化**（`wc -l` + `git diff --numstat`，2026-09-21）：全文 **211 → 350 行**；
  表格行新增 **87** 条、删除 **23** 条（净 **+64**），其中带 ✅ 的新增行 **68** 条、
  这 68 条里 **53** 条带 0.13.52 之后的版本锚点（其余为 §二 的改正留痕行与 §三 的愿景拆格）。
  §一 新增了一个专节（生态与扩展 P3）与一个专块（记忆⑤）。

- 未列入本表的原则性约定（决策权归属 / 布局三段 / 规则单一真相源等）见 `AGENTS.md`。

### 更新惯例（绑定触发点，2026-09-21 起）

> **为什么必须有触发点**：`AGENTS.md` §四 规律 2 的实证——`CHANGELOG.md` / `README.md` 每版必须动所以从不落后，
> 本表没有硬触发点所以**一烂就是 32 个版本条目没同步**（0.13.52–0.13.83，见表头与上面的追平补记）。
> 旧规矩「每批收口时同步本表」早在 0.13.36（`495eadb`，2026-09-14）就写着，此后再没被同步过一次——规矩靠人记得，等于没有规矩。
> 本节据以替它。

1. **发版即动表** —— `npm run dist` 前，表头的「对账基线」版本必须等于 `package.json` 版本，
   否则出包步骤失败（这道闸由脚本执行，不靠人记）：
   `scripts/check-docs-fresh.cjs`（= `npm run check:docs`，接在 `npm run dist` 的第一环）
   用正则取 `> 对账基线：**x.y.z**` 里的第一个版本号与 `package.json` 比对，**读不到版本号同样判红**
   （改表头那行的格式 = 改闸的接口，两边必须同批改）。同一道闸同时核对 `README.md` 的加粗版本标记。
   闸门**只挡出包、不进 CI**：CI 挡"改动本身坏没坏"，"对账表追平没追平"属发版动作。
2. **批收口即动表** —— 一个批次做完，凡该批新增能力**同批**进本表，不做事后补账；
   新增行必须带**版本 + 提交号**，且引用的源码符号是**落笔当时 grep 得到的**（禁裸行号，写文件 + 符号名）。
3. **符号失配即改正** —— 本表条目引用的源码符号若被改名或删除，**改正当批完成**，不留"回头看才知道"的行。
   遇到推翻性决策（不是补充，是反转）时按 `AGENTS.md` §四 规律 4 附一个动作：
   **全库 grep 被推翻的关键词，逐处判定并标注**（例：改子代理分层口径 → `grep -rn "三层\|项目级"`）。
