# 功能汇表：已实现 与 待实现

> 对账基线：**0.13.49 + 未发版增量**（09-16 master `369d3e6`：plan25 用户画像/eval 集、plan26 S1-S3 可观测时间线/智能标题/滚动摘要 —— 详见 §四追平记录）
>
> **本次复验方法（与以往不同）**：以往是「PLAN/ 状态行 × progress.md × src/ 三方交叉」——
> 读的是**计划自己怎么说**。本次改为**不读计划，直接去源码找符号**（类型定义 / 导出函数 / IPC 通道 / 引用点），
> 计划与实践不一致的**一律以源码为准**。结果：19 项标记完成的能力**全部验证通过**，
> 同时查出 **5 处台账滞后**（详见文末 §四）。
>
> 状态符号：✅ 已交付 · 🚧 已立项未完成 · ❌ 未立项（仅愿景）。

---

## 一、已实现

### 会话与模型

| 功能 | 状态 | 出处 / 说明 |
|---|:--:|---|
| 多模型接入（OpenAI 兼容协议 + Anthropic 原生，providers/ 抽象层） | ✅ | plan3；Key 走 safeStorage 加密，不硬编码 |
| 模型目录与管理界面（增删改 / 连通性测试） | ✅ | plan7 F5 / F5.1 |
| 流式输出 + 唯一发送口 | ✅ | `chat-emitter.ts` |
| 多会话并发（一窗口多会话各跑各的，上限 3） | ✅ | plan11 六步全完成（0.13.13） |
| 会话持久化分层存储（索引 + 正文，读盘足迹不随正文增长） | ✅ | plan10 0/A 批 |
| 会话回滚（N 轮 → 原前缀）+ 确认桥 | ✅ | plan10 B 批 |
| 检查点（跨轮 runs）+ 文件变更记录 + revert-hunk | ✅ | plan8 R4 |
| Token Saver（省 token 档位） | ✅ | plan8 R-10；四档 `rich/ultimate/balanced/light` 见 `shared/token-tier.ts:14`；8k/12k/0.72 为保守默认，校准待做 |
| 上下文压缩/收窄提示 | ✅ | plan8（撤指引 v2 校准未做，见下表） |
| 会话大纲（各轮提问悬浮列表 + 点击跳转） | ✅ | 0.13.46 |
| 模型按端点分组切换（设置页 + 输入框下拉两处） | ✅ | 0.13.45；选择粒度下沉到模型条目，`models:set-entry` 补广播（D-045） |
| 模型自视配置报告（每轮向模型报告当前运行配置） | ✅ | 0.13.45 |

### Agent 内核

| 功能 | 状态 | 出处 / 说明 |
|---|:--:|---|
| 工具系统（命令 / 文件读写 / 检索） | ✅ | 代码检索走 ripgrep agentic search（不用向量，plan4 结论） |
| 子代理（spawn_agents + 调度器 + 执行进度面板） | ✅ | plan6；`.agents/*.md` 定义 |
| 任务清单（update_todos + 输入框上方可折叠面板） | ✅ | plan7 批 D |
| 后台任务（background=true + check/kill + 列表回显） | ✅ | plan7 批 D |
| Agent 向用户提问（ask_user 带选项，超时绝不代答） | ✅ | plan7 批 D-提问（`2bfb090`） |
| 危险操作确认（R5 确认桥：待决请求 + IPC + 超时 + 关窗中止） | ✅ | `confirm.ts` |
| 权限档（只读 / 逐次授权 / 信任目录，唯一用户档位） | ✅ | plan8 D-032；**plan29 S1（`34a39c2`）名实相符**：完全访问 = Agent 线无边界 + 界外回显绝对路径（界面线仍锁工作区，结构性分界由源码级断言钉死）；越界写入回报「不在本轮回滚覆盖范围内」 |
| **计划批准**（planner 出方案 → 停下等用户点头 → executor 续跑） | ✅ | plan27（`196ab66`）：零 electron 桥 `plan-approval.ts`（600s 超时）；闸在 `runAgent` 不进 loop——子代理天然不闸（守卫丁防回退）；frontmatter 新字段 approval/executor |
| **edit 工具**（精确字符串替换） | ✅ | plan28 S1（`25ce074`）：非唯一 → 拒绝；任一处不成立 → 整次不做（原子）；CRLF/LF 按文件口径归一 |
| **命令超时可调 + 输出上限** | ✅ | plan28 S3/S4（`25ce074`）：timeoutMs 模型可设（默认 120s / 上限 600s，越界拒绝不夹取）；maxBuffer 1M→8M；失败三分型（输出超限 / 超时 / 真错误） |
| **流守卫（四层计时器）** | ✅ | plan29 S2（`25ce074`）：首包 60s + 分片间隔 90s 取代整轮墙钟（**按用户决议彻底删除**，不保留开关）；报错分「首包超时 / 流中断」，子代理父停子停 |
| 目标（Goal，跨轮次存活的长期意图） | ✅ ①–⑥ 全交付 | plan12；**⑤ Agent 自建、⑥ 待办联动曾长期标"未做"，实际 0.13.37（`ec10294`）已交付** —— `set_goal` @ `tools/goal-tools.ts:16` 且已进 `runner.ts:69` 白名单，联动 @ `TodoPanel.tsx:18`；2026-09-15 源码复验改正 |
| **执行事件流**（六 kind 落盘 `<userData>/exec-events.jsonl`，子代理标 `sub`；时间线页签数据源） | ✅ | plan26 S1（`ec91855`）；与记忆事件流平行，落盘/轮转/失败容错同套机制 |
| **智能标题**（首答落盘后一次轻调用；只覆盖机械标题 + 原子条件更新防冲用户改名 + fail-soft） | ✅ | plan26 S3（`369d3e6`）；`conv:changed` 广播，侧栏实时更新 |
| **滚动摘要**（裁剪时调模型摘要被裁段，缓存复用；双层 fail-soft 退机械占位） | ✅ | plan26 S3；进程内会话级缓存（重启重摘，已登记取舍） |
| 子 Agent 管理界面（三层 CRUD + 主 Agent 切换） | ✅ | plan17 / plan7 F8（`b5e9942`）；`AgentManager.tsx` 已接线进设置页「子 Agent」分区（`SettingsView.tsx:1113`）。**愿景表曾标 ❌ 未立项，已改正** |

### 工作台（右抽屉页签）

| 功能 | 状态 | 出处 / 说明 |
|---|:--:|---|
| 文件页签：目录树 + 写操作 + 右键菜单 + 拖拽上传 + 二进制/图片/Markdown 预览 | ✅ | plan7 批 A/A2/A3 |
| Office 内嵌预览：docx（mammoth）/ xlsx·xls·xlsm（SheetJS）转沙箱 iframe；多 sheet 切换 + 截断保护 | ✅ | 0.13.36（用户反馈「docx 看不懂十六进制」） |
| 二进制出口：pptx 等不支持内嵌的格式 → 十六进制头 + 「用系统程序打开」（工作区边界内） | ✅ | 0.13.36 |
| 编辑器页签：Monaco（代码编辑）+ Diff 审查 + 未保存守卫 | ✅ | plan7 批 B（`75ea027`）/ plan13。**⚠️ 2026-09-15 澄清：Monaco 与 Diff 是两个独立组件** —— `CodeEditor.tsx`(269 行，**零 diff 字样`) 与 `DiffView.tsx`(325 行，**jsdiff + 统一视图**) 不同源；原写法连着写易读成"Monaco 的 diff"。不并排的理由写在 `DiffView.tsx:19-20`：Monaco 与 jsdiff 各一套切块算法，并用会「点第 2 处、写盘第 3 处」静默对不上 |
| 终端页签：node-pty 真终端（重大转向后落地） | ✅ | plan7 批 C / plan14（0.13.25） |
| **页签保活 + 渲染性能修复**（plan30，0.13.51） | ✅ | 失活页签隐藏不卸载（browser 例外失活即卸载）+ MessageMarkdown LRU 解析缓存 + 九面板 memo——真机实测会话切换 765→281ms、卡顿帧 87→2；包装层 `display:contents` 保高度链（0.13.51 修 docx 预览卡尺寸回归） |
| 任务页签：子代理进度 + 后台任务列表 | ✅ | plan7 批 D |
| 源代码管理：变更列表 / 勾选暂存 / 差异 / 提交 / 待推送计数 / 外部刷新 | ✅ | plan16（内联着色 Diff，Monaco 复用未接） |
| 检查点页签：runs 列表 + 正文 Diff + 回滚 | ✅ | plan8 R4 / plan10 B |
| **时间线页签**：执行事件流回放（工具调用/审批/裁剪/起止，按会话过滤，元数据摘要行） | ✅ | plan26 S2（`c67109b`）；事件只含元数据（payload 白名单，入参/正文不落盘）；真渲染门禁 verify-shot 338/338 |
| 面板宽度拖拽 + 分栏引擎 | ✅ | plan7 A0 / plan9 |

### 记忆与进化（P4，自进化闭环）

> 本节 **2026-09-15 新增** —— 原表完全没有这一层（基线停在 0.13.35，而 P4 自 0.13.41 起四环全闭）。
> 这是本次复验补上的**最大缺口**，不是"补几行"的量级。

**① 语义记忆（0.13.41 批 1）**

| 功能 | 状态 | 证据 / 说明 |
|---|:--:|---|
| 两条写入通路（模型 `remember` / 用户选中即记） | ✅ | 选中即记**不经过模型**，记的就是选中的原话 |
| 影响面三分类 + 注入与预算截断 | ✅ | 满载 100 条时 8KB 字节预算先到 → 只注入 81 条，**如实标注"未生效"** |
| 事件流 `memory/events.jsonl` | ✅ | 写入（含被拒）/ 召回 / 删除 / 标记 / 注入 |
| 三条架构守卫 | ✅ | 记忆层物理碰不到权限设置；授权语义拒写并指路；凭据形状硬拒。**有单测钉死** |
| 右抽屉记忆页签 + 「本次新增」巡检区 | ✅ | 删除立即生效，下一轮起不再出现 |
| 用量牌「记忆税 N(估)」 | ✅ | 占多少上下文是**看得见的数**，不是感觉 |
| 完全访问档下开启记忆的红色告警 | ✅ | 每次重开都会再提醒一次 |

**② 自动捕获 / 反思（批 2）**

| 功能 | 状态 | 证据 / 说明 |
|---|:--:|---|
| 会话切换时异步反思 → 产出候选 | ✅ | D-040：关窗只记队列、启动补跑 |
| 候选**物理隔离**在 `memory/candidates/` | ✅ | 用户批准前**不进注入索引段** |
| 批准 / 拒绝 / 冲突标记 + 前置门 2KB | ✅ | 日上限与队列 |

**③ Playbook（批 3）**

| 功能 | 状态 | 证据 / 说明 |
|---|:--:|---|
| `evolution/playbooks/` 布局 + tags 条件召回 | ✅ | 预算与语义记忆**分开** |
| `save_playbook` / `recall_playbook` 已注册下发 | ✅ | 0.13.48 补齐接线（`6a620ac`） |
| 右抽屉 Playbook 页签 | ✅ | |

**④ 遗忘与度量（批 4）**

| 功能 | 状态 | 证据 / 说明 |
|---|:--:|---|
| LRU 遗忘 + style 豁免 | ✅ | |
| 描述相似度警告 | ✅ | |
| 纠正通路 + 重复纠正率 / 误伤率 | ✅ | 0.13.48 补齐两个指标的数据源（`6a620ac`） |

**④ 用户画像 + eval 集（plan25，0.13.50 未发版，`4a979fc`/`0e481e8`/`a523ecd`）**

| 功能 | 状态 | 证据 / 说明 |
|---|:--:|---|
| 用户画像（`class='profile'` 特殊记忆：固定 slug、注入正文置顶、独立 4KB 预算） | ✅ | D-071~073；四层写入防线（save 层拒 model 直写 / 工具 enum 排除 / UI 无选项 / approve 覆盖断言） |
| 反思产出画像候选（跨场景特征 → user-profile；撞名原地覆盖） | ✅ | 反思 prompt 独立模块（判据可断言）；手改防线（重复画像取新 + warnings） |
| L1 场景回归 16 条（CI 零模型，桩 chat 走完整记忆链路） | ✅ | `tests/evals/`；定性如实登记为"场景级回归测试"，不冒充 eval（盲审修正） |
| L2 在线 eval（electron 子进程 safeStorage 解密真跑；全代码判分 pass@k） | ✅ | `npm run evals`；**实测 deepseek-flash 3/3 通过 5s** + repeat=3 9/9；报告本地留档 bench/（不入 git） |
| 记忆 import 拒绝画像导入（明确话术） | ✅ | memory-import toClass |

> ⚠️ **安全边界（照抄 README 的如实声明，这里不打折）**
> 三道防线是**必要非充分条件，不是"已解决"**：判定是关键词与形状匹配，**可被同义改写绕过**；
> 只读档也具备**出境与操作远端页面**的能力；记忆文件是 userData 下的**明文 markdown**，本机任意进程可读。
> 自动捕获会**读会话正文**，且反思本身是一次**出境的模型调用**。巡检区是给用户的习惯，护栏不是承诺。

### 设置与系统集成

| 功能 | 状态 | 出处 / 说明 |
|---|:--:|---|
| 外观：主题 + 字号四档（rem 基缩放）+ 系统字体选择（注册表枚举 + 白名单清洗） | ✅ | plan7 F3（0.13.33）；四档源码核实：`shared/splitter.ts` `sm/md/lg/xl` = **87.5 / 100 / 112.5 / 125%**（小档取 87.5% 而非 85%，因为 14px 换算 12.25px 会发虚） |
| 电脑控制开关（允许模型操控鼠标键盘与桌面应用） | ✅ | 0.13.45；自视段会向模型报告该状态 |
| 导入其他记忆（把别的 AI 工具的长期信息搬进来，两步） | ✅ | 0.13.42；设置 → 记忆 |
| 系统集成：锁屏运行 / 熄屏保持唤醒 / 开机自启 | ✅ | plan7 F1 / plan15 |
| 网络代理：三档 + 11 处 fetch 出口收口（可注入） | ✅ | plan7 F2（0.13.32） |
| 存储位置：更改 / 下次启动迁移 / 恢复默认 / 撤销 / lastEvent 提示 | ✅ | plan10 C（0.13.35，`9a3af4d`） |
| 设置窗口出口收敛：删自绘 ×（与系统标题栏 × 重复易误关应用），保留标题锚点 + Esc 关窗 | ✅ | 0.13.36（用户反馈双 X） |
| 默认工作空间（新任务落点，可恢复内置默认） | ✅ | plan7 F4① |
| 界面文案专业化（两轮 39 文件 206 条） | ✅ | plan7 批 E-文案 |

### 数据与健壮性

| 功能 | 状态 | 出处 / 说明 |
|---|:--:|---|
| 数据目录可配置：排除法迁移 + 三段式校验（SHA-256 + 独立清点）+ 回退反向迁移 + restore-backup 保全 | ✅ | plan10 C |
| location.json 配置自举（损坏回退默认绝不启动失败；指针失效回退保留指针） | ✅ | plan10 C |
| 数据目录锁（wx 独占 + pid 存活 + 陈锁自愈），与单实例锁语义分离 | ✅ | plan10 C + plan8 R13 |
| 崩溃守卫 / 进程树管理 / URL 防护 / 原子写 + fsync | ✅ | plan8 |
| 会话绑定工作区 + 工作区授权边界（resolveInsideWorkspace） | ✅ | plan1 P1 |

---

## 二、待实现（按计划台账）

| 来源 | 条目 | 状态 | 备注 |
|---|---|:--:|---|
| plan10 | R1 单会话巨型文件测量（分层方案前最后一次机会） | 🚧 未开始 | TaskList #5 挂起中 |
| ~~plan12 ⑤ Agent 自建目标 + ⑥ 与目标/待办联动~~ | ✅ **已交付** | **原写"未开始 / set_goal 全仓零命中"，实际 0.13.37（`ec10294`）已落地** —— 本行 2026-09-15 移出待实现，见 §一 Agent 内核节 |
| plan7 A3 | PDF 与音视频预览评估 | 🚧 尾巴 | A3 其余三块已交付 |
| plan7 E | UI 精修剩余：**消息气泡 / 空状态 / 加载态 / 焦点态**（纯视觉） | 🚧 | **2026-09-15 拆分**：原条目里的「间距节奏」「字号层次」两项**已随 R7 收口完成**（`6d3a899` 363 处 + 四档缩放），移出本行；剩下的四项属P5 "纯视觉"，在 R7 结束后才轮得到 |
| plan16 后续 | SCM Diff 复用 Monaco 编辑器（当前为内联着色渲染） | 🚧 | 理由见 plan16 §七 |
| plan8 | Token Saver 校准方案 v2（每臂 ≥3 次比中位数 + 报告轮数/调用次数） | 🚧 未做 | n=1 A/B 对照组抖动 3.3 倍，量不出结论 |
| plan8 | 「重复执行的同一命令」更明确提示（需跨轮状态） | 🚧 保留 | 要做先想清楚状态放哪层 |
| plan8 | 「本会话总是允许」 | 🚧 保留 | 等实际反馈；做也应为「记住这条具体命令」 |
| ~~plan8 设计 token 色值定稿（朱砂深浅 / 水印大字）~~ | ✅ **已了结**（一半定稿、一半取消） | **朱砂深浅已定稿** `#a8342c`（`styles.css:98`，墨色层次 + 朱砂点睛的用色体系早已兑现）；**水印大字用户 2026-09-15 明示「不做」**（D-049），从待办移除。此条 2026-09-15 关闭 |
| — | i18n 全量抽词（界面多语言） | 🚧 单独评估 | 不混在设置里顺手做 |
| plan28 S2 | run_command 会话复用（terminal-session 是交互 PTY，需 shell-integration 层：哨兵 EOF / 退出码提取 / ANSI 清洗 / Ctrl+C 与树杀） | ⏳ 未开工 | plan28 §五 D-085 诚实登记；独立一片工程量 |
| plan29 S4 | 问题一「粘滞」量测（三条机制各自占比，只测不改；为 worker_threads 迁移提供判据） | 🚧 进行中（2026-09-16） | 结果回写 plan29 §七 |
| 问题四 | 记忆库去重 / 同义归一（同名同义记忆堆积，批准列表刷屏） | 📋 只登记 | `PLAN/诊断-2026-09-16/01` 问题四；优先级与 S4 量测结果合并评估 |

---

## 三、长期愿景（D-034③，2026-09-12 用户明示）

| 愿景 | 拆解 | 现状 |
|---|---|---|
| 像 VS Code 一样写代码并运行代码 | 编辑器 | ✅ Monaco（批 B） |
| | 运行 / 调试 | ❌ 未立项 |
| | Git 集成 | ✅ 大半（SCM 基础闭环已交付） |
| 能自定义子 agent 并切换 | 内核（`.agents/*.md` 定义 + 调度器） | ✅ |
| | 管理与切换界面 | ✅ **plan17 已交付**（`AgentManager.tsx` → 设置页分区，2026-09-15 改正：原标 ❌ 未立项是滞后） |

---

## 四、对账说明

- 「目标（Goal）」与「批 A3」「plan11」曾出现**台账滞后**（实际已交付但计划标待做），2026-09-13 全项目对账（`docs/交接-全项目对账-26.09.13.md`）已改正——本表的 ✅ 均以计划状态行 + 源码双证为准。
- **2026-09-15 源码级复验补记**（本次与前几次的关键差别见表头）：查出 **5 处滞后**，均已就地改正并在对应行留痕 ——
  1. **Goal ⑤⑥**：谓"未做 / `set_goal` 全仓零命中"，实际 0.13.37 已交付
  2. **子 Agent 管理界面**：愿景表谓"❌ 未立项"，实际 plan17 已落地并接入设置页
  3. **水印大字**：谓"待用户圈定"，用户已明示**不做**（D-049）
  4. **朱砂深浅**：谓"待定稿"，实际早已定稿 `#a8342c`
  5. **P4 记忆与进化整层缺失**：原表停在 P2，缺 0.13.41–0.13.48 的整个记忆闭环（已新增专节）

  **教训**：这批失真全部集中在「**等某人拍板的条目**」与「**别的文档会顺带更新的条目**」——
  前者因为没有明确归属而漂着（水印漂了三天），后者因为有人改 README / CHANGELOG
  就默认被覆盖（其实每张表各管各的）。**凡写着"待圈定 / 待用户决定"的行，
  应当自带一个"问过没有"的时间戳**，否则它会永远挂在那里装作还在评估。
- **2026-09-16 追平补记**（P4/P5 增量）：新增「用户画像 + eval 集」专节（记忆④）、
  时间线页签、执行事件流 / 智能标题 / 滚动摘要三行（Agent 内核）。同批清偿的历史欠账：
  plan8:550「裁剪没有可见性」→ trim 事件带 droppedCount/bytes/summarized；plan25 判据 10
  「真渲染门禁未跑」→ plan26 S2 以 verify-shot 338/338 兑现。
  **P5 余项**（bench:all 聚合报告）登记为未做（按协作裁决 S3 即停挪后）。
- **2026-09-16 二次追平**（分支 `feat/p5-tooling-ux`，未合并）：Agent 内核表新增
  计划批准（plan27）/ edit 工具与命令超时（plan28）/ 流守卫与档位名实相符（plan29 S1/S2）四行；
  待实现表新增 plan28 S2 会话复用、plan29 S4 量测、问题四（记忆去重）三行。
  同日裁决：**AGENTS.md / CLAUDE.md 纯本地化不入 git**（D-093/D-094）——本表其余行号引用
  若指向 AGENTS.md 章节仍有效（文件只在本地）。
- **2026-09-16 三次追平（0.13.51 收口）**：工作台表新增「页签保活 + 渲染性能修复」行（plan30 两批 +
  布局回归修复）；同日量测定案问题一占比（渲染 97%）并出 0.13.50/0.13.51 双包。
- 未列入本表的原则性约定（决策权归属 / 布局三段 / 规则单一真相源等）见 `AGENTS.md`。
- 更新惯例：每批收口时同步本表（新增 ✅ 行带版本锚点；待实现条目完成即上移）。
