# 交接文档（2026-09-12 04:30）

> **给下一个会话（DSH）**：接手「九十里路」项目前先读完这份。
> 配套必读：`NOTEBOOK/progress.md`（当前步骤 + 时间线）、`AGENTS.md`（项目铁律）、
> `PLAN/plan7_P2工作台.md` 与 `PLAN/plan8_健壮性与技术债.md`（两份现行计划）。

---

## 一、项目一句话

**九十里路**：Electron 桌面 Agent 应用（会自己长经验、公开在 GitHub）。
技术栈：Electron 33 + React 18 + TS + electron-vite + electron-store（**无 SQLite、无 Tailwind、无 node-pty**，规避清单见 AGENTS.md）。

- 仓库：`github.com/Gazer-887/jiushililu`（公开，master）
- 本地：`D:\jiushililu`
- 当前版本：**0.10.0**（5c8f790，CI 绿，测试 265 例，提交全部 SSH 签名）

## 二、当前进度（P2 工作台阶段）

**已完成**：P0 骨架 → P1 内核（Agent 主循环 + 工具六件套 + 子代理）→ P2 主体
（单通道合并 D-032、三段式抽屉、输入控制台、真浏览器、资源管理器只读、面板拖拽、**主题系统**）。

**plan8 地基**：R1–R5 + R11 ✅（异常兜底/日志/CSP+导航守卫/检查点回滚/危险确认/禁止不查就答）。
**R7 设计系统**：第一块（颜色 token 化 + 水墨/经典双主题 + 设置·外观）✅；**其余未做**。

**下一步（按优先级）**：
1. **R7 其余**：字号 12→6 级、圆角 12→3 级、**padding 56 种→4/8 阶梯**、首屏淡墨水印
   —— 审计数据与建议见 `PLAN/plan8` R7；**字号阶梯需用户圈定**
2. **批 A2 文件写操作**：**前置 = 先抽统一写入服务**（界面与 Agent 共用，
   检查点快照挂服务层）——否则界面写操作绕过检查点，删掉的文件退不回来
3. R6 e2e（Playwright；headless 可行性待评估，已后置）

## 三、等用户做的事

**装 0.10.0 真机验收**（`dist/九十里路 Setup 0.10.0.exe`，覆盖装到 `D:\JSLL\jiushililu`）：
1. 设置 → 外观 → 切「水墨」主题（看整体效果）
2. 五步验收 D-032：`你好`（不该调工具）/ `列出工作区文件`（**该出现工具卡片**，
   工作区 `D:\jsllworkplace_for_test` 里有 3 个真文件）/ `新建 abc.txt`（真落地）/
   变更面板回滚（文件消失）/ 让 Agent 删文件看它是否先确认

## 四、设计方向（用户已定，别再问）

**水墨风：黑白灰 + 朱砂红**（参考 opencode 的克制感）。
- 灰阶承担全部层次（墨分五色），红只做点睛（面积必须极小）
- **语义色服从体系**：正向/激活 = 墨色（**没有绿和蓝**），负向 = 朱砂 `#a8342c`
- 逻辑详见 `DIARY/专题-水墨风配色.md`
- **用户明示「后续还会继续打磨细节和风格」** → UI 意见随到随做（台账在 `PLAN/plan7`），
  **不等批 E**；token 取值（字号阶梯/圆角档位/朱砂深浅）**要用户圈定**，别替他定

## 五、这个项目的"规矩"（踩坑换来的，务必遵守）

**协作方式**（用户明示）：
1. 用户提修改意见 → **自行评估优先级并归位到计划**，只回问方向性抉择；当天回写，防计划表失真
2. 用户说「**不要 X**」→ **先确认是不是「换成 Y」**（0.9.7 踩过：删了文案，其实他要替换）
3. **UI/审美决策先出渲染预览再改代码**（对话页清空就是看了预览才定的）
4. **默认不画图**（对话中），除非他要求——省 token
5. 术语/知识点随发现随进 `DIARY/`（全中文命名、三件套、教材级准确）

**技术铁律**：
1. 提交前 **等 CI 绿**（本地全绿≠通过，跨平台差异只在 CI 暴露——踩过"同毫秒排序"的坑）
2. **先等 CI 绿再打包**（0.9.3 首包不含后续修复，白打一次）
3. 打包验证用 **asar 特征串**（须唯一；asar 已打成一坨，按文件名搜不到）
4. Electron 命令要 `env -u ELECTRON_RUN_AS_NODE`；宿主 safe-delete 护栏按轮累计，
   大批量删除用 .NET（`[System.IO.Directory]::Delete`）或改名暂存
5. **检查点快照必须早于写入**；同一文件一轮内只记第一次快照（见 `src/main/store/checkpoints.ts`）
6. 渲染进程**不得 import electron**（CI 无二进制会炸）——纯逻辑放 `src/shared/`

## 六、关键文件索引

| 文件 | 内容 |
|---|---|
| `src/main/store/checkpoints.ts` | 检查点回滚（快照/回滚/修剪） |
| `src/shared/checkpoint.ts` | 回滚纯逻辑（单测覆盖） |
| `src/main/confirm.ts` | 危险操作确认桥（无人应答=拒绝） |
| `src/main/log.ts` / `crash-guard.ts` | 日志 / 异常兜底 |
| `src/main/url-guard.ts` | 导航守卫纯逻辑（origin 比对） |
| `src/shared/splitter.ts` | 面板宽度约束纯逻辑 |
| `src/shared/fs-tree.ts` | 文件树纯逻辑（跳过 .env/node_modules） |
| `scripts/verify-shot.cjs` | **真实渲染验证工具**（真点界面+量尺寸+截图；`INK=1` 出水墨预览） |
| `scripts/audit-css.cjs` | **CSS 债务审计**（R7 用） |
| `NOTEBOOK/learnings.md` | 全部工程教训（CI 假绿 / 特征串假阳性 / 半像素…） |

## 七、已知问题 / 未决

1. **`dist-stale-*` ×6（约 2.2GB）删不掉**——文件被某 node 进程持有句柄
   （非权限问题）。已 gitignored 无害；待进程释放后用
   `[System.IO.Directory]::Delete($path,$true)` 清
2. CI 的 GitHub Actions 还是 v4（Node20 弃用警告），升级 v5 未做
3. R5 危险确认**在内核默认路径走不到**（`run_command` 不下发）——复验需自定义 Agent 声明该工具
4. 会话存储是 electron-store JSON 全量读内存（R8 评估项，量级还不需要动）
5. **水墨主题目前只覆盖颜色**——字号/间距/圆角仍是旧的混杂取值（R7 其余部分解决）

## 八、验收状态

- **D-032（单通道合并）**：0.9.4 复验通过（模型养成核实习惯）；
  0.10.0 换了主题系统后**建议再跑一遍五步**（见 §三）
- **plan8**：R1–R5、R11 ✅；R7 第一块 ✅；R6/R8/R9/R10 未做
- **plan7**：批 A0 ✅、批 A 只读 ✅；批 A2/B/C/D/E 未做

---

*交接人：安安（WorkBuddy）· 2026-09-12 04:30 · 版本 0.10.0（5c8f790）*
