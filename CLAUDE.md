@AGENTS.md

> **这是桥接文件，不是规范本体。**
>
> 本项目规范的唯一真相源是 **`AGENTS.md`**。
> 但 Claude Code 只读 `CLAUDE.md`、不读 `AGENTS.md`（官方明确说明，无反向回退），
> 因此这里用 `@` 导入语法把规范引过来，保证 Claude Code 会话也能加载到。
>
> **请勿在下方写任何内容** —— 所有约定一律写进 `AGENTS.md`，否则两份会漂移。
>
> 参考：Codex / Cursor / OpenCode / Copilot / Windsurf / Cline / Roo / Amp / Zed / Jules
> 均原生读取 `AGENTS.md`，不需要这个文件。
