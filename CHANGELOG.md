# 变更记录

本文件记录「九十里路」每个版本的**用户可见变化**。版本号遵循 `主.次.修订`：
- **主**：架构级变更
- **次**：一批功能或结构性改动
- **修订**：修复与小改

> 说明：本项目在 P0–P2 期间以「一个批次一个版本」的节奏推进，版本号主要作为**安装包标识**，
> 尚未进入语义化从严阶段。

---

## 0.13.85 · 2026-09-21

**修：四处"设置项与读数其实没接上"—— 反思模型、反思用量、纠正识别、注入税**

这一批的四条同属一种病：接口声明了、界面格子也建好了，装配那一段没人接，于是**任何一道闸都不会红**。

### 修

- **「设置 · 记忆 · 反思模型」现在真的生效**。此前这一格填什么都不起作用：反思仍按对话模型跑，
  用户挑的便宜模型白挑。填入的仍是**模型名**（该格为自由文本），端点与密钥沿用当前档案。
- **用量牌上的「反思 N tokens」第一次有数**。这条会话被反思消耗掉的用量此前从不入账。口径为**逐次累加**
  —— 每次反思报的是本次用量，同一条会话反思过三次就该记三次的和；对话那格仍按「只长不缩」合并，
  两者的数据源不同（前者是逐次增量，后者是每轮重报的总量），因此不共用一把尺。
- **两类此前不入账的反思调用现在入账**：① 模型返回内容形状不合格（这次调用真实发生、日额度也真实消耗，
  不该记成零）；② 档案选择了非流式响应（该模式下厂商在整份响应里一次报全用量，此前从未读取，
  这类档案的对话与反思用量牌长期为空）。
- **反思入账后用量牌随即刷新**，不需要切走再切回该会话。
- **「重复纠正率」这个读数不再恒为 0**。记忆工具靠"本轮用户原话"判断一次改写是不是纠正，
  这句话在装配层早就算好了，却没往工具层传：于是纠正识别只在单元测试里存在，
  生产通路上永远不会记下一次纠正。
- **注入税现在包含执行手册那一段**。此前只统计记忆段，手册段每轮占掉的开销没有读数 ——
  「越用越重」这件事少了一格。

### 边界

- 反思模型那格填的必须是**当前端点能调的模型名**；填错会得到反思失败（已写入主进程日志），
  不影响正常对话。
- 反思用量只在厂商报量时出现；厂商没报就**什么都不显示**，不拿估算冒充真值。
- **注入税是本地估算**，且记忆段与手册段使用了**两套不同的估算口径**，因此该读数是两个读数之和：
  用于观察趋势与比较会话，不等于账单上的精确 token。要让它成为单一口径的真值，需先统一两段的估算方式
  （会改变既有读数，属独立事项，本批未做）。

---

## 0.13.84 · 2026-09-21

**新增：界面语言可切换（中英双语首批，本批覆盖侧栏与设置页语言项）**

### 新增

- **设置 · 外观 增加「界面语言」**，可选简体中文 / English，切换即时生效、所有窗口同步、重启后保持。
  该设置与主题走同一条通路（主进程存、广播、各窗口重读），因此不存在"一边切了一边没切"。
- **侧栏文案随语言切换**：新建任务、工作区分组标题、空态提示、设置入口、页脚标语。
- 缺译的内容**回退简体中文**并在主进程日志留痕，不出现空白或内部键名。

### 边界

- **本批只覆盖上述两处**，其余界面（会话区、右栏各页签、对话框、通知）仍为中文，按计划分批推进
  （plan52 §三 S2–S4）。回退机制保证这期间英文用户看到的仍是可读中文，而不是键名。
- 日志、模型提示词、代码注释**不参与翻译**：三者都不是界面文案，混译会让检索与调优同时变难。

---

## 0.13.83 · 2026-09-21

**修：删除会话后的运行状态虚报、生成中仍可撤销回滚、反思失败不留痕**

### 修

- **删除一条正在运行的会话后，它不再被计入「当前另有 N 条会话正在运行」的提示**。此前该会话的条目仍留在
  运行状态表里，提示会永久停在多出的那一条上，且没有恢复入口。
- **删除正在运行的会话时，该会话仍在进行的一轮同时被取消**。此前它会继续跑完：继续消耗额度，
  并继续向一条已经不存在的会话写入内容。
- **该会话正在生成回复时，「撤销回滚」不再可执行**，并给出与「回滚」同一条提示。此前该操作在流式过程中
  仍可进行，会把被回滚隐藏的正文与当前这一轮的回答混进同一条会话。
- **回滚后的提示条在该会话继续发送新消息时自动消失**。此前它会一直停留，而继续对话本身已使
  「可以撤销回这次回滚」不再成立。
- **记忆反思失败现在会写入主进程日志**。此前反思失败与"这一次确实没有可沉淀的内容"在界面上和日志里
  表现为同一件事（零候选、零记录），无从区分。

### 边界

- 侧栏标记与并发提示这类跨会话状态仍只有单元测试在守，未纳入真渲染门禁（已登记待办 K12）。
- 隔离验证进程缺 `skills-disabled:get` 桩，该通道在真渲染门禁里拿不到数据（已登记待办 K13）。本版未动。

---

## 0.13.82 · 2026-09-21

**修：会话状态标记残留，以及一条会丢回答的切换路径**

### 修

- **一轮回答结束后，侧栏该会话的「正在生成」标记不再残留**。此前该标记在回答完成后仍会显示，
  直到切换到别的会话才消失。
- **不再误报「当前另有 N 条会话正在运行」**。该提示用于提醒多条会话同时改动同一工作区的覆盖风险，
  但此前它把已经结束的历史会话也计入，导致提示与事实不符。
- **在一轮回答仍在输出时点「新建任务」，不再把该会话已输出的回答覆盖成空**（同时修掉由此引起的
  「新会话里点发送没有反应」——该失败此前既不转圈也不报错）。
- 「新建任务」页不再残留上一条会话的待办清单与子代理面板。

### 边界

- 删除一条正在运行的会话时，其状态仍会被计入上述并发提示。已登记待办（K10），本版未动。

---

## 0.13.81 · 2026-09-21

**修复：一轮回答在产出正文之前被停止后，该会话无法再发送消息**

### 修

- **「停止生成」不再使会话失效**。此前若在一轮回答产出任何正文之前点击停止，该会话之后的每一次
  发送都会被参数校验拒绝，且没有恢复入口。现在这类会话可以正常继续。
- **记忆反思链路同步修复**：反思调用不经过主循环，此前会把上述空正文轮次原样发给模型，
  导致该会话的记忆候选静默为零（无提示、无日志线索）。
- **发送失败的提示改为显示主进程原文**。原提示是固定的「请求被主进程拒绝（参数校验未通过）」，
  不含定位信息；现在会给出是哪一条消息、哪个字段不符合要求。

---

## 0.13.80 · 2026-09-21

**子代理第一次吃到"做事纪律"，且只吃它该有的那几条**

### 改

- **子代理的 system prompt 现在带上做事纪律**（能查就查不许猜 / 没核实不许笃定 / 能力不足如实说 /
  自检里跳过 ≠ 通过 …）。此前这段只拼给主代理，而真正跑命令、写产物、交验证报告的是子代理 ——
  它们连"必须先核实再回答"都没被要求过。
- **纪律按每个子代理实收的工具表取舍**，不是整段照搬：内置 executor 的 `tools:` 里没有
  `update_todos` / `set_goal` / `spawn_agents`，整段搬过去就等于承诺它拿不到的能力
  （与 0.13.77 真机实测抓到的"提示词与工具表矛盾"同一种病）。原先合写在一起的"列清单"与"登记目标"
  两条据此拆开，各绑各的工具。
- **缺能力时的出路改为直接点名**：原先写"名单见 `<self_view>`"，而子代理没有自视段，
  那句话对它是悬空指针。现在把能提供该能力的子代理名字写进规则本身。
- 自视段**不给**子代理：它的作用是让用户能问"你能做什么"，而子代理从不对用户说话，
  其工具清单又与 API 同时下发的 schema 重复。实测该段约 140 token，成本不是理由，没必要才是不给的理由。

### 修（复查抓出，本批新开的口子）

- **子代理不再拿到 `update_todos` 与 `set_goal`**。这两件写的是**会话级面板**，只能有一个所有者：
  待办是整表替换，并发子代理各推一份就会互相覆盖、并把主代理自己的清单抹掉；目标更是跨轮次存活、
  署名挂在主会话的持久状态 —— 而子代理读取的正是仓库文件与构建日志，文件里一句"以后每次都要…"
  就可能被铸成用户面板上的目标。此前它们在子代理手里是闲置的（子代理没吃过做事纪律），
  本批给子代理加上"先列清单 / 登记目标"的鼓励之后就变成被推动，故**能力连同鼓励一起收回**。

### 成本（实测）

子代理新增纪律块 737 字符（约 461 token），同一工具表下字节稳定 ⇒ 落在前缀缓存里。

---

## 0.13.79 · 2026-09-20

**确认卡说清是谁要跑命令；自检里"跳过"不再算作通过**

### 修

- **命令确认卡的「发起」现在写的是真正要执行的那一个**：此前该字段恒等于会话主代理的名字
  （标签在创建工具实例时定格，子代理复用同一批实例）。并发派发多个子代理时，用户会看到
  几张内容不同、署名相同的确认卡，而「发起」正是点「允许这一次」前唯一的判断依据。
  现在命令类工具按发起者各自建实例，后台任务记录同样按发起者落账。
  **权限档、确认桥、检查点记账口径均未改变**，只是把"谁在做"如实报出来。
- **并发执行命令不再互相串台**：同一个会话内多条命令改为**串行执行**。此前 `run()` 往同一个
  stdin 写命令、往同一个 stdout 收字节且无互斥——两个子代理同时跑命令时，一方会收到另一方的输出、
  一方的超时会连带杀掉另一方正在执行的进程。这条路径自 0.13.78 起才可达（子代理第一次拿到
  命令执行能力），本轮以真并发用例复现并修复。

### 改

- **做事纪律新增一条：自检与验收里「跳过 ≠ 通过」**。没能真正跑成的项要单独计为「跳过」并写明
  原因与补救路径，汇总行不许写"全部通过"，退出码需区分三态（全过 0 / 有失败 1 / 有跳过 2）。
  依据：v0.13.77 真机实测中，被测 Agent 生成的自检脚本把一项未验证的能力计入通过并返回 `exit=0`，
  任何自动化流程都会把它当作绿灯放行。

---

## 0.13.78 · 2026-09-20

**子代理的工具白名单第一次真正生效；提示词不再承诺本轮拿不到的能力**

- **修（装配层，根因）**：子代理原先**复用主代理那一份已过滤工具集**，`tools:` 声明对子代理完全
  不生效 —— 派 `code-executor`（声明含 `run_command`）时它拿不到命令工具，而它的职责正文要求
  「跑构建 / 跑测试来验证」；派 `quality-reviewer`（只声明读类）时它反倒拿到 `write_file` + `edit`。
  现在每个子代理按**自己的声明 ∩ 权限档**装配：审查型真的只读，执行型真的能验证。
  这是 plan6 D4「审查型只给读、执行型才给写」第一次成立。
- **补（安全）**：上限从"主代理实收集"变为"权限档 ∩ 自己的声明"，于是派发口成了一条能力获取途径
  —— 带**计划批准闸**的 Agent 因此拿不到 `spawn_agents`（否则批准卡还没弹，写操作已能经子代理落盘）；
  子代理的 system prompt 补上与主代理**同一条**防注入基线（它读的正是构建日志与命令输出）。
- **改（提示词）**：行为纪律后两条按**本轮实收工具表**取舍、编号按位置计算 —— 没有 `run_command`
  时不再教模型「转后台」，改为给出可执行出路；`check_command` / `kill_command` 与 `run_command`
  同进同退（不再下发"能查能杀、却创建不了"的孤儿工具；破坏性的 `kill_command` 只给显式声明的）。
- **增（自视段）**：本会话缺某项高危能力、且确有可派 Agent 提供它时，`<self_view>` 如实写
  「缺什么 + 谁能给」；没有出口则不写。
- **修**：一次性任务入口漏传权限档，此前恒按可写档运行 —— 只读档用户在该入口上拿到的是可写档工具集。
- **更正对外记录**：`docs/实测-web-chat-lab-26.09.20.md` §四 A1 的「能力就在隔壁（换个 Agent 就有）」
  只对了一半 —— 派子代理那条路当时同样拿不到 `run_command`。该报告已公开，**原文不改**，
  更正以**本条目为唯一真源**，报告末尾只留指针。

---

## 0.13.77 · 2026-09-20

**本版含 0.13.76（未发布）的全部改动**：桌面派 MCP 工具装配层漏传修复、对话页回调引用不稳定修复，
详见下方 0.13.76 条目。

**修：两处弹出层被裁切；增：卡顿定位探针**

### 修

- **设置页靠近底部的 ⓘ 说明气泡显示不全**：原先气泡固定向下弹出，贴近滚动区下缘的条目
  （如「网络」）说明会被切掉，需要滚动才能看全；而滚动时它又跟着内容跑。现改为**自动判断方向**——
  下方放不下就翻到上方，两侧都不够时收缩高度并在气泡内部滚动；滚动过程中会重新判断，
  保证任何滚动位置下都能完整读到。键盘聚焦同样生效，关闭脚本时退回原行为而不是说明消失。
- **工作台「＋」添加面板显示不全**：窗格较矮时，面板里的后几项（终端 / 浏览器 / 任务管理 / 记忆等）
  会被切掉只剩开头一项。现补上限高度与内部滚动，并复用已有的自适应排布让窄栏里也能铺开。
- **Linux 环境下 11 项自检误报失败**：这批测试把「Windows 平台」与系统临时目录混在同一次调用里，
  拼出非法路径。产品代码在两个平台上本就正确，修的是测试本身；修完 Linux 环境反而真正覆盖了
  原本跳过的 POSIX 分支。

### 增

- **卡顿定位探针**：此前主进程被卡住时，只有跨进程调用会留下记录，**进程内部的同步文件读写
  完全不留痕**——这正是卡顿查不到原因的技术根因。现于启动时统一给文件同步操作装上探针，
  卡住时会额外报出：哪一段操作正好横跨了停滞起点（即嫌疑最大的一段）、停滞期间共有几段同步操作、
  它们合计占用了多少毫秒，以及一个自检计数用于区分「这次没抓到」与「探针根本没生效」。
  **这不等于卡顿已解决**：真凶仍未定位，本轮交付的是下一次能否抓到它。
- **探针存活心跳**：每 5 分钟落一条 INFO，报出这段时间记了几笔同步块、合计多少毫秒、累计总数。
  若自启动以来一笔都没记到则改报 WARN —— 应用启动必然走大量同步文件读写，
  零记账只可能是探针没生效，此时"没有卡顿"的读数不可信。**"没抓到"与"探针没响"从此可分。**
- **探针自身的可信度保障**：新增「以调用方视角回读」的自检与三个诊断计数器。若探针在某次
  停滞中一个块都没记到，日志会如实报 `traced:0`，而不是让人把「没数据」读成「没问题」。

---

## 0.13.76 · 2026-09-19


**修：桌面派 MCP 工具全判离线（装配层漏传）；对话页回调引用不稳定触发渲染层自持循环**

### 修

- **装了 windows-mcp、开关已开、面板显示「已连接」，模型却说没有桌面能力**：装配层漏传了
  `computerControl` 与门控回调两个参数，导致桌面派工具被整体丢弃，且门控日志一条都不产生。
  现如实透传，并补上「装配口」回归测试三条（此前 14 项单测都绕开了这条真正出事的缝）
- **对话页卡顿粘滞（真机复现）**：对话页把「主 Agent 选择」回调写成了内联箭头函数，
  每次界面重画都是全新引用，触发一个能自己给自己上劲的循环——重画 → 重新拉取 Agent 清单 →
  清单变了 → 再重画。日志实证：12 分钟内 166 次卡顿告警、累计被占死 635 秒、
  单次最长 9.8 秒。现用稳定引用钉死，并把同类写法接进架构测试，防止复发

### 增

- **架构测试：回调引用稳定性静态扫描** —— 扫描界面代码里这类回调的写法，
  不稳定就报错（含「定义点回查」以堵住用变量名包装的绕过，另有四条自测防止守卫失效）
- **卡顿最小复现脚本**（`tests/bench/render-callback-loop.mjs`）—— 可独立运行、自动判定成败

> **诚实说明**：上述修复的循环是一条**确证存在**的缺陷（复现脚本实测 0.3 秒 200 次 vs 稳定引用 1 次），
> 但经两轮独立复查，**它是否是本次卡顿的主因尚未证实**。日志中唯一可见的通道 `agents:list`
> 落在每次停滞的**末段**（事件循环被占死期间无法记账，它是恢复后的第一拍），属症状而非元凶。
> **主进程卡顿的真凶仍在定位中**，诊断埋点方案待定。

---

## 0.13.75 · 2026-09-19

**修：终端状态栏就地可开启 PowerShell profile；设置窗口去最小化/最大化并独立命名；更正 v0.13.71 的错误位置指引**

### 改

- **状态栏的 shell 名现在可点**：终端顶部那行 `PowerShell（未加载 profile）` 点一下即开启 profile
  并重启终端，文字随之翻成 `PowerShell（已加载 profile）`；再点一次可关。
  开关不再只住在设置页里——**提示与入口必须在同一个位置**

### 修

- **设置窗口不再提供最小化与最大化**：右上角只保留系统关闭按钮。此前点「-」后，任务栏的窗口缩略
  按钮会停留在屏幕左下角并遮挡内容；禁止最小化后该状态不会产生。应用内「返回」与 Esc 出口不变
- **设置窗口的标题栏与任务栏名称改为「设置」**：此前与主窗口同名（渲染入口是单一 HTML，
  其文档标题会覆盖窗口标题声明），两个窗口在任务栏里无法区分
- **更正 v0.13.71 那条记录的导航错误**：原文写"设置页「系统」区"，而设置页导航中并无该分区，
  实际位置是「通用设置」→「终端 profile」组。已发布的 Release 正文不改（对外留痕），
  在此更正并补上就地入口

---

## 0.13.74 · 2026-09-19

**改：右栏工作台减负（Playbook 并入设置 / 时间线并入主对话）+ 卡顿定位探针**

### 改

- **右栏可选项从 9 项收敛到 7 项**：Playbook 移进「设置 · Playbook」分区，时间线移进主对话——
  工作台只留高频工作面板（资源管理器 / 文件变更 / 源代码管理 / 终端 / 浏览器 / 任务管理 / 记忆）
- **时间线改为主对话头部「时间线」开关**：点击在对话顶部浮出执行事件流（工具 / 审批 / 裁剪），
  不必再占一栏；主对话列最窄时也不会把两边挤没
- **选择器自适应排布**：面板选择项由竖排改为随宽度自动换列的网格，宽了铺开、窄了收拢，不再挤成一坨
- **修空工作台"时铺满时不铺满"**：空态此前只按内容宽渲染，现固定占满整行宽

### 新

- **卡顿定位探针（IPC 面包屑）**：主进程为每个通道记进出，事件循环停滞时把"进了没出"的最近几条
  一起写进日志——下次卡顿能在 `app.log` 直接指认是哪个通道占死了主进程，不再只有"卡了"这一句

---

## 0.13.73 · 2026-09-19

**改：右栏/主对话列布局改比例制（65/35）—— 拖到极限也不再"主列不够小"**

### 改

- 右栏宽度上限从固定 640px 改为**比例**：最多占可分配宽（窗口 − 左栏）的 65%，且主对话列
  保底 360px（输入框与气泡的最小可读宽）——1920 屏 + 左栏 300 时，主列最窄从 ~970px 降到 ~567px，
  比例随窗宽自动伸缩
- 窗口尺寸变化时已存的右栏宽**自动重钳**（含窄窗启动读档），比例不是一句空话
- 左右两栏仍是可整体收起的抽屉；左栏维持原区间（180~420px），本次不动

---

## 0.13.72 · 2026-09-18

**改：会话刻度条手感 + 设置入口显性化（用户实机反馈三改一批）**

### 改

- **刻度条更好点**：命中热区扩到 24×12px，条形码本体保持 8px 细线——好点中，不显粗
- **悬停即出预览卡**：鼠标指到哪根刻度，那轮发言立即浮出（此前只有滚动激活的那根显示）
- **滚轮跳轮**：光标在刻度条上滚动即可上/下一轮跳转，不必再对齐光标点选
- **设置入口**：侧栏齿轮图标改为「设置」文字框；设置窗口标题旁新增浅色「返回」——
  设置页定位澄清为软件内页，返回与 Esc/× 同一出口

---

## 0.13.71 · 2026-09-18

**新：内置终端可选加载 PowerShell profile（默认关）**

### 新

- 设置页「通用设置」→「终端 profile」组新增「加载 PowerShell profile」开关：开启后内置终端执行你的
  PowerShell profile，其中的别名与环境初始化（如 conda 激活）随之生效
  （**更正**：本条原写「系统」区，设置页导航里没有该分区，把人引向找不到的位置 —— 0.13.74 修正，
  并在终端状态栏就地加了同一开关）
- 默认关闭：终端以确定性优先（不加载 profile 启动实测更快，且不受 profile 内容影响）；
  开启可能引入未知延迟或报错，开关旁已如实说明
- 生效时机 = 下一次起终端；正在运行的会话不换壳

---

## 0.13.70 · 2026-09-18

**改：栏折叠语义 —— 折叠后页签条保留（右栏塌缩报障的观感修复）**

### 改

- 右栏「折叠本栏」不再把标题栏和页签条一起藏掉——旧语义下折叠的栏看起来像整列消失
  （09-18 右栏塌缩报障的观感另一半，用户拍板「留页签条」）
- 现在折叠只收起标题栏；页签条保留且常驻 ⌄ 展开钮：栏永远看得见、随时可还原
- 内容区行为不变：占满、宽度不变；页签滚轮横滚、脏标记、关闭入口在折叠态同样可用

---

## 0.13.69 · 2026-09-18

**修：电脑控制设置文案与现状对齐（稳定基线修订）**

### 修

- 「电脑控制」设置区仍写着"当前版本尚未搭载电脑控制工具"——该说法在桌面操作能力上线后已失真。
  现在如实说明：工具由外部 MCP 服务（windows-mcp）提供，**开关是权限闸**；
  开启后若尚未添加该服务，界面当场提示"需先添加才会下发"

### 其他

- 仓库规范化：补齐 Issue 模板（问题反馈 / 功能建议，中英双语标签）

---

## 0.13.68 · 2026-09-18

**新：模型自接入去死循环 —— 免保存即可拉模型 + URL 版本段修复（plan47）**

### 新

- 「获取可用模型」**不再要求端点先保存**：填好**协议 + 地址 + API Key** 三样即可拉，
  勾选导入后再一次性保存 —— 破掉「想拉列表须先保存、想保存须先填模型名」的死循环
- 编辑已存端点时 Key 留空仍回落那把已存密文，行为不变
- **拉取失败给人话三档**：① 该端点不提供列表 → 指路手动填写模型 ID；② Key 被拒（401/403）→
  提醒按量与套餐常是两把不同的 Key；③ 连不上 → 提示检查网络与代理

### 修

- **baseURL 版本段判据写死 `/v1` 的缺陷**：智谱 `/api/paas/v4`、火山方舟 `/api/v3` 等
  非 `/v1` 地址此前会被再插一层 `/v1` 而必 404；现按通用版本段识别（`/v1` `/v3` `/v4` `/v1beta` …），
  已带版本段的地址原样拼接

---

## 0.13.67 · 2026-09-18

**新：外部工具动作"看得见" —— MCP 来源徽标 + 参数明文回显（plan44 S2 之一/三）**

### 新

- 外部 MCP 工具的调用卡片挂 **`MCP·<服务器名>` 徽标**，工具名只留本名（如 `Click`）——
  "这是跨出应用边界的动作"必须一眼可辨
- 桌面类工具参数多为坐标数字，此前卡片显示"执行中…"——现在**明文回显**（如 `x=1024 y=768`，
  至多三个标量、80 字截断）；具名工具的既有显示规则不变

### 边界

- 截图缩略图回显（S2 之二）需开图片落盘侧信道，**单开一批**（plan44 S2b）；
  当前模型侧走 UI 元素树（Snapshot）路径，不依赖截图视觉——与选型"不挑模型"一致

---

## 0.13.66 · 2026-09-18

**新：电脑操作能力接入 —— 做能力的支持，而非能力本身（plan44 S1+S3）**

### 新

- **设置页 MCP 区新增「电脑操作（computer-use）」推荐卡片**：依赖检测联动开发环境探测（uv 在不在一目了然）、
  「查看风险说明」完整披露（含"无法区分你本人与 AI 的输入"的技术天花板诚实行）、
  「添加」仅写入 windows-mcp 服务配置——**不代装、不代开权限**
- **两道闸独立**：① 添加 MCP 配置；② 通用设置「电脑控制」开关——开关关闭时桌面操作工具**整体不下发**
  （模型根本看不见它们，而非"看得见但别用"）
- **工具白名单门控**：仅放行屏幕/键鼠/UI 树类 15 个工具；文件系统、注册表、PowerShell、网页抓取类**一律屏蔽**
  （避免绕过工作区边界与重复造轮子）；**升级新增的未知工具名默认屏蔽并记日志**——宁可进不来，不悄悄进来
- 进程终止类工具放行但描述带风险标注；桌面操作**仅作用于主显示器**（按工具参数如实覆写）
- 自视段如实更新：电脑控制开启时报"桌面操作工具由用户配置的 MCP server 提供"

### 边界

- 开启后 AI 的点击/输入与你的在系统层无区别——风险披露已如实说明；敏感界面勿在开启状态下离开电脑

---

## 0.13.65 · 2026-09-18

**新：设置页「开发环境」—— Agent 用哪个运行时，你说了算（plan43 S1+S2）**

### 新

- **运行时探测**：六类来源（PATH / conda / nvm / volta / pyenv / uv）并发发现本机 Node / Python / uv；
  版本一律 `--version` 实测（不信目录名）；商店占位符（执行会弹微软商店的假 python）自动排除
- **专业筛选**：能证明来源的按 conda → venv → 系统排序展示；证明不了的归「其他」**默认折叠**
  （折叠不隐藏，展开仍可选）——宁可折叠，不可错杀
- **下拉双行**：名称+版本+环境别名（如 `Python 3.12.13 ('ai_env')`）/ 完整路径 / 当前项勾选；
  选择即时持久化，卸载后触发器标「已失效」可诊断
- **刷新**按钮重探（结果不缓存过期问题由手动刷新兜底）

### 边界（立场，不是缺陷）

- **没有「＋」按钮**：探测是唯一入口，找不到就是没装；不下载、不代装、不指路——
  装环境这件事本身可以交给 Agent
- 本期只做配置面；**选中运行时注入 Agent 执行环境（S3）单开一批**

---

## 0.13.64 · 2026-09-18

**新：语音输入 —— 点按录音，转写插到光标处（端点自配，plan45）**

### 新

- **输入框麦克风按钮**：点按开始/停止录音；转写结果插入光标所在位置；60 秒自动停（防忘停），
  不足 0.5 秒按误触丢弃；转写失败**保留本段录音可重试**，不静默丢弃
- **设置页「语音输入」区**：OpenAI 兼容端点 URL + API Key（系统加密存储）+ 模型名（可选）+ 语言 +
  「测试连接」；附可选服务清单（本机自建 / 云端兼容 / 内网自建）——**只说明，不代装、不预置任何地址**
- **一次性隐私披露**：首次录音前展示"音频将发往哪个端点"，确认后记住，设置页可重看
- 麦克风权限：仅放行**纯音频**请求，摄像头与其他权限维持默认拒绝

### 边界（有意为之）

- 标点、分段、ITN 全部由转写端点决定，客户端**零后处理**；端点未启用标点模型时结果可能无标点
- 不内置任何语音模型与 Key；真机验收项：本机 SenseVoice 对 webm 的支持（T1）、真麦克风链路（T2）

---

## 0.13.63 · 2026-09-18

**修：记忆读取命中缓存 —— 判据 10 裁决落地（D-106）**

### 修

- **记忆加载不再逐轮重读未变文件**：按 (修改时间, 字节数) 判新鲜，没变就跳过读盘与解析；
  满载 100 条的基准耗时 **-63%**（bench:all 三跑实测，Windows Defender 实时防护环境下重回阈值内）。
  应用内写入/外部手改/删除文件均即时可见（4 条新增单测锁行为）

---

## 0.13.62 · 2026-09-18

**修：记忆注入的隐性开销 —— 每轮对话不再重复全量两两比对**

### 修

- **记忆读取热路径**：「疑似重复」检测此前每次加载记忆库都做全量两两相似度比对（条目数的平方级），
  且位于每轮对话的注入路径上；现按内容签名缓存比对结果，记忆没变就不重算。
  满载 100 条实测：单轮读取中位数 139ms → 73ms（同机对照，含系统实时防护底噪 ~49ms）

### 其他（开发侧）

- 新增 **`npm run bench:all`** 一条命令聚合基准：JSON + 人话 MD 报告、与上次读数对比、首跑标 baseline、
  环境对照（裸同步读 ms/文件）随报告输出——阈值判读不再被机器状态误导（plan26 S4 兑现）
- 修复记忆基准的播种（被写入侧相似闸门误拦）；基准读数如实呈现，判据 10 在实时防护环境超线**未调阈值**，
  待裁（方案见 `NOTEBOOK/problem.md`）
- CI 动作 checkout/setup-node 升 v5；构建命令前置清扫 electron-vite 临时配置孤儿

---

## 0.13.61 · 2026-09-17

**新：会话刻度条动起来 —— 滚到哪轮，哪轮亮起并浮出预览**

### 新

- **滚动联动高亮**：消息区滚动时，右缘刻度条实时点亮当前轮（激活刻度横向变长 8px → 28px，主题色）；
  挂载与缩放时自动重测各轮位置，滚动路径零重渲染
- **激活预览卡**：当前轮点亮时，其刻度向左浮出一张单行文字预览（≤60 字，附件以「[附件]」占位），
  垂直居中对齐该刻度；预览卡为纯展示层，不拦截任何点击

### 修

- CHANGELOG 上一版的 0.13.60 条目重复录入两份，去重（无用户可见变化）

---

## 0.13.60 · 2026-09-17

**新：技能 / MCP 的开关与管理 —— 装进来的能力，看得见、关得掉**

### 新

- **设置页技能管理**：新建（表单）/ 导入 .md（解析预填、确认再存）/ 删除（走回收站）/ **开关**；
  内置与自定义**分组显示**；内置项带「›」展开一句话中文简介，自定义项由你自己负责
- **内置技能换代**：9 个通用方法论技能随包分发（证据先于断言 / 系统化排查 / 设计先行 / 写计划 /
  方案压力测试 / 并行派发子代理 / 代码评审 / 知识提纯 / 写技能的方法论），全部带中英双语简介；
  原先两个携带本机约定的技能已移除
- **MCP 开关**：每个服务器一个开关，关掉 = **立即断开进程、工具不再下发给模型**（不只是藏界面）
- **输入框「+」面板**：同步展示当前开启的技能与 MCP（两者分开列，与设置页实时一致）
- **输入框「/」浮层**：输入 / 后上下键选技能，回车插入调用前缀，接着写任务即可

### 改

- **子 Agent 取消「项目级」**：只保留内置与自定义两层（子 Agent 随场景而行动，分层与开关都是冗余步骤）；
  工作区 `.agents/` 里的旧定义会在加载警告中提示迁移

### 修

- 技能开关名单里的失效条目（技能已删除）会在打开设置页时自动清理，不再出现「点不动的僵尸项」

---

## 0.13.59 · 2026-09-17

**新：消息操作条 —— 复制正文 / 重新生成 / 编辑提问；回退提示说清"什么退了、什么没退"**

### 新

- **消息操作条**：每条消息下常驻操作按钮 —— **复制正文**（成功后图标变对勾，1.5 秒自动复位）、
  **重新生成**（仅最后一条回答；等价于回退到该条之前、用同一提问再问一次）、
  **编辑提问**（仅你自己的消息：回退到该条之前，并把原提问填回输入框，改完可直接重发）
- 消息带**时间戳**（新产生的消息起）；旧存档没有时间信息则不显示（**不编造**）

### 改

- **回退提示说清作用范围**：此前提示条与确认框都写「仅回滚对话消息，不影响工作区文件」，
  容易被读成"什么都没发生过"；而实际是**对话退了、工作区文件与 git 提交没有退**。
  现在改为明确的两段式说明，确认框与提示条同步

### 修

- **超长会话的中间轮可能被界面过滤掉**：发送新消息时界面会过滤"没有正文"的历史消息，
  而其中「只有思考/工具、没有正文」的轮次是合法的 —— 被过滤会让消息下标与磁盘不一致，
  使"回退到某条之前"定位偏移。现已与落盘侧规则对齐

## 0.13.58 · 2026-09-17

**新：工作台页签栏支持滚轮横向滚动；修：超长会话的分段降级可能让整批保存失败、当轮消息全丢**

### 新

- **页签栏滚轮横滚**：工作台各栏的页签条现在支持**滚轮直接横向滚动**，不必再去够那条原生滑条。
  页签未溢出时不劫持滚动（照常冒泡）；原生滑条隐藏后，触控板横划路径保持不变

### 修

- **超长会话的分段降级可能让整批保存失败**：会话总长触及上限、触发"丢分段保正文"时，
  中间轮"只有思考/工具、没有正文"的消息会在丢掉分段后变成非法内容，导致**整批保存被拒**——
  表现为该轮对话全部丢失。现在这类消息的分段不再参与丢弃

## 0.13.57 · 2026-09-17

**修：权限档切换即时反映到终端界面；真实渲染门禁 7 项存量红清零**

### 修

- **权限档变更即时生效于终端面板**：切换到「只读」后，面板立即重取权限并如实显示拒绝状态，
  不再出现"横幅写着只读、界面仍显示运行中"的窗口期（主进程侧拦截一直在线，本次补齐界面这半环）
- 真实渲染门禁：页签保活改造后 8 处探针因全局选择器命中其他页签产生误报（含两处静默空转，
  判据已失去约束力），本次为探针建立"激活页签"作用域契约并全部限定；终端"重挂重放"场景改为
  关闭页签→重开以制造真实时序。门禁 339 项全绿

## 0.13.56 · 2026-09-17

**新：执行过程与回复按真实顺序交错显示，重启后不丢；模型切换不再可能改错配置**

### 新

- **过程与结果交错呈现**：此前思考与工具卡片整体聚成一坨插在回复前面；现在思考块、工具卡、正文
  按实际发生顺序穿插在同一条回答内，"哪段结论来自哪次执行"一眼对上。执行分段随会话落盘，
  **重启后回顾不再只剩纯文本**（分段过长时自动舍弃分段、正文一字不丢）
- 会话中间的"只有思考/工具、无正文"轮次现在也会被保存（此前会被丢弃，导致回滚定位偏移）

### 修

- **模型快速切换永不改名**：在切换器输入或打开老会话时若模型名不在目录中，此前会"把当前模型改名"
  （实案：端点配置被静默改掉导致请求 503）。现在只按名字精确切换到已有条目，找不到则明确提示，
  配置不发生任何变化；改名操作只保留在设置页的显式编辑中

## 0.13.55 · 2026-09-17

**修：网页抓取不再被重定向和错误页卡住；浏览器不可用时的提示给出真实出路**

### 改

- **fetch_url 自动跟随重定向（受控）**：此前出于安全策略完全不跟随 302/301，微信文章等常见跳转站点直接抓取失败。
  现在自动跟随至多 3 跳，每一跳都重新经过内网拦截与协议白名单校验，且 https 起始地址不允许被降级为 http；
  失败原因分类报告（安全策略拒绝 / 地址格式错误 / 跳转超限 / 超时），便于确定下一步
- **错误页不再冒充正文**：站点返回 HTTP 200 但内容实为错误提示页（如微信「参数错误」「该内容已被发布者删除」）时，
  工具将明确报告该页为提示页并给出处理建议，不再把页面残片当文章交回
- **内网拦截范围扩大**（SSRF 加固）：新增 mDNS/内部域后缀（`.local`、`.internal` 等）、运营商级 NAT 段
  （100.64.0.0/10，会一并拦截 Tailscale 自托管地址，属有意取舍）、IPv6 映射与 NAT64 内嵌 IPv4 展开判定
- **browser_navigate 提示修正**：浏览器未初始化时的报错不再建议"稍后重试"（无效操作），改为说明真实状态与出路；
  浏览器适配器注入与否各留一条启动日志，便于排障

### 修

- 0.13.54 变更说明中「打开日志目录」应为「打开日志文件夹」（设置页按钮实名），一并更正

## 0.13.54 · 2026-09-17

**改：关窗即时响应；内置搜索不再冻结应用；新增卡顿自检探针**

### 改

- **关闭窗口即时响应**：此前关窗需等待会话落盘（最长 2 秒），期间窗口无视觉反馈，易被误认为未响应。
  现在点击关闭后窗口立即隐藏，落盘在后台完成；隐藏期间双击应用图标可即时恢复窗口
- **内置搜索（降级模式）改为异步执行**：当随包 ripgrep 不可用时，内置扫描器此前同步遍历整个工作区，
  大型项目下可能使应用整体（含系统输入）无响应数秒至数十秒。现在扫描周期性让出执行权，不再阻塞界面
- **卡顿自检探针常驻**：主进程被单个任务占用超过 1 秒时，日志将记录停滞时长与所处执行阶段
  （设置 → 打开日志文件夹，可见 app.log 中的「事件循环停滞」条目），为性能问题提供可定位的依据

## 0.13.53 · 2026-09-16

**改：`run_command` 升级为持久 shell 会话——`cd`、环境变量、环境激活跨步存活了**

### 改

- **命令会话复用**：此前每条 `run_command` 都冷启动一个新 shell，`cd` / `set` /
  `call activate` / `source venv` **全都不跨步存活**——Agent 永远进不了目录、激活不了环境。
  现在前台命令跑在一个持久会话里，上述状态**跨步延续**；每个 agent run 从工作区根重新开始（可预测性优先）
- 会话起不来的怪环境自动回落一次性执行，能力不打折；空闲 10 分钟自动回收，同活上限 4 个
- **browser_read_page 读页上限 20K → 512KB**（与 fetch_url 同级），截断统一交给内核窗口化兜底

## 0.13.52 · 2026-09-16

**新：web_search 联网搜索（默认零密钥 + Firecrawl 可选）；记忆去重；命令结果带机器可判尾标**

### 新

- **web_search 搜索工具**：默认走 DuckDuckGo（零密钥开箱即用）；设置页可选配置 Firecrawl 密钥切换
  Firecrawl 源；**Firecrawl 失败自动熔断 10 分钟直走默认源**——免费额度耗尽不会拖慢搜索，密钥永远不是唯一依赖
- **记忆去重**：保存时相似度闸门（同文换名 / 包含 / 近乎相同才拦，宁可漏放不错杀）+「记忆」面板新增
  **疑似重复**区，可一键合并或忽略
- run_command：结果附 `exit_code / error_class / elapsed_ms` **机器可判尾标**，失败分型不再靠猜

## 0.13.51 · 2026-09-16

**修：docx 预览卡在旧窗口尺寸**

### 修

- 页签保活包装层改 `display:contents`：切页签后 docx 预览不再卡在旧窗口尺寸

## 0.13.50 · 2026-09-16

**新：技能系统 / MCP 客户端 / 规则系统 / 用户画像 / 时间线面板 / 计划批准；界面卡顿治理**

### 新

- **技能系统**（F6）：技能 = 渐进式披露的流程知识包，按需加载
- **MCP 客户端**（F7）：stdio / SSE 两种方式接入外部 MCP 工具
- **规则系统**（P3 收口）：无条件注入的约束层
- **用户画像**：`class=profile` 特殊记忆，跨会话记住使用习惯
- **时间线面板**：统一执行事件流，可回放每一步做了什么
- **智能标题 + 滚动摘要 + 裁剪可见性**：长会话标题自动起、上下文裁剪可见
- **计划批准**：planner 给出方案后停下等用户点头，不再闷头执行
- **edit 工具**：精确替换三判据（不唯一即拒 / 任一段失配整次不落盘 / 空串即拒）
- **命令超时可调**：默认 30s→120s，模型可传 1s~600s（越界拒绝不静默夹取）；输出上限 1MB→8MB；
  超限 / 超时 / 真失败三者分开报，不再把「被我们掐了」误报成「命令失败」
- **完全访问档名实相符**：Agent 线真无边界，界外操作结果标注绝对路径；界面线（检查点等）分毫不动

### 优化

- **渲染卡顿治理**：页签保活（切页签不销毁重建）+ 消息 Markdown 解析缓存 + 九个内置面板 memo——
  会话切换实测 765ms→281ms（实际感知 ~31ms 量级），卡顿帧 87→2

## 0.13.49 · 2026-09-15

**改：界面文案去掉开发批次标注；修正 README 的一处路径口径**

### 改

- **设置 → 记忆**：「自动记忆（批 2）」→「自动记忆」
- **输入框用量提示**：「反思用量（批 2 · 会话切换时跑）」→「反思用量（切换会话时调用）」
- 界面文案**不出现开发批次标注**（规范见 `AGENTS.md` §十）；代码注释里的批次标注**保留**
  —— 那是给后来人看的留痕（§九），与界面文案是两份合同
- **README 候选目录口径更正**：原写 `memory/candidates/`，实际是 `<root>/candidates`
  （与 `<root>/notes/` 平级）；物理隔离靠"`listFiles()` 只列 notes"，不是靠目录嵌套

## 0.13.48 · 2026-09-15

**修：Playbook 写完却零接线（模型其实调不到）；两个度量指标定义了却从不产生数据**

### 修

- **Playbook 接线补齐** —— 此前只写了纯逻辑、**零接线**：模型调不到 `save_playbook`。
  现在磁盘后端 / 装配层 / 工具注册 / IPC 三通道 / 右抽屉面板全部接通
- **纠正通路开口** —— `remember` 此前**没有任何「改既有条目」的通路**（同名一律拒，
  而拒绝文案还写着「请先编辑那一条」，等于让它做一件做不到的事）。
  现在因果链成立时允许纠正
- **重复纠正率 / 误伤率终于有数据** —— 此前 `correct` / `flag` 事件定义了却从不发出，
  两个指标恒为 `null` / `0`

> 这几个洞是**四道闸全绿的情况下漏掉的** —— 因为四道闸都在回答「我写的东西对不对」，
> 没有一道回答「这些代码在运行时被谁调用」。撰写交接文档时按「每条结论都要能验证」
> 逐项核实才发现（四条 grep 零命中）。

## 0.13.47 · 2026-09-15

**增：自进化记忆批 2/3/4 —— 它开始自己琢磨你，也开始记「怎么做」**

### 增

- **自动捕获（批 2）**：切换会话时它自己复盘一遍，把觉得该记的**先放进候选区**，
  **你批准才生效**。候选物理隔离在独立目录，批准前不进上下文
  —— 附带的好处：反思要读会话正文，万一正文里有你贴过的密钥，写不进去
- **Playbook（批 3）**：「怎么做某件事」的经验独立成册（`evolution/playbooks/`），
  按标签条件召回（活跃标签有交集才注入）；预算与语义记忆**分开**，不互相挤占
- **遗忘与度量（批 4）**：LRU 自动遗忘（`style` 类豁免）；描述相似度只**警告不自动合并**
  （合并需要语义判断，机器做不了）；重复纠正率 / 误伤率从事件流算出

### 说明

- 批 2 的反思会**读会话正文**，并可能产出候选；候选在你批准前不进注入索引段。
  但反思本身是**一次出境的模型调用**，请据此决定是否开启

## 0.13.46 · 2026-09-15

**增：会话大纲（问答段跳转）；修：工作台文件预览不铺满纵向空间**

### 增

- **会话大纲**：聊天区右缘新增悬浮「大纲」入口，展开后列出本会话各轮提问（附件折叠为占位、
  超长两行截断），点击任一条即平滑滚动定位到该轮问答，落点朱砂高亮一闪——「跳到了哪」看得见。
  点空白处或按 Esc 收起；两轮问答起才出现，单条提问不摆按钮。

### 修

- 工作台文件预览（二进制 / 图片等视图）此前不占满纵向可用空间，现撑满整个栏高；
  十六进制与图片视图同批修正，超高内容照常滚动。

## 0.13.45 · 2026-09-15

**增：模型按端点分组切换；任务清单全部完成自动折叠；模型自视配置报告；设置页电脑控制开关**

### 增

- **模型切换按端点分组**（设置页列表 + 输入框下拉两处）：同一条 API Key（端点）为一组，
  原显示名称做组名，组下摆该端点全部模型。切换粒度从「端点」下沉到「模型条目」——点组下某个模型
  即激活该条目（端点未激活时一并切）；输入框按钮显示**当前模型名**，悬停可见端点与模型 ID 全量信息。
- **任务清单全部完成自动折叠**：清单从未完成走到全完成的瞬间自动收起，统计行保留当证据；
  手动展开后不会被自动折回（沿触发，只收一次）。
- **模型自视段**：每轮对话向模型报告当前运行配置——模型名、连接协议、运行端、电脑控制状态、
  实际下发的工具清单与可派子代理。模型对「你能做什么」一类问题可以如实作答，且不得声称
  清单之外的能力；未上线的技能 / MCP 不出现在报告里，不制造能力幻觉。
- **设置页新增「电脑控制」开关**：允许模型操控鼠标键盘与桌面应用程序交互的能力预埋。
  当前版本尚未搭载对应工具，开关先保存偏好并进入模型自视报告；功能上线后此处即权限闸。
  开启时会当场提示现状。

### 修

- 模型切换下拉此前只到端点粒度，端点下配了多个模型时无法指定用哪一个。
- `models:set-entry` 通道补广播：此前该通道无调用者，切换模型条目后其他窗口可能拿到旧激活值。

## 0.13.44 · 2026-09-15

**修：模型页新增端点「测试连接」用错模型导致 HTTP 400；API 密钥「已保存」状态显示假象**

### 修

- **新增端点的「测试连接」改用表单里填的模型**：之前该场景的请求体会带上全局激活模型的残留 ID
  （如默认的 `deepseek-flash`），打到新端点必然报 `Unsupported model`（实测：小米 MIMO 端点返回
  HTTP 400）。现在取「模型目录」第一条非空模型 ID；目录为空则提示先填写模型 ID。
- **「获取可用模型」的提示说清真正缺的动作**：端点尚未保存时，提示由「请先保存该端点（填写地址与
  API Key）」改为「该端点尚未保存，请先点『保存模型』，再拉取模型列表」——判定条件是端点未入库，
  原文案会误导用户反复检查地址与密钥。
- **API 密钥区的「已保存」状态改读当前编辑端点的真值**：之前读的是通用设置的密钥状态，新增端点时
  也会显示「已保存：sk-****xxxx」的假象；现在新增端点如实显示「尚未保存」，编辑态显示该端点自己的
  密钥掩码。

### 工具

- 新增 `scripts/dist.ps1` 出包专用入口：进程树内关闭 safe-delete 对构建产物的删除拦截
  （`out/` 与 `dist/win-unpacked/` 每次全量重建 200+ 文件，必触批量删除确认），并清除宿主可能携带的
  `CI` 环境变量（否则 electron-builder 误判处于 CI 而要求 GH_TOKEN）。用法见脚本头注释。

## 0.13.43 · 2026-09-15

**修：新会话首条消息重复（界面与模型各收到两次）与随之而来的无响应；改：Token Saver 档位排序**

### 修

- **新会话的第一条消息不再重复**：新建任务时首条输入会被「创建会话」与「发送」各处理一次 ——
  界面显示两条、模型实际收到 **`[user, user]` 两条连续同角色消息**。
  部分兼容后端对连续同角色消息会卡住或返回空流（0.13.42 反馈的"长时间无回复"与它高度相关）。
  现在首条只发一次；门禁新增端到端断言（新任务页走真实流程，断言界面与载荷各只有一条）。
- **对话链路补诊断三件套**：开始时刻 / 首包时刻（正文或思考）/ 完成耗时全部落日志 ——
  之前"卡死"发生时日志一片空白，连"请求发没发出去"都说不清。下次再遇到，日志能直接区分
  「请求没发出 / 发了没首包 / 首包后断流」三种卡法。

### 改

- **Token Saver 档位展示顺序**改为「轻量 → 平衡 → 极致 → 土豪」（按省力程度从高到低，默认档居中）。
  仅展示顺序；各档行为不变。

### 开发者可见

- 选中即记（通路 B）保存成功不再弹系统对话框（会阻塞渲染进程），失败原因改为卡片内就地显示、
  可原地重试；成功的"已记住"反馈由对话流里的写入痕迹面板承担（本来就有）。

---

## 0.13.42 · 2026-09-15

**修：设置页 ⓘ 气泡显示缺陷；改：外观/子 Agent/记忆三页按反馈调版；增：导入其他记忆**

### 修

- 设置页所有 **ⓘ 气泡**的显示缺陷（通用设置的访问权限 / Token Saver / 系统 / 网络等处）：
  原来气泡**向上弹 + 水平居中**，靠近视口顶部的被滚动容器裁掉上半截、靠左的向左溢出被裁，
  且会盖住上方控件。现改为**向下弹 + 左缘对齐记号 + 限高内滚**，一处修复全站生效。

### 改（按反馈逐条）

- **外观页**：三个 ⓘ（主题 / 界面字号 / 界面字体）全部撤除；
  字号档「特大」不再孤零零缩在左上 —— **铺满整行且文字居中**，与上排三档对称。
- **子 Agent 页**：ⓘ 撤除；「自定义 / 项目 / 内置」三栏间距收紧；
  语病修复：「暂无自定义定义」→「**暂无自定义Agent**」。
- **记忆页**：注释不再收 ⓘ，**直接排成文字**（是什么 / 去哪管 / 安全边界三句话）。

### 增

- **导入其他记忆**（设置 → 记忆）：把其它 AI 工具里关于你的长期信息搬进来。两步：
  ① 复制提示词到那个 AI 对话；② 把它的回答粘贴回来，自动拆成条目逐条导入。
  导入与手填走**完全相同**的校验与落盘通道 —— 撞名、超限、凭据形状一样会被拒并逐条说明原因，
  导入没有后门；导入条目按用户主动搬运记 `origin: user`。

---

## 0.13.41 · 2026-09-15

**增：记忆（自进化第一批）—— 它开始记得你说过的偏好，并且看得见它记了什么**

### 它能做什么

- 在对话里说「记住……」，模型会把它写进记忆库；下次新对话**直接生效**，不用再说一遍。
- **选中即记**：选中对话里的任意一句话，右键「记住这句」—— 这条**不经过模型**，
  记下的就是你选中的原话。
- 右栏新增「记忆」页签：查看、编辑、删除；模型自己写的条目集中在**「本次新增」巡检区**，扫一眼就知道它记了什么。
- 删除立即生效：同一段对话的下一轮起，那条记忆不会再出现。
- 用量牌新增「**记忆税 N(估)**」—— 记忆占了多少上下文，是个能看见的数，而不是感觉。

### 它怎么保证不闯祸（如实，不是套话）

- **三道防线**：记忆层代码物理上碰不到权限设置（架构守卫，且有单测钉死）；
  「免确认 / 不用问」一类授权表述**拒绝写入**并指路设置页；已知形状的密钥/token **硬拒**。
- 防线是**必要非充分条件**：判定可被同义改写绕过，只读档也有出境能力，记忆文件是本机明文。
  所以巡检区是给用户的习惯，护栏不是给用户的承诺 —— README 里有完整声明。
- 「完全访问」档下开启记忆会**当场红色告警**（这个组合风险最大），每次重新开启都会再提醒一次。

### 可以关

- 设置页新增「记忆」分区：关掉「允许模型写记忆」后，模型侧工具整个不下发（结构性关断，不是嘴上说说）；
  你自己选中记下来的不受影响。

### 面向开发者

- 事件流（`memory/events.jsonl`）落地：写入（含被拒）/ 召回 / 删除 / 标记 / 注入，批 1 先埋数。
- bench（判据 10）：100 条满载读盘 p50 **14.29ms**、保存 p50 **25.50ms**（本机 NVMe · 热缓存）；
  阈值按实测 ×3 登记为回归警报线。
- 已知边界：`maxEntries 100` 与 `maxIndexBytes 8KB` 满载时只注入 81 条（如实显示"未生效"），批 4 校准。

---

## 0.13.40 · 2026-09-14

**改：设置页全面极简 —— 说明文字收进 ⓘ；增：内置子代理三领域体系**

### 界面极简

- 选项卡（主题 / 字号 / 权限 / Token Saver）内不再直接显示说明文字，卡片只留标题。
- 各组说明统一收进组标题旁的 **ⓘ**（悬停或点击展开，气泡内分条列出各选项说明）；
  系统、网络、子 Agent 分区的组级说明同样收取。
- **保留直接显示**（它们是状态与预览，不是注释）：代理「当前生效」探测行、
  字体预览行、「设了不等于生效」类故障提示行、子 Agent 每行的职责简介。

### 内置子代理扩展（规划 / 执行 / 审查）

- **规划 3**：`scope-planner`（全局拆解与基线）· `detail-planner`（分步细化）· `risk-planner`（风险预判）
- **执行 4**：`code-executor`（代码执行）· `doc-executor`（文档生成）· `data-executor`（数据处理）·
  `adaptive-executor`（动态适配与异常处置）
- **审查 3**：`quality-reviewer`（成果质检）· `compliance-reviewer`（规范审查）· `iteration-reviewer`（复盘迭代）
- 原有的 `planner` / `reviewer` 由上述体系取代；全部为只读或按职责限定的工具白名单，
  可在「设置 → 子 Agent」查看，模型通过 `spawn_agents` 按职责说明自主派发。

## 0.13.39 · 2026-09-14

**增：子 Agent 管理 —— 定义可以创删改查，选择真的生效**

### 以前

内置的「规划员」「审查员」只能看不能改；自定义 Agent 只能手工往数据目录塞文件；
输入框「＋」里能勾选"技能 / 子 Agent"，但选择不会被记住也不会生效（纯装饰）。

### 现在

- **设置 → 子 Agent**：三层列表（内置 / 自定义 / 项目）+ 表单新建 / 编辑 / 删除。
  定义仍是 Markdown 文件（可手改，列表会反映）；表单校验与解析器同一条规则。
  同名覆盖关系在列表里可见；加载失败的文件会在顶部警告区说明原因。
- **「＋」里的主 Agent 选择**：单选一个 Agent 后，从下一条消息起用它的职责描述、
  工具白名单与模型偏好跑对话；选择随会话保存，重启后恢复。
- **项目层支持**：工作区 `.agents/` 目录里的定义自动加载并优先于全局同名定义。
- **修复**：会话使用自定义 Agent 时，自建目标的"发起者"如实记录为该 Agent，
  而不再固定写"内核默认"。

## 0.13.38 · 2026-09-14

**增：六主题配色 —— 晴空 / 信纸 / 桃花 / 夜梦 / 春和 / 极光**

### 以前

只有两套主题（经典 / 水墨），且终端"浅色"其实是黑底白盖。

### 现在

- **改名**：经典 → **晴空**，水墨 → **信纸**（老设置自动迁移，不用重选）。
- **新增四套**：**桃花**（浅粉）、**夜梦**（护眼深蓝灰，不是纯黑）、**春和**（浅绿 + 白）、
  **极光**（浅蓝）—— 每套的成功 / 危险 / 警告色都随主题自成体系。
- **终端规则重定**：夜梦 = 黑色终端；其余五套 = **纯白终端**（真白底 + 深字），
  切主题终端即时跟随。

## 0.13.37 · 2026-09-14

**增：Agent 能自己立目标了 + 待办面板挂上"服务于哪条目标"**

### 现在

- 对话里交代长期意图（"以后每次都要…""这个项目最终要…"），Agent 会用新工具
  `set_goal` 登记成目标（标注"由 内核默认 创建"）；子代理不具备此工具。
- 待办清单挂着进行中的目标时，面板顶部一行「目标：…（等 N 条）」。
- **修：重启后待办清单一直空白**（此前待办只在应用启动瞬间拉一次，
  时机不对就永远拉不到）。

## 0.13.36 · 2026-09-14

**增：docx / xlsx 直接看内容 + Office 出口收敛；修：设置窗口双 X**

### 现在

- 工作区里点 **docx / xlsx / xls / xlsm** 直接内嵌预览（解析在主进程、
  沙箱 iframe 上屏，HTML 不落盘）；xlsx 多工作表可切换，超 500 行 × 50 列 × 12 表
  如实截断提示。
- **pptx 等不支持的格式**给「用系统程序打开」按钮（只限工作区内文件）。
- **修**：设置窗口右上角**双 X 叠加**（系统 × + 自绘 ×）——删自绘 ×，补 Esc 关窗。

## 0.13.35 · 2026-09-14

**增：数据目录位置可配置 + 安全迁移（设置 · 通用设置 · 存储）**

### 现在

- 数据目录可换成任意位置（含另一块盘），**排除法迁移** + 三段式校验，
  失败自动回退、不丢会话；回旧位置同样支持（反向迁移）。
- **数据目录锁**：两个实例指向同一数据目录时后者被拦（防止互相覆盖丢数据）。

---

## 0.13.34 · 2026-09-14

**修：工作区可以回内置默认了（设置 · 通用设置 · 工作区）—— 默认落点不再是单程票**

### 以前

可以把工作区换成任意真实目录，但一旦换了就**回不去内置默认** —— 恢复功能在底层存在，
却没有接到界面上。想回去只能手动去选那个藏得深的内部目录。

### 现在

- **「恢复内置默认」按钮**：选了自定义目录后一键回到内置工作区；
  内置默认档下该按钮禁用（点了也不会有反应的按钮不装作可用）。
- **文案说清这项设置的全部语义**：新任务默认在这个目录进行；
  已创建的会话各自绑定当时的工作区，改这里不影响它们。
- 恢复动作与「选择目录」同一语义：终端会话一并收掉，不留停在旧项目目录里的 shell。

### 说明

「存储」里的另一半（系统缓存目录的位置与旧数据迁移）是独立的一批，
见 `PLAN/plan10_会话存储与回滚.md` C 批 —— 那一步会动用户数据，必须单独做。

---

## 0.13.33 · 2026-09-14

**增：界面字号四档 + 系统字体选择（设置 · 外观）—— 字大了，全站跟着大**

### 以前

界面字号是写死的，高分屏或视力吃力只能靠系统缩放糊过来；字体也换不了。

### 现在

- **字号四档**：小 / 标准（默认）/ 大 / 特大。实现是根元素字号缩放，
  全站文字（含 token 化的字号体系）一起动，改了即时生效、重启保持。
  标准档与旧版逐像素一致 —— 不换档就感觉不到这次改版。
- **界面字体**：从系统字体里选（主进程读注册表，约 180 个字体家族），切换即时生效，
  设置页有预览；选回「默认」即恢复出厂字体栈。
  列不出字体的环境会明说原因并退化为手动输入，不给一个选了没反应的下拉框。
- **边界写明**：代码编辑器与终端保持等宽字号，不跟随此设置（与 VS Code 同一取舍）。

### 幕后（真机验证又抓出一个真 bug）

字体枚举的解析器第一版按"注册表值名带引号"写的，连单测都是照这个格式编的 ——
而真机 `reg query` 输出**根本不带引号**，真跑起来会得到一个空列表。
靠真机全量输出（299 条记录）的端到端探针抓出来并修复。
教训进了 `NOTEBOOK/learnings.md`：**单测桩必须照抄真格式，编一个想当然的格式等于没测。**

---

## 0.13.32 · 2026-09-14

**增：网络代理（设置 · 通用设置 · 网络）—— 配了代理，模型请求真的会走**

### 以前

设置里根本没有代理这一项；想给模型请求配代理，只能去开系统全局代理（TUN / 系统代理）。

### 现在

- **三档**：跟随系统（默认）/ 直连 / 手动配置。手动档支持 `127.0.0.1:7897`、
  `http=…;https=…` 分协议、`socks5://` 三种写法。
- **这一行是全部的关键**：「当前生效：…（按 api.openai.com 探测）」—— 由主进程实时探测显示。
  代理配错的表现是超时，而"没生效"和"生效了但连不上"对用户是同一个现象，只有这一行能分开两者。
- **凭据不落明文**：地址里的 `user:pass` 保存时会被剥出来走系统加密（safeStorage）单独存，
  界面只回显剥过的地址；账号密码填过一次就不再回显，可一键清除。
- **体检不过不保存**：手动档没填地址 / 地址有怪字符 → 当场说原因，不假装已生效。
- 文案诚实：代理只影响**之后发起的**请求，已建立的连接不受影响。

### 顺带修的一个真 bug（门禁抓出来的）

点「应用」时如果只发地址不带档位，主进程会拿"上次落盘的档位"顶替 ——
用户切到手动配置、填好地址、点应用，结果存完变回跟随系统，且不报错。已修。

### 幕后（为什么这次敢说"真的会走"）

模型请求原来走 Node 原生 `fetch`，它**不吃** Electron 的代理配置 —— 配了也白配，且完全静默。
本次把全部 11 处模型/网页请求收口到一个可注入的网络出口，启动时换成 Chromium 网络栈的 `net.fetch`
（两条探针实证：它吃代理、能流式读、能中断）。具体见 `scripts/probe-main-proxy.cjs` 与
`scripts/probe-main-net.cjs`（`npm run probe:netfetch` 不存在时直接 `electron` 跑）。

---

## 0.13.31 · 2026-09-13

**增：文件「移动」—— 资源管理器不再只会"同目录改名」**

### 以前

改名只能在同一个目录里改；想把文件挪进子文件夹，只能去系统文件管理器里做。

### 现在

- **拖到文件夹上就是移动**：把文件（或整个文件夹）拖到另一个文件夹行上，它就过去了。
  目录不存在会自动创建。
- **右键「重命名 / 移动到…」**：直接在输入框里写「归档/新名字.md」，一步改名带搬家。
- **不会悄悄覆盖**：目标位置已经有同名文件时**拒绝移动**并说明 ——
  文件系统自带的改名动作其实是**直接替换**的，那样会白白丢掉另一个文件。
- **不会把自己搬进自己**：把文件夹拖到它自己（或它的子目录）上会被拦下，并说清为什么。

顺手修掉一个小毛病：点「重命名」时原本会冒出**两个**输入框。

---

## 0.13.30 · 2026-09-13

**增：源代码管理面板 —— 工作区里改了什么，看得见也提交得掉**

### 以前

工作台里「源代码管理」这个页签一直是个**明写「尚未实现」的占位符**，
只显示得出一个分支名 —— 想提交还得自己去开终端。

### 现在

- **改动一条不漏**：列出所有变更（已修改 / 已新增 / 已删除 / 未跟踪），
  每条都带中文说明（不指望你自己看懂 `??` 是什么意思）。
- **勾选 = 暂存**：勾上就进「已暂存的更改」，取消则退回「更改」——
  取消暂存**只动暂存区，你的改动还在**，不会被丢掉。
- **点文件看差异**：增删行分别着色，不用切出去。
- **写消息提交**：没勾文件时按钮禁用并明说「先勾选要提交的文件」，
  勾了没写消息则说「还没写提交消息」—— 禁用一定给理由。
- **自动刷新**：Agent 改了文件、终端跑了 git 命令，面板自己就更新，不用手点刷新。
- **顶部那一行**：干净时显示「没有改动」，有提交堆积时显示「↑ N 个提交待推送」。

### 明确不做（避免误会）

分支切换、远程推送 / 拉取、冲突解决、提交历史浏览 —— 本批都不做。
其中**远程操作涉及凭证**（HTTPS 要 token、SSH 要 key），与本项目
「不内置任何 API Key」的硬约束冲突。这些仍可去终端里做。

---

## 0.13.29 · 2026-09-13

**改：设置变成独立窗口 —— 不再占着对话区，也不再是"进去出不来"**

### 设置现在是自己的一扇窗

以前点齿轮，是**把主区域的内容换成设置页**：对话被顶掉，看完还得「← 返回」退回来。

现在点齿轮，**另开一扇设置窗口**浮在主窗口之上，自带「设置」标题栏和右上角 `×`。
左栏四个分区（通用设置 / 模型 / 外观 / 故障排查），右栏是内容。
主窗口的对话**原封不动留在原地** —— 开着设置照样看对话，不用来回切。

### 改一项，主窗口立刻跟着变

设置窗口和主窗口是**两份界面、同一份数据**：你在设置里换主题、改工作区、切权限档，
主窗口**当场就变**，不用关掉设置窗口、也不用重启。反过来也一样。

### 「添加模型」进了设置窗口内部

点「添加模型」，表单在设置窗口里**就地把列表换掉**（左上角有「← 返回」），
外壳和左栏都还在 —— 不会又弹出一扇新窗口让人数不清开了几层。

### 顺带修掉一个潜伏的问题

多开一扇窗口之后，主进程里原来"取第一个窗口"的写法就不安全了 ——
危险操作的确认框可能弹到设置窗口上、关窗口时可能落错盘。
这回把所有这类写法都改成**按用途取窗口**（该给主窗口的给主窗口，该给设置窗口的给设置窗口），
确认框、日志告警、关窗落盘现在都落在正确的窗口上。

---

## 0.13.28 · 2026-09-13

**改：问答卡不再堆着不收 · 长注释收进「ⓘ」气泡 · 终端配色按你的意思对调**

### 问答卡：已答的直接收起来

以前每张答过的卡都摊在那儿，选项行、你写的字、模型回执全留着 —— 问几次就堆满半个会话区。
现在**答完自动折叠成一行**，那句话就把"我当初答了什么"说清了：你写的原文 / 选中的那项 / 或「未作答」。
想回看就点那一行展开，详情一字不少。上方还加了「全部清除」，一次收干净。

> 卡片高度从 337px 压到 41px。

### 长注释收进气泡（全软件通用）

设置页那些"一段话的说明"不再占版面了 —— 换成一个「ⓘ」标记，鼠标移上去（或键盘 Tab 过去）才出气泡。
「省 token 档位」也按你说的改名 **Token Saver**。
这套注释层是新做的通用组件，**日后凡是注释太长的地方都往它上面收**，不各写各的。

### 终端配色对调

按你的要求换过来：**经典 → 纯黑底白字**，**水墨 → 纸白底墨字**。两套的光标都是朱砂色。

---

## 0.13.27 · 2026-09-13

**修：可写访问档下 Agent 现在真的能执行命令了 —— 但每条命令都要你点头**

这一版修的是一个**承诺与实现对不上**的问题（全项目对账时发现）：

- 设置里「可写访问」一直写着「**执行命令仍需逐次授权**」，确认框与确认桥也都在（还配了 12 条单测），
  但**可写档的工具上限把"执行命令"整个滤掉了** → 确认框**永远不会出现**，因为那个工具根本不发给模型；
  而"完全访问"档又按设计不弹确认。于是那句话成了空话，可写档也跑不了测试/构建（模型改完代码无法自验）。
- 根因：09-12 引入「权限档是硬上限」时顺手把它压掉了，文档与界面文案都没跟着改。**四道闸全绿、单测全过**——这类死代码门禁抓不到。

**现在的行为**（与设置页文案一致）：

| 权限档 | 执行命令 | 确认 |
|---|---|---|
| 只读访问 | ✗ 没有这个工具 | — |
| 可写访问（默认） | ✓ 自定义 Agent 声明后可用 | **每次执行前都要你确认**（含后台命令） |
| 完全访问 | ✓ | 不弹窗（你明确选的"别拦我"） |

- 「内核默认工具集不含执行命令」这条**没有变**：开箱即用的默认 Agent 依然拿不到它，
  只有自定义 Agent 在 `tools` 里显式声明才会下发 —— 所以这不是"偷偷放开权限"。
- 新增三条单测把这条接线钉住（可写档下发 / 真的会问 / 完全访问不弹），
  并做了**证伪实验**：把上限改回旧行为 → **精确 2 条变红**。

### 内部

- `allowedToolsFor` 重写：可写与完全访问**工具集相同**，差别只在"高危工具要不要逐次确认"
- 单元测试 889 → **891**

### 代码检索换了引擎：自研扫描器 → ripgrep

**这不是性能优化，是把一处"说 A 做 B"对齐**——`plan3`/`plan4` 从 P1 起就写着
「L0 代码检索用 ripgrep」，实际跑的是自研逐文件扫描器。

- **现在的检索**：优先用随包的 ripgrep（版本与安装包绑定，不受用户机器影响），
  找不到时降级到内置扫描器，**并在结果里写明降级原因**（以前"搜不到"和"没搜"长得一样）
- **新增两个搜索开关**：正则模式、区分大小写
- **结果上限 50 → 200**；到上限时明说"还有更多没有返回"，而不是让你以为就这么多
- **如实报告被跳过的东西**：超过大小上限的文件会计数告知；跳过的目录会列出来
- 崩溃安全：会话写盘补上 `fsync`，断电/强杀后不再出现"文件名对、内容空"的存档
- 单元测试 891 → **924**

## 0.13.26 · 2026-09-13

**新：设置 · 通用设置多了「系统」一组 —— 锁屏/熄屏后继续运行 + 开机自启（plan7 批 F1）**

### ① 锁屏与熄屏后继续运行

打开后，**屏幕可以照常关**，但系统不会因为**空闲**自动睡眠，Agent 任务与终端进程继续跑。

- 它就是"主机不睡"这一个开关：**不会**让屏幕常亮（不替用户决定屏幕亮不亮）
- 界面如实写出边界：**合盖或手动从开始菜单睡眠仍会中断**；代价是**功耗与电池消耗增加**
- 取数不靠猜：开关旁边会显示"设置已保存但当前未生效"这类原因（真起不来时**不静默显示已开启**）

### ② 开机自启

登录系统后自动启动本应用。界面同时说明现在的两个边界：**尚未提供托盘，启动时会直接显示主窗口**；
**关闭主窗口即退出应用**，后台任务与终端一并终止。

- **只有安装版能用**：开发态会写入一个指向 Electron 而非本应用的启动项，所以在开发态直接禁用并说明原因
- 被系统"停用"也看得见：任务管理器里停用后，注册表项其实还在 —— 应用读的是"登录时会不会真的拉起"，
  而不是"启动项写没写进去"，因此不会出现"界面显示已开启、机器却不启动"
- 用户在系统里关掉它，应用**以系统为准**（不偷偷改回去）；换了安装目录 / portable 版路径失效时，
  按用户意图**补写**一次（不会把用户的选择悄悄抹成"关"）

### 内部

- 依赖全部注入的 `system-integration` 模块（**不 import electron** → 进得了单测链路）+ 两条 IPC 通道
- **真组合根探针** `npm run probe:system`：单测是替身、渲染门禁是 IPC 存根，两者都证明不了"真 API 被调到"
  → 探针包住真 API（并自检补丁生效）、`require` 真产物跑真组合根、从渲染端驱动，带**阴性对照**
- 真渲染门禁 200 → **210 项**（新增：初值真来自主进程 · 勾选框与提示行的**几何可见** ·
  界面跟随返回值而非乐观更新 · 两条"设了却不生效"分支用哨兵串验）
- 打包版实机核对：日志出现 `后台运行已生效 {blockerId}`；`powercfg /requests` 的执行段能看到本应用持有请求

## 0.13.25 · 2026-09-13

**新：内置终端 —— 真 PTY（xterm + node-pty）**（plan7 批 C）

### ① 一个能真的用的终端

工作台新增「终端」面板。它跑的是**真 PTY 会话**：Tab 补全、`Ctrl+C`、行内编辑、`vim` 一类全屏程序
都能用；`resize` 真生效（拖动面板，程序里看到的列数会跟着变）。

- **会话活在主进程，界面只是可丢弃的视图**：切走页签再切回来，输出**不重不漏**地重放回来
- **权限档说了算**：只读档下**开不了**终端（启动处拒绝），运行期每次按键与点「重启」之前都会再查一遍；
  界面直接说明原因，不做"点了没反应"
- **配色跟随应用主题**：浅色界面是纸白底 + 墨字 + 朱砂光标；切主题**当场**跟着换

### ② 打包链路修好

原来 `npm run dist` 会因为"给每个原生依赖按 Electron 的 ABI 重编"而失败（本机没有 VS 工具链）。
node-pty 走的是 **N-API 预编译**（ABI 跨版本稳定），所以关掉这一步重编是安全的 ——
`npmRebuild: false`，并在配置注释里写清"将来引入**非 N-API** 原生模块时必须重新评估"。

### ③ 打包验收补了一节

`scripts/verify-asar.cjs` 新增"**原生模块必须解包到 asar 外**"：逐条查 `app.asar.unpacked/` 下 5 个二进制
（`pty.node` / `conpty.node` / `conpty.dll` / `OpenConsole.exe` / `winpty-agent.exe`）。
这类坑的形态是"**开发态一切正常、打包版一开终端就报模块加载失败**" —— 本地四道闸一个都拦不住。

### 内部

- 独立审查两轮共 4 位子代理，抓出并修掉 4 个**静默**缺陷：重放与实时帧重叠 · 「重启终端」是死按钮 ·
  会话 id 撞号（同毫秒）· ACK 黑洞（切页签后 pty 被永久按住）；另补上计划里写着"必须有"的**背压水位**
- CSP 两档放宽（`style-src-elem` / `style-src-attr`）：门禁一次跑出 **172 条**拒绝，来源是应用自己的
  bundle（monaco 的 diff 余量装饰 + xterm 真彩路径）；`script-src 'self'` **一字未动**，放开后 0 条
- 单测 739 → **834**；真渲染门禁 164 → **177 项**；两组证伪实验（守卫改回旧实现 / attr 档改回 `'none'`）精确变红

## 0.13.24 · 2026-09-13

**新：输出侧的省法 —— 让模型"少说废话"（plan8 R9.1 §七③）+ 前缀稳定自查（§七④）**

### ① 输出纪律：省 token，但不省准确性

输出的钱花在**字数**上，而模型的默认习惯里有三块纯浪费：
把推理过程再说一遍、把工具返回的原文大段复述、把提问重述一遍。
这一版按档位给"输出纪律提示"：

- **土豪 / 极致**：不加（让模型充分展开）
- **平衡（默认）**：先结论 / 不复述工具原文 / 不复述问题
- **轻量**：再加篇幅克制

三条都**不损害正确性**（少说的是冗余，不是依据），而且都是**明说**的 ——
绝不靠"偷偷改数据"来省。

### ② 思考强度：只有最省那一档才动你的设置

DSH 面板实测：**输出里约 52% 是推理**（思考链按输出计费）——
所以"想多少"是输出侧最大的省钱开关。这一版：

- **土豪 / 极致 / 平衡**：一个字都不改你在模型档案里配的思考强度
- **轻量**：压到 `low`（选这一档时你已经说了"允许质量略降"）

### ③ 前缀稳定自查（零代价，四档都守）

实测输入里 **99.3% 是缓存命中** —— 前提是"开头一模一样"。
这一版把系统提示的组成审了一遍：全是静态文本，没有时间 / 轮数 / 会话 id 混进去，
并把这条写成了断言（将来谁往里塞会变的东西，测试立刻失败）。

**唯一已知的破坏点**：上下文接近上限时，历史会被折叠成一条「[历史摘要]」插在**中间**，
从那个点往后缓存失效。这是**值得的代价**（不折叠就会撞上下文上限、整轮作废），
但现在它会**留一条日志** —— 将来看到"这一轮 token 突然跳高"，就知道该去查它。

### 内部

- 子代理与主代理**吃同一份**纪律、同一个思考强度（子代理的输出同样计费，不该只约束一半）
- 单测 697 → **705**（新增前缀稳定那一组）；真渲染门禁 **133**；四道闸全绿

---

## 0.13.23 · 2026-09-13

**新：省 token 的「档位」开关 —— 你在乎能力还是在乎钱，你说了算**

上一版把"花了多少、省了多少、命中多少"摆出来了。这一版解决**另一个问题：省到什么程度，由谁定**。

以前那些省 token 的做法是**写死在代码里**的（读文件默认 200 行、输出超过 1400 字节就压）。
现在它们全归到一个**档位**下，在设置 → 通用设置里可选：

| 档位 | 你会得到什么 |
|---|---|
| **土豪** | 一点都不省：工具输出原样进上下文（最贵，但模型看到的最全） |
| **极致** | 只做零代价的省：塞不下时才压，尽量少动 |
| **平衡**（默认） | 兼顾模型能力与节省 —— **保持与之前完全一样的行为** |
| **轻量** | 最省：更常压缩、给更少上下文；允许质量略降 |

**三条不许做成开关的红线**（它们是正确性，与档位无关）：
**不静默截断**（压了就必须说清"这不是全部"）、**不压报错现场**（报错行连同上下文强制保留）、
**不伪造缺失数据**（厂商没报就说没报）。
轻量档"掉质量"只允许来自"更常压缩、给更少上下文"，**不许来自骗模型**。

档位会**记进账本**并显示在输入框右下角的用量牌上（如「平衡」）——
这样你回头看数字时，知道它是哪一档下跑出来的。

### 内部

- 四档由**一个纯函数**翻译成具体参数，下游只认参数、**不认档位名** —— 以后加档只动一个文件
- 单测 667 → **687**（四档共同红线**逐档各断言一遍**，不是抽查一档）；真渲染门禁 129 → **133**
- 读文件默认行数、工具输出的压缩门槛随档位走；**子代理跟主代理同档**
- `reasoningEffort`（思考强度）与输出纪律提示留给下一版 —— 本次**没有**在配置里放"看着有、实际没人读"的假字段

---

## 0.13.22 · 2026-09-13

**新：用量牌多出两个数 —— 缓存命中率与思考占比（plan8 R9.1 计量扩展）**

输入框右下角那块用量牌，以前只有"一共花了多少"。这一版补上**钱花在哪一头上**。

### ① 「命中 xx%」—— 输入里有多少是便宜的那部分

模型厂商会把**你上一轮发过的开头**存下来复用（前缀缓存），命中那部分**便宜得多**。
现在用量牌直接告诉你命中比例 —— 实测一次普通对话是 **93%**（2210 个输入 token 里 2048 命中）。

一条硬规矩：**厂商没报这个数，就整块不显示**（连 `0%` 都不写）。
"没报"和"一点没命中"是两件事，写个 `0%` 等于替厂商宣布一件我们并不知道的事。

### ② 「思考 xx%」—— 输出里有多少花在"想"上

模型的思考链也按输出 token 计费。参考公开量级：输出里有**约一半**是推理。
知道这个数，才知道"要不要让它少想一点"值不值。

⚠️ 与上面那条刚好相反：厂商**明确报了 0**（这轮确实没思考）时会显示 `思考 0%` ——
那是**事实**，该显示就显示。两种"0"在代码里走的也是两条路。

### ③ 顺手修掉一处账面缺陷（Anthropic 用户能看到差别）

Anthropic 的用量**分两处报**（开头报输入、结尾报输出），而主循环那处写的是**覆盖**：
后到的"输出"会把先到的"输入"**抹成 0** —— 账面少一半，日志里还什么都看不出来。
现在改成**合并**（各取有值的那侧，不是相加：开头那个 `output_tokens: 1` 只是"刚开始"的占位）。

### 内部

- 加了个**形状探针**：解析厂商字段之前，先把真实响应里到底有哪些字段打一行日志。
  各家的字段名不一样、还会变，**凭记忆写 = 埋一个永远不生效的解析**（不报错，只是恒为 null，最难查）。
  探针只在开发态落盘，正式包不打。
- 单测 640 → **667**；真渲染门禁 126 → **129**（新增"报了 0 要显示 / 没报整块消失"两条）。

**已知**：省 token 的**档位开关**（土豪 / 极致 / 平衡 / 轻量）还没做，那是下一件。

---

## 0.13.21 · 2026-09-12

**新：工具输出不再"盲目砍一刀"（plan8 R9.1 省 token 第一件）**

先说这一版**修掉的三个真实毛病**（都是量出来的，不是猜的）：

### ① 读文件改成"按窗口读"，并带行号

以前 `read_file` 是**全文倾倒**，上限写着 1MB —— 实测：读一次 `package-lock.json` = **9.2 万 token**，
而 1MB 的上限换算过来是 **26 万 token**，对任何真实上下文窗口都**形同虚设**。
现在：默认给**前 200 行**，每行前面带**行号**（`12|代码`，方便后面按行引用/编辑），
末尾**如实说明**"文件共 N 行、本次给了第几到第几行、继续读用 offset=几"。

- 新增 `offset` / `limit` 参数；单次读取有**绝对预算**（约 1.2 万 token），超了就说"已达单次上限"
- 单行超长（压缩过的 JS 一行几万字符）**单独掐断**——"限制行数"挡不住这种形状
- 二进制文件（含 NUL）直接说清楚，不再灌一屏替换字符进上下文
- 脏参数（`offset: "abc"` / 0 / 负数）**回落到默认值**，不报错折腾你一轮

### ② 命令输出不再丢掉"结论那一半"

以前 `run_command` 只保留 **前 8000 字符** —— 而编译错误、栈追踪、测试结论**都在末尾**，
等于每次输出太长时，你看到的永远是没有结论的那半截（业界把这种"只保头"列为反面教材）。
现在原样交回，由**唯一一处**统一成形：

```
[工具输出过长，已压缩展示] 原文 41727 字节 / 约 10432 token …
  ← 头 60 行全文
  —— 中段报错相关命中 N 行，列出最早与最后的若干条（带原文行号）
  —— 中段其余按每 K 行抽 1 行（带原文行号，可据此精确取回）
  —— 末尾 40 行全文            ← 结论在这里，永远留着
```

- **报错现场保护**：堆栈帧、`warning TS6133`、`npm ERR!`、`EACCES`、`exit code 1`、
  `✕`/`--- FAIL:`/`Expected+Received`、中文"超时"……命中即保留**连上下文**（红队给的 14 条真实样例已钉进测试）
- **不达标就原样放行**：压不到原始的 72% 以下，或者压完还是太大，一律**放弃压缩**（宁可多花，也不糊）
- 同样的收形也管住了 `fetch_url`（以前砍前 20000 字符）与子代理报告（以前砍前 6000 字符）

### ③ 省了多少，看得见

输入框右下角那块用量牌多了「**省 5.2k**」——**单独标成"本地估算"**，
**不并进**上面那个厂商真值（一个是账单、一个是我们替你做的减法，混在一起就分不清了）。
它跟用量一样会落盘，重启还在。

### 三个"要命处"是被独立审查打出来的（都在测试里留下了哨兵）

| 问题 | 后果 | 修法 |
|---|---|---|
| 只有"比原来小"这道**相对门** | 3MB 输入压到 2.1MB 也算过门，而那是 **50 万 token** → 厂商直接报"超出上下文"、**整轮作废**（兜底的 `trimMessages` 在这种形状下**一动不动**） | 加**绝对预算**（窗口 8k / 单文件读 12k） |
| 文案写"报错行**全部**保留" | 800 条命中只列 25 条 —— 丢掉的里面正好有**最后那条根因** | 如实报"命中 N 行，此处列…**不是全部**"，且**取两端** |
| 想做的"跨轮去重" | 我们这边**工具消息不进会话历史**，"上一轮的同一条输出"根本不在上下文里 → stub 是**指向空处的指针** | **不做**（计划里写明了理由与重启条件） |

### 验证

- 单测 598 → **637**（新增 39 条）；真实渲染门禁 124 → **126**
- 新增断言**逐条反证过**：把"省下的量"累计打掉 → 只有那两条红（其余 124 条全绿）
- 三条独立审查/调研参与了设计：两份设计审查（可行性 + 红队）+ 一份业界调研
  （Claude Code / Cline / OpenHands / Roo / Aider / Continue 的具体常量与反面证据）

---


**新：真实用量看得见了（输入框右下角多了一块小牌）**

以前界面只有左边那个圆环 —— 那是**本地按字数估的**上下文占用。现在多了一块
**厂商真报的 token 账**：本会话累计 + 最近一轮各多少，悬停能看到输入/输出分开列。

### 口径（这一版的重点全在这三条）

- **不记钱，只计量**。你说"不用记钱"之后，我把早先那盘"单价 × 用量 = 钱"的算术
  （连同一个单价解析函数）**删掉了** —— 价格不是能推出来的东西，各家不同、
  随时调价，硬编价目表 = 第一次调价就变成谎话。数错了是 bug，**价格错了是误导**。
- **厂商没报 → 什么都不显示**，绝不写 0。多数第三方端点（你那个 agnes 就是）压根不带
  `usage` 字段，这时候显示 "0 tok" 比不显示更坏 —— 你会以为"这轮不花 token"。
- **两个协议各认各家的**：OpenAI 兼容的在流式最后一个 chunk 报（要带
  `stream_options.include_usage`，已加）；Anthropic **分两处报**（输入在 message_start、
  输出在 message_delta）。认不出形状一律 `null`，不硬编一个 0。

### 落在哪里

- 账本记在**会话索引**里（不是内存里亮一下就完）→ **重启之后数字还在**，
  切会话各记各的，不串台
- 一轮里模型来回调了好几次（工具调用）→ 那几个 usage **加起来**才是这一轮的量
- **只长不缩**：晚到的旧快照不许把账写回去（数字倒退比不显示更让人费解）

### 验证

- 单测 578 → **598** 全绿；真实渲染门禁 **119 → 124** 项全绿
- 新增门禁**逐条反证过**（不然绿可能只是碰巧）：
  - 把"累计"改成"覆盖" → 只有「第二轮累计上去」那条变红（实际显示 1.0k）
  - 厂商没报时补一个假 0 → 只有「不冒出用量牌」那条变红
  - 载荷不带账本 → 只有「`conv:save` 带上了账本」那条变红

---

## 0.13.19 · 2026-09-12

**修：模型页的来源标签全顶着「深度求索」（连自己加的 agnes 端点也是）**

### 根因

「来源」被我当成**要存的数据**了 —— 而它其实只是**地址的函数**。
老数据升级成新形状时，那一行把每个端点一律标成了 `deepseek`（猜的），
于是用户自己加的 agnes 端点也顶着「深度求索」这个品牌标签。

### 修法：推导，不存储

- 新增 `sourceOfBaseURL()`：地址里有 `deepseek` → 深度求索；**认不出来一律算自定义**
  （宁可不标品牌，也不要标错品牌）
- 建档 / 保存端点时用它填 `source`
- ⚠️ **读盘时重推一次**（`normalizeProfiles`）→ **旧数据自愈**：
  你那两个端点升级后，agnes 会自己变回「自定义」，不用手改
- 标签文案收敛到 `sourceLabel()` 一处（界面各处不再各写一遍三元表达式）

### 验证

- 单测 574 → **578** 全绿（新增 3 条：大小写无关的推导 / 认不出来算自定义 / 文案只定义一处）
- verify-shot **119** 全绿，模型页来源标签实测 **["深度求索","自定义","自定义"]**

---

## 0.13.18 · 2026-09-12

**新：进行中的目标（plan12）—— 跨轮次存活的长期意图**

### 能用了

输入框**上方**多了一条「目标」（摆在待办上面）：

```
◎ 目标                                        ＋ 加目标
· 把工作台做成每天都能用的东西             暂停 编辑 完成 删除
‖ 给核心模块补上端到端测试（内核默认 提的）   继续 编辑 完成 删除
```

- **跨轮次、跨重启存活** —— 关掉应用再打开，它还在（存在 `userData/goals.json`）
- **目标是会话的属性**：切到另一条会话就换一批，切回来还在
- 每行：暂停 / 继续 · 编辑 · 完成 · **删除**；**非法动作给人话**
  （对已完成的目标再点完成会说"要先「重开」才能再次完成"，而不是点了没反应）
- 没有目标时**整条不占位**；最多显示 3 条，多的折成「N 条更多」

### 与待办的区别（这是整件事的分界）

| | 待办（在下面） | 目标（在上面） |
|---|---|---|
| 回答什么 | **这一轮**干什么 | 我要**持续**达成什么 |
| 活多久 | 一轮跑完就清 | 跨轮次、跨重启 |
| 谁维护 | Agent 自己刷 | 用户手建 + **Agent 可自建**（内核工具下一步接） |
| 怎么结束 | 做完自然消失 | **显式**完成 + 有"怎么算做到"的判据 |

一句话：**待办是过程，目标是意图。**

### 验证

- 单测 **574** 全绿（状态机 22 条：六种动作的合法/非法转移、读盘容错、排序）
- verify-shot **119** 全绿（新增 2 条：两条目标都渲染 / 行内动作齐 + 「加目标」）

⚠️ 有一条**没写成断言**（诚实记下）：「目标摆在待办上面」这次**判不了** —— 待办面板在没有待办时
自己不占位，探针跑到那一刻它不在 DOM 里。写成"待办不在就放行"会得到一条**永远绿的假断言**，
所以宁可先不写（顺序目前由 JSX 结构保证），并留了 TODO：等有"待办非空"的稳定场景再补真判据。

---

## 0.13.17 · 2026-09-12

**改：模型配置升级成「端点 + 模型目录」—— 一把 Key 能调多个模型（F5.1，照你给的截图）**

### 为什么改

上一版是「一个档案 = **一个**模型」：换厂商就得覆盖 Key，同一家的第二个模型还得把地址和 Key 再填一遍。
你指出的是对的 —— **一把 Key 通常能调好几个模型**（agnes-image / agnes-video / agnes-3.0…）。

### 现在的形态

```
自定义模型                                         [添加模型]
模型添加后会自动写入到本地 …\models.json 文件
 🐋 Agnes AI          自定义   [当前]      ✎  🔗  🗑
 ─────────────────────────────────────────────────────
 显示名称  [Agnes AI]
 API 地址  [https://apihub.agnes-ai.cn/v1]
 模型目录                   恢复默认模型   获取可用模型
  [agnes-image-2.5-flash] [显示名称]  ⌄  🗑
  [agnes-video-2.5-flash] [显示名称]  ⌄  🗑      ← ⌄ 展开=这个模型自己的高级设置
  [添加模型]
```

- **一个端点（地址 + 协议 + 一把 Key）挂一串模型** —— 不必为每个模型重填连接
- 每行 `⌄` 展开 = **这个模型自己的高级设置**（输出上限 / 上下文窗口 / 思考强度 / 工具轮数 / 采样三兄弟 / 支持图片）
  —— **只存改过的字段**：端点默认一改，没覆盖过的模型自动跟着变（全量复制会长出"改了没生效"这类怪事）
- **「获取可用模型」**：问厂商这个端点能调哪些模型（OpenAI 兼容 `/models`、Anthropic `/v1/models`），
  拉回来的候选勾一下批量导入；已有的不重复加
- 删除护栏两级：**端点至少留一个**、**端点里的模型也至少留一条**（删空了就是一条空连接）

### 迁移（唯一会动到你已存配置的一步）

- 老数据两种形状都认：0.13.16 的「扁平档案」**就地升级**成端点 + 一条目录；更早的「单模型设置 + 一把 Key」包成第一个端点
- **参数一个不丢**、**幂等**（再读不会重复搬）、**迁移前自动备份** `models.json.bak-<时间戳>`
- 会话绑定升级为两级（端点 + 目录里的哪一条），老数据按名字兜底 —— 不会因此打不开会话

### 验证

- 单测 **574** 全绿（模型档案 27 条：两种形状的迁移、只覆盖改过的字段、两级删除护栏、读盘容错）
- verify-shot **117** 全绿（新增 4 条：目录编辑器打开 / 一行一个模型 / 三个动作齐全 /
  **点开 `⌄` 真的有 7 个高级字段**）

---

## 0.13.16 · 2026-09-12

**改：多模型收尾 —— 输入框切的是「档案」，会话记住"用哪把 Key"**

F5 的两条尾巴（0.13.15 交付主体时留下的）：

### ① 输入框的模型切换器：改成切**档案**

以前它只是"改当前连接的模型名" —— 多模型之后那样用多半会 400（拿新名字去撞旧连接）。
现在下拉里列的是**档案**（显示名 + 来源标签），点一下即切当前档案，与设置页那份列表同源。
（保留"同一条连接上换模型名"的输入框 —— 厂商改名、临时试新模型这种少数情况还用得上。）

### ② 会话绑定「模型档案 id」

- 新建会话时，主进程把**当前档案**记进会话（`modelProfileId`），同时保留模型名（给人看）
- 打开老会话：**优先按档案 id 切**；没有 id（升级前的数据）→ **按名字找同名档案**；
  那个档案已被删 → 回落到当前档案。**三条路都不会让会话打不开，也不会丢掉模型绑定**

---

## 0.13.15 · 2026-09-12

**新：多模型管理 —— 不再只能存一个模型（设置·模型改版，照 WorkBuddy 那张形态）**

### 能用了

设置·模型 现在是一个**列表**（原来是一张表单）：

```
自定义模型                                    [添加模型]
模型添加后会自动写入到本地 <userData>\models.json 文件
──────────────────────────────────────────────────────
 🐋 DeepSeek-V4 Flash  深度求索  当前    ✎  🔗  🗑
 ◇ agnes-2.5-flash     自定义          ✎  🔗  🗑  改用
 ◇ deepseek-flash      自定义          ✎  🔗  🗑  改用
```

- **每个模型一套完整配置 + 一把自己的 Key** —— 切厂商不必再覆盖原来的 Key
- 每行：**编辑** / **测试连接**（用**它自己**的 Key）/ **删除** / **改用**（不是当前的那个才有）
- **当前用哪个**看得出来（"当前"标记）——列表没有当前态就是一坨
- **老数据自动迁移**：原来那份单模型配置 + 那把 Key 会变成第一个档案，**不用重填**
- 删除**先确认**，且**至少要留一个**（删空了就发不出任何请求了）；拦下来时给的是人话理由
- 界面上显示的 `models.json` 路径是**主进程给的真值**，不是写死的文案

### 红线

**API Key 一个字节都不进 `models.json`** —— 仍按"档案 id → 密文"存在 settings.json（safeStorage 加密）。
界面只拿得到掩码（`sk-…abcd`）。

### 内核一行都没改

`getSettingsView()` / `getDecryptedApiKey()` / `hasApiKey()` 这几个名字语义没变，只是从
"读设置里那一份"变成"读**当前档案**" —— 于是 Agent 主循环、Provider 适配、其余设置页代码全都不用动。

### 验证

- 单测 **569** 全绿（新增 22 条：迁移不丢参数 / 当前档案三条兜底 / 删除护栏 / 读盘容错）
- verify-shot **113** 全绿，新增模型页断言：三行、来源标签、**当前标记只有 1 个**、
  每行 3–4 个操作、真实路径、**表单不再常驻**（这正是"表单→列表"的判据）

---

## 0.13.14 · 2026-09-12

**改：设置页的浅字注释砍掉冗余（用户：「浅字注释很多余」）**

判据一句话：**这句话是不是用户在做一个决定前必须知道的？** 不是的（产品口号、把标签复述一遍）一律删或压缩。

- **删**：「界面布局：左右抽屉宽度可在分隔处拖动…」（拖动是直觉，双击复位是彩蛋）
- **删**：「外观：切换立即生效，重启后保持。」（一切设置都这样，说了等于没说）
- **砍口号**：「能力归模型、权限归人…」只留真正有用的那半句「与输入框工具栏那处是同一个设置」
- **压缩**：工作区边界、Key 存储、上下文窗口、输出上限、故障排查 —— 五处保留信息量，去掉垫字
- **一字未动**（这三条承重，删了才是错）：多模态勾选提示 · 连通测试会花 Token · 日志已过滤敏感信息

保留的提示仍在一屏之内、每句一行 —— 不是"清空提示"，是**只留要人记住的那句**。

---

## 0.13.13 · 2026-09-12

**新：多会话并发 —— 一个窗口里两条会话各跑各的**

### 能用了

- 一个窗口里**同时跑最多 3 条会话**（一条改代码、一条查资料、一条跑长任务）
- **切走不丢**：后台那条继续跑，切回来能看到它的实时流
- **侧边栏标记**：哪几条在跑一眼看得出（不留神会以为切走的那条停了）
- 发第二条并发消息时**提醒一次**"两个会话同时改同一个工作区会互相覆盖文件"（只提醒，不拦）

### 顺带修掉的现存问题

**「流式输出期间切会话会串台」** —— A 正在吐字时你切到 B，后面的字会落进 B。
根因与本功能完全相同（事件没有身份），所以一并解决了，并**单加了一条回归门**钉住它。

### 安全：危险操作确认框从"单槽"改成"队列"

以前它只显示一条请求，**后到的直接顶掉**正在显示的那条 ——
并发时你**读着 A 的命令、点下的却是 B 的「允许」**，
"看过了才批准"这句话就不成立了。现在：

- 一次只显示一条，并写明**还有几条在排队**
- 每条都标明**来自哪条会话**（你才知道自己在批谁的）
- 答复按**该条自己的 id** 配对
- 顺手删掉了 `confirm.ts` 里那个**零调用点**的 `hasPending()`
  （注释写着"界面据此避免重复弹"，实际从来没接线 —— 这种代码比没有更危险）

### 数据安全：后台会话不再白跑

以前只有"当前显示的那条"会被存盘 —— **后台那条跑完谁也不会替它存，整轮白跑**。
现在：后台会话跑完/报错立刻落它自己那条；关窗口时**全部**落盘（主进程等回执才真关，带超时兜底）。

### 证明它真的能并发（不是"看起来能"）

- **真渲染验证**：两条会话同时发 → 断言 ① 两个"正在生成"标记 ② 切到 B 时 A 的字**不串进来**
  ③ A 结束后 B 仍在跑 ④ 后台那条（A）跑完**真的落了盘、落的是它自己** ⑤ 切回 A 看到自己的字
- **单测**：并发闸（同会话拒绝 / 跨会话放行 / 超上限拒绝 / 停错会话不发生）+ 会话路由与落盘归属

⚠️ 一条**诚实的覆盖边界**：上面那条真渲染验证验的是**渲染端**（harness 把 `chat:send` stub 掉了，
闸是 1 还是 3，界面上都显示两条在跑 —— 实测过）。所以**主进程那道闸**单独抽成纯函数
`agent/concurrency.ts` 并用单测覆盖（8 条）。

---

## 0.13.12 · 2026-09-12

**修：预览栏切到编辑态，编辑区只有两行高（而且放不大）**

### 问题（用户真机报的）

在预览栏里点「编辑」（markdown 和 html 都一样），编辑区**缩成一个小盒子**，
下面一大片空白，而且**拖也拖不大**。

### 原因：高度链断在中间

- `.dock-body` 有确定高度（`flex: 1`），但中间那层 `.fp` 是"高度 = 内容"的盒子
- 于是编辑框自己的 `flex: 1` **没有可分配的空间** → 塌成最小行数（实测 **54px**）
- 而 `resize: none` 又把手动放大堵死 —— 两条路都没了

真机上量到的就是 54px 高、所在的栏 620px 高（占 **9%**）。

### 修法

- 给编辑框一个**自己就成立**的高度（60vh，与 HTML 预览同一档）+
  `min-height: 240px` / `max-height: calc(100vh - 160px)` 两个护栏
- **放开 `resize: vertical`** —— 嫌小自己拖，不用等我改代码
- 实测修完：占所在栏的 **71%**，可以拖大

### 顺带修掉验证工具自己的两个"假红"

这一轮排查暴露了 `verify-shot` **不可重复**（同一份代码连跑两遍结果不同），
而"跑第二遍才红"长得跟回归一模一样，最容易让人去改没坏的代码：

1. 每轮残留的 `userData`（Chromium `Preferences`/`Cache`）会让拖拽那一族探针整片红 → **开跑前先清空**
2. "拖 3 次只写 1 次盘"那条是 **race**：机器一忙 300ms 防抖会被推迟，睡固定 750ms 就拿到"还没写" → 改成**轮询等它发生**（本意是"合并成 1 次"，不是"此刻已写"）

修完连跑两遍：**107/107** 稳定。

---

## 0.13.11 · 2026-09-12

**新：HTML 文件可以按网页渲染了（渲染 / 源码 开关）**

### 变化

在预览栏打开 `.html` / `.htm` / `.xhtml`，标题栏多一个「源码」按钮：

- **默认按网页渲染** —— 排版、配色、内联样式、同目录的图片与 CSS 都会正常显示
- 点「源码」切回原始代码，再点「渲染」切回去（双向，不锁死）
- 保存文件后预览自动刷新（重新挂载 + `no-store`，不会拿旧缓存糊弄你）

### 安全边界（这条是刻意设计的，不是没做完）

预览页面**不执行脚本、不联网**：

- 沙箱 iframe（`sandbox=""`，不透明源）+ 预览响应头里的 `sandbox` 与 `script-src 'none'`
  —— 两道锁互相独立，任一道单独就足以挡住脚本
- `default-src 'none'`：外链图片 / 远程字体 / CDN 一律不发请求（工作区的 HTML 里
  完全可能埋着 `<img src="https://…/像素.gif">`，那是一次**静默外连**）
- 预览只读**工作区内**的文件，且只放行网页 / 样式 / 图片 / 字体这几类后缀 ——
  脚本类后缀**根本不在白名单里**

想看全保真效果（含脚本、含外链资源）请用系统浏览器打开那个文件 ——
那时代码跑在浏览器自己的沙箱里，不在应用里。

### 为什么不是一行 iframe 就完事（实测出来的坑）

第一版用的是 `srcdoc`，DOM 里有帧、`sandbox` 也在、**看起来一切正常** ——
但桩页渲染出来是一整片白：`srcdoc` / `blob:` / `data:` 都属于**本地 scheme**，
子文档会**继承父页的策略容器**，于是应用自身的 `style-src 'self'`
把预览里所有内联样式一起砍光（控制台一排 `Refused to apply inline style`）。
渲染"能跑"，但跑出来是没样式的骨架 —— 对预览功能等于没做。

改用**自定义协议**（真实 scheme，拿到全新策略容器）后同一桩页像素采样全中，
且响应头里的 `sandbox` 单独就能挡住脚本（做阳性对照验过：把两道锁都摘掉，
脚本立刻跑起来、背景变红 —— 证明这条断言不是"我们没见过它跑"）。

---

## 0.13.10 · 2026-09-12

**修：流式输出期间去看设置页，回答会丢**

### 问题

正在生成回答时，如果你点开左侧的「设置」（或任何离开对话页的操作）：

- 那期间**吐出来的字你永远看不到**
- 更糟的是，如果回答恰好在你看设置时**生成完**，那个"完成"信号没人接 ——
  于是**整段回答不会被存下来**，回到对话里也找不回来

### 原因

接收流式输出的订阅**挂在对话页上**，而主区域是"切到哪页就只渲染哪页" ——
**一离开对话页，订阅就被拆掉了**，主进程还在往外发，但没人接。

**修法**：订阅搬到应用级（`App`），它的生命周期跟**窗口**走，不跟"你正在看哪一页"走 ——
这也正是流式事件本该有的归属。

### 顺带

「停止」按钮现在会**自己把"生成中"状态收回去**，不再只指望后端随后发一个完成信号。
理由是：用户按了停止，界面就必须停止显示"正在生成" —— 这是**意图**，不是**投影**。
（万一那个完成信号没到，界面就会永远停在"生成中"，而点它正是"停止"，点完还是"生成中"。）

### 验证

verify-shot **91 → 95 项**，新增的一条正对着这个 bug：
**在设置页期间推一段流，断言它仍然被存盘** —— 旧写法下这条必红。
并**实测确认过它不是空断言**（把订阅关掉重跑 → 6 条红）。

---

## 0.13.9 · 2026-09-12

**单实例锁：不再允许同时开两份**

### 为什么

两份程序同时开着，它们**共享同一个数据目录**，各自"读旧数据 → 整文件覆盖写"——
**后写的会把先写的整个抹掉**，而且是**静默**的（你会突然发现某条会话不见了）。

### 现在的行为

- 已经开着一份时，再点一次图标：**不新建窗口**，而是把**已有窗口叫到前面** ——
  你的意图是"我要用它"，结果就是"它出现在你面前"
- 如果窗口是最小化的，会自动还原

### 一条不影响日常使用的细节

这个"只能开一份"是**按数据目录算**的：用 `--user-data-dir` 指向不同目录的实例**互不影响**。
所以本项目的冒烟测试与验证脚本（它们跑在临时目录里）照常工作，
**不会因为主程序开着就跑不了**。

### 说明

这是计划里 C 批（存储位置可配置 + 数据迁移）的**硬前置**：
位置迁移期间如果允许两个实例，它们会**同时判断"新目录是空的"→ 同时复制**，
正好造出"两边都有、都对不上"的半迁移状态 —— 那是最糟的一种。

---

## 0.13.8 · 2026-09-12

**Markdown 现在能直接改了**

### 怎么用

在右侧文件预览栏里，文本文件（含 `.md`）的标题旁边多了一个**「编辑」**按钮 →
点进去就是一个编辑区 → **Ctrl+S 或点「保存」**写回工作区。

- 想看渲染效果就点「预览」切回去——**没保存的字不会丢**（切回来还在，
  甚至**关掉应用再打开也还在**）
- 改了没保存的页签上会有个**小圆点**
- 关这种页签时会**先问一句**（取消 / 放弃修改并关闭），不会默默把你的修改扔掉
- **保存走进同一条写入通道**：和资源管理器里新建/改名是同一个 ——
  所以它**自动记进「文件变更记录」，随时能回滚**

### 两件"不会坑你"的设计

- **文件在别处被改过时会拦下来**：如果这个文件在你打开之后被 Agent 或别的程序改过，
  保存会被拦下并给你两个选择（**覆盖** / **重新载入**），**不会闷头覆盖掉别人的改动**
- **超大文件不给编辑**：超过 256 KB 的文件只读了一段，
  保存回去会把没读到的部分冲掉 —— 所以直接不给编辑，并把原因写在界面上

### 说明

这是「批 A3」的第二块（第一块是二进制/图片预览，0.13.2 已发）。
按计划**不是 Monaco**：这一批要的是"够改就行"，语法高亮 / 多光标 / Diff 留给后面的通用编辑器批次。

---

## 0.13.7 · 2026-09-12

**修：右键「回到这条之前」点下去没反应**

承 0.13.6 的会话回滚。两个 bug，第二个更隐蔽：

### ① 点了没反应（菜单自己的问题）

消息右键菜单在**按下鼠标那一刻**就把自己关掉了 —— 于是"松开"落在别处，
浏览器认为这不是一次点击，**回滚请求压根没发出去**（应用日志里一条记录都没有）。

### ② 撤销会"静默失效"（这个更值得说）

回滚之后只要**再发生一次保存**（切会话、点停止、关窗口都会触发），
被回滚掉的那段内容就会被真的抹掉 —— 于是「撤销」按下去**什么都不发生**。
**回滚本身看着是成功的**，只有你去点撤销时才发现问题 —— 这是最难查的一类。

现在：**回滚后继续说话**才会让旧分支作废（这是有意的），
单纯切个会话、关个窗口都不会再动它。

### 说明

两个 bug 都是**测试没覆盖到**的地方，所以这一版把验证也补强了：
菜单交互从"代码里模拟点击"改成**真鼠标点击** ——
因为模拟点击**只发"点击"、不发"按下"**，恰好绕过了①那条竞态。
改完之后特意把修复退回去重跑了一次，确认新断言**真的会红**（6 条），才敢说它有效。

---

## 0.13.6 · 2026-09-12

**会话回滚：右键一条消息 → 回到这条之前（而且能撤销）**

### 怎么用

在对话里**右键任意一条消息** → 「回到这条之前」 → 确认 → 之后的消息从对话里隐去。
底部会出现一条提示：**只回滚了对话消息、工作区文件没动**，右边有个**撤销**。

- 右键**你发的消息** = 从那条起重来
- 右键**助手的回答** = 只删掉这个回答、留下你的问题，可以直接重问

### 三件事说清楚

- **删不掉东西**：被隐去的内容**还留在磁盘上**，所以「撤销」是可靠的、也是零成本的
- **生成中不许回滚**：正在出字的时候回滚，等于在动的数据上做手术 —— 会直接拒绝并说明原因
- **和「文件回滚」不是一回事**：文件回滚在右侧抽屉的「文件变更记录」里，
  回的是 Agent 改过的**文件**；这个回的是**对话本身**。确认框与提示条的措辞都**分开写**，
  不会让你以为点了一个两个都退

### 说明

这是「会话存储与回滚」计划的 B 批（`PLAN/plan10_会话存储与回滚.md`），也就是当初清单里的 **④**。
地基（A 批·分层）在 0.13.5 已经发出 —— 这一版往上加了回滚那一层。

---

## 0.13.5 · 2026-09-12

**会话存储分层 —— 为「会话回滚」打地基（外加一次实测：不用换数据库）**

### 用户能感觉到的

- **打开应用更快、侧边栏更顺**（会话多起来之后尤其明显）：以前"读会话列表"要先把**所有对话的正文**
  都解析一遍才能数出每条有几句话；现在列表只读一份摘要
- **保存一条会话不再重写全部会话**
- **第一次启动这一版会做一次数据搬家**（把正文从大文件里分出去），
  过程自动、**原文件会备份**（`conversations.json.bak-v1`），失败则不改动、照常启动

### 实测：R8 那个悬了很久的问题，有答案了

会话该不该从 JSON 换成数据库？这次**先把合格线写死再测**（列表/保存耗时 P95 < 50ms、列表内存增量 < 20MB）：

| 规模 | 列表耗时（新 / 旧） | 保存耗时（新 / 旧） | 列表内存增量（新 / 旧） |
|---|---|---|---|
| 真实量级 3×18 | 0.9 / 0.9 ms | 4.1 / 1.8 ms | 0.01 / 0.04 MB |
| 200 会话 × 50 条 | **1.5 / 19.9 ms** | **5.6 / 38.1 ms** | **0.29 / 6.01 MB** |
| 1000 会话 × 50 条（正文 16MB） | **3.5 / 104.3 ms** | **9.7 / 162.3 ms** | **1.36 / 30.01 MB** |

**结论：不换。** 三条合格线全部远低于阈值。而右半列（旧写法）说明的是：
**分层不是锦上添花，是"能不能用"的分界**（1000 条会话时旧写法读列表要 104ms）。

### 说明

这一版**没有新功能**，是为 ④ 会话回滚做的地基（`PLAN/plan10_会话存储与回滚.md` 的 A 批）。
之所以单独发一版：数据搬家这种事，**要在开始往上盖东西之前先在真机上验一遍**。

---

## 0.13.4 · 2026-09-12

**修一条会静默丢对话的保存路径**

### 问题：这三种情况下，最新那段对话会没存上

- 流式还在吐字时**点侧边栏切会话**（表现是"点了没反应"）
- 流式时**点「停止」**
- 流式时**关窗口**

**原因**：界面按下发送时会立刻放一条"空的助手消息"当锚点（流式返回的字往它身上长），
而保存那一步的校验要求每条消息**至少 1 个字符** ——
于是"列表里有一条空消息"这个**合法中间状态**，在保存那一步成了**非法**，保存被拒。

**为什么一直没被发现**：调用保存的那几处写的是"不等它、也不管成没成"，
所以失败之后**界面没提示、日志没痕迹**，用户只会觉得"我这段对话怎么没了"。
切会话那条更绕：保存抛错之后，**后面"切换会话"那一步根本没执行**——
看起来像界面卡住，真正的原因在上一行。

### 顺带修掉的第二个洞：一条看不见的"会话寿命上限"

同一份校验还被"发给模型"那条路共用，上限 **200 条消息**。
对保存来说，这意味着**聊到 200 条之后，保存就永久失败**（同样是静默的）。

现在两条约束**分开**：发给模型的那份仍守 200（一次请求别把上下文撑爆），
**保存的那份**放宽条数、真正把关的是总字数，而且允许"一条还没说过话的会话"。

### 还修了什么

- **保存失败会说话了**：主进程记日志，界面出提示。
  提示**单独占一格**——因为它最常发生在"切会话那一刻"，而原来那种提示会被切会话清掉。
- **落盘逻辑加了 12 条单测**（397 → **409 例**），其中两条是"钉死"用的：
  ① 明写"旧写法会拒掉这条真实路径"，证明这不是无事生非；
  ② "两个校验不许再合并回去"——合并的那一刻两条断言一起变红。

### 说明

这一版是**只有修复、没有新功能**的补丁版：问题是在给 ④ 会话回滚做计划审查时，
被两份独立审查各自独立撞到的（详见 `PLAN/plan10_会话存储与回滚.md`）。
不属于回滚功能本身，所以先单独发出来，不押在后面的批次上。

---

## 0.13.3 · 2026-09-12

**拖拽进会话：放开边界 + 把"没反应"变成"会说话"**

### 可以直接拖工作区外的文件了

- **从系统资源管理器拖任意文件进来都会变成附件** —— 不再要求文件必须在工作区内。
  依据：把一份文件拖进会话是**你的显式动作**，等于把这份内容交给模型，和粘贴一段文字同级；
  拦下来保护不到任何东西，只会让人觉得"拖不进去"。
- 这类附件会在 chip 上标一枚**「工作区外」**，让你发出去之前看得见。
- **Agent 自己读写文件的边界一点没变**（仍然严格限在工作区内）——
  松开的只是"你亲手递进来的这一份"，不是"模型能自己去哪儿拿"。

### 落点从"输入框那一小块"扩到**整块会话区**

- 新建任务页与对话页，**整个会话区域**都能接住拖进来的文件，不用再瞄准输入框。

### 失败不再静默

- 以前拖拽载荷丢了（拖了但界面毫无反应、日志也没痕迹）—— 现在会明确说话：
  「没收到文件路径：请拖「文件」而不是文件夹……」。
- 附件被拒时，理由写清**被拒的路径**与**当时的工作区边界**，并在应用日志里留痕；
  不再只回一句"越界已被拒绝"（那样一条消息有四种成因，谁也定位不了）。
- 界面上不再显示 Electron 那层 `Error invoking remote method '…':` 的包装壳。

### 验证（本次的重点同样在**验证本身**）

- **真机手势拖拽**：此前的拖拽验证是**合成事件**（`new DragEvent(...)` 自己塞进 DOM）——
  它绕过了浏览器判定"这个元素能不能拖"的全部逻辑，**天生就是绿的**，证明不了真机拖得动。
  现在改用 **CDP 真手势**（真鼠标按下/移动 + 拖拽拦截），拿到的是**浏览器自己收上来的真实载荷**，
  并配一条**阳性对照**（分栏标题本来就该能拖）—— 对照不绿，"行拖不动"就说明不了任何事。
- **补上从未被覆盖过的那条分支**：从系统拖文件走的是 `dataTransfer.files` + `getPathForFile`，
  以前没有任何一层验过它 —— 而用户报的报错恰好只可能从这条分支来。
- **验证环境对齐生产**：验证脚本一直用 `sandbox: false`，而真机是 `sandbox: true`。
- 单测 **397 例**、真实渲染验证 **66 项断言**（+7）

---

## 0.13.2 · 2026-09-12

**文件拖进会话 + 二进制/图片预览**

### 文件拖进会话

- **从工作区文件树拖一行文件到输入框** → 变成附件（拖拽时输入框会高亮提示落点）
- **从系统资源管理器拖文件进来**也行
- 附件 chip 与"选文件"那条完全一致（同一个 64KB 上限、同一份工作区边界校验）

### 二进制 / 图片预览

- **图片**（png / jpg / gif / webp / bmp / ico / avif / svg）现在**真的能看**了
- **其余二进制不再是一句「暂不支持预览」**：给前 256 字节的**十六进制转储** ——
  看文件头往往就能认出它是什么格式
- **超大图**（> 8MB）只给元信息 + 明确提示，不硬塞（免得界面卡住）
- 安全：「SVG 是可执行内容」，只通过 `<img src>` 渲染（img 上下文不执行脚本），
  禁止 `<object>` / `<iframe>` / 内联

### 验证

- 单测 **397 例**（+7：附件的边界拒绝与截断、图片类型判定、十六进制转储）
- 真实渲染验证 **59 项断言**（+6）：图片**真的解码出来**（量 `naturalWidth`，不只是 DOM 里有 `<img>`）、
  超大图不渲染、十六进制能认出 ELF 魔数、拖拽载荷是工作区相对路径、放下后出现 1 个附件 chip

---

## 0.13.1 · 2026-09-12

**真机验收反馈修复 + 工作台形态回退**（用户装 0.13.0 后反馈四条）

> 用户原话：「右侧两窗格之间是无法拖着调动宽度的，其他都很好，
> 但是你做多窗格完全没必要，其实不方便看，就一个窗格能放工作台功能和文件预览等多个页签就行，
> 工作台那一栏也是多余的，没必要。然后两个页签栏并列可以做成右键页签出现的扩展功能」

### 修掉：栏间分隔条拖不动

**根因是两个函数对「上限」的看法不一致**：

```
工作台 515px、两栏时（用户机器实测）
  clampPaneWidth 的上限 = 515 − 绝对下限(120) = 395
  allocate 第②步的上限 = 515 − 末栏 min(200) = 315   ← 不一致
  → 往右拖到 360 被立刻缩回 315（"拖了没反应"）；往左拖却有效
```

修法：末栏在"让出"这一步的下限改用**绝对下限**，让两个函数用**同一个约束** ——
同一个约束只许有一处定义。

> 为什么原来的验证没抓到：那条"拖拽有效"的断言跑在 **359px 窄工作台**下，
> 那个宽度里两个函数恰好不冲突，于是给了绿灯。
> **用一个"恰好不算错"的口径去验，就会拿到一个"恰好是假的"绿灯。**

### 去掉工作台标题栏

- 原来那条「工作台 ｜ ＋ ｜ ✕」整条删除（用户：「那一栏也是多余的」）
- 开面板的 **＋ 本来就在栏内页签条上**；收起工作台交给顶栏那个开关
- 连带清掉四组只服务于它的样式与动作

### 多窗格降级为「右键页签」的扩展功能

- 默认就是**一个栏、多个页签**（功能面板与文件都在同一栏里换页签）
- **右键页签 → 在右侧分栏**：把那个页签**挪**到右边新一栏（是挪，不是复制）
- 不再有常驻的「新建一栏」按钮 —— 避免误开出一堆栏

### 分栏后的宽度按可用宽均分

- 原来新栏吃一个与容器无关的固定像素（515px 里开两栏会得到一宽一窄）
- 现在按**实测可用宽**均分，看着才像"分栏"

### 验证

- 单测 **379 例**（+3：宽工作台档拖拽回归 · 与上限一致 · 均分默认）
- 真实渲染验证 **43 项断言**（+2：标题栏确已去掉 · 右键分栏是"挪"不是复制）

---

## 0.13.0 · 2026-09-12

**工作台分栏引擎**（用户意见：「文件预览可以像 DSH 一样在右侧新开一栏，而原本的那些可以做进 ＋ 号里」）

> 本轮最大的一次结构改造：右侧工作台从「**单活跃页签**」变成「**多栏 × 每栏多页签**」。
> 参照实现是 DSH 的 `dsh-worktable` 插件（本机只读）—— **先蒸馏它、再动手**，
> 照抄哪几条、偏离哪几条、不要哪几条，逐条写在 `PLAN/plan9_工作台分栏引擎.md`。

### 工作台：多栏

- 右侧工作台现在是**横向多栏**，每栏各有标题栏、页签条与内容，栏内可开多个页签
- **「＋」开窗菜单**取代了原来那排常驻的六页签条
  （资源管理器 / 文件变更 / 源代码管理 / 终端 / 浏览器 / 任务管理）
- **折叠**：隐藏该栏的标题栏与页签条、内容占满，**宽度不变**；折叠后留一个常驻展开按钮
- 关掉一栏里最后一个页签 → 该栏变成「未指派」并显示开窗菜单（**不会连栏一起消失**）
- 默认布局是**空的**（与"工作台默认收起"一致），展开后给开窗菜单，不悄悄塞一个资源管理器

### 文件预览：独立成栏

- 点文件树里的文件 → 在**右侧开它自己的栏**（不再压在文件树底下的半截预览里）
- 同一个文件只占一个页签；点第二个文件**复用同一栏**（同栏多页签），不是每点一个就多一栏
- Markdown 照旧走富文本渲染
- **旧的「拖高手柄调预览高度」整套退役**（栏宽改成横向拖）——连带删掉只服务于它的纯函数与单测

### 拖拽

- **栏间拖宽**：4px 热区 + 中缝细线，拖拽中实时跟手（Pointer Events + 指针捕获）
- **整栏换位**：拖栏的标题栏到另一栏上即可互换，落点有高亮
- **拖拽中不写盘**，松手后合并落盘一次（不做每帧写盘的卡顿源）

### 边界行为（写清楚，免得被当成 bug）

- 栏放不下时**逐级降级**：收窄 → 收到绝对下限 → 实在放不下就让工作台区**横向滚动**
- **浏览器页签全工作台唯一**（主进程只有一个原生视图，开第二份会互相遮挡）
- 栏数上限 6、单栏页签上限 20

### 验证

- 单测 **376 例**（净 +72：分栏模型新增 76 例，退役的预览手柄删掉 4 例）
- 真实渲染验证 **41 项断言** —— `scripts/verify-shot.cjs` 本轮才真正具备
  「期望值比对 + 失败退出码」的能力（此前只有 `console.log` + `app.exit(0)`，
  等于拿一把没有刻度的尺子当验收标准）

---

## 0.12.1 · 2026-09-12

> 本版本是 **0.12.0 的验收反馈修复 + 过程可见**，四个提交，均为用户真机使用后提出：
> ① 过程看得见了，但**位置不对**（卡牌堆在报告之后、新会话还残留上一轮）
> ② 文件预览"太小"—— 真因是**被挤出面板**，不是高度不够
> ③ 工具栏要**图标化**，且新建要落在**选中的文件夹**下
> ④ 图片建档（R12）：验证截图不再散落根目录

**过程块的位置与残留修复**（用户实测：「卡牌全在报告之下，新建会话时几乎看不到报告」）

- **新建任务时过程状态残留**（真凶）：`createConversation` 漏清 `toolEvents`，
  `reasoning` 三处都没清 → 新会话里挂着上一轮的卡片、界面被占满
- **位置**：思考块与工具卡片从"所有消息之后"移到**最后一条助手消息之前** ——
  它们发生在报告之前，堆到末尾会把报告挤出视野
- **思考块被 flex 压扁**：`.chat-messages` 空间不足时子项被压缩（实测只剩 4px 高，
  几何上存在、视觉上消失）→ `.chat-messages > * { flex-shrink: 0 }`；
  底色从 `--bg` 改 `--panel`（与对话区同色也会让它隐形）
- 验证断言同步收紧：`reasoningVisible` 要求高度 > 20 ——
  原来的 `height > 0` 对"被压成 4px"会误判成可见

---

**过程可见：工具调用详情 + 思考流**（用户对比 DSH 指出「看不到执行和思考痕迹」）

### 工具调用：显示"在干什么"

- `ToolEvent` 新增 `detail`：从入参提取一句人话（`read_file → src/main/index.ts`）
- 界面显示「read_file · src/main/index.ts」，不再是干巴巴的「执行中…」
- 模型给的 JSON 不可靠（截断 / 非对象 / 超长）→ 全程兜底，拿不到就退回原样显示

### 思考流：解析并显示

- 解析 DeepSeek 系的 `reasoning_content`（**不进正文**，只外送 —— 它是过程不是回答）
- 新 IPC 通道 `chat:reasoning`；界面「✻ 思考过程」可折叠块，默认展开
- Anthropic 侧没有：其 thinking 与 tools 互斥，工具循环里拿不到（代码里写明原因）

---

**0.12.0 验收反馈**（用户五条意见的前两条）

### 文件预览：可调高 + Markdown 渲染

- 预览区高度**可拖拽调整**（手柄在预览区顶部，往上拖变高，120–620px）
- `.md` / `.markdown` 走**富文本渲染**（复用消息区那套 Markdown 组件，天然免疫 XSS）
- 换算抽成纯函数并单测 —— **合成事件验不了真实拖拽**：实测浏览器会把真实鼠标位置的
  mousemove 也发过来、覆盖合成坐标
- **修掉一个"看不见"的 bug**：面板缺「固定高度 + 内部滚动」约束，文件树会把预览区
  **挤出可视范围**（rect 高度正常、top 已超出面板底边 —— 高度对 ≠ 看得见）

### 资源管理器工具栏：图标化

- 新建文件 / 新建文件夹 / 刷新 / 折叠全部 —— 四个**内联 SVG 图标**
- **新建落在当前选中的文件夹下**（选中文件则落它所在目录，没选则根目录）；按钮 title 写明落点
- 点目录行现在也会**选中**它（此前只展开）—— 这是"指定文件夹"语义的前提

---

## 0.12.0 · 2026-09-12

**资源管理器全功能 + 子代理 + 后台任务**（用户意见三连，属 plan7 批 A2 / 批 D）

> 本版本三个主题：
> ① **资源管理器变成全功能工作台入口** —— 写操作、右键菜单、拖拽上传，
>    且**界面里改的东西同样可回滚**
> ② **子代理真正跑起来了** —— 此前调度器"备而未用"，现在主代理能把**独立子任务并行派出去**
> ③ **后台任务** —— 耗时的命令不再撞 30 秒超时，可查进度、可终止

---

**右栏「任务」页签：子代理运行记录**（用户意见：「改为子代理和后台任务查看，也是跟 DSH 学的」）

### 关键发现（先摆事实）

`scheduler.ts` 早在 P1 就写好了（并发上限 + 单代理预算 + 独立上下文 + 白名单门控），
但 **`runSubagents()` 全项目零调用** —— 没有任何入口能触发它，
于是"子代理"在界面上永远是一片空白。**所以先补内核，界面才有东西可显示。**

### 内核

- 新增 `spawn_agents` 工具：主代理把**相互独立**的子任务并行派出去
- 两条边界：① 子代理用**同一套已按权限档过滤的工具**——不能借子代理绕过权限上限；
  ② 子代理**拿不到 spawn_agents 自己**——否则可以递归派生，成本失控
- scheduler 新增**运行事件**（start / end / error）：光有最终结果数组不够，
  中间那段时间界面是一片空白，得让"进行中"这件事在界面上成立
- 提示词「做事纪律」新增第 5 条：能并行的独立活派给子代理（附"任务书必须自包含"的提醒）

### 界面

- 右栏「任务」页签由占位文案改为**子代理运行记录**：谁在跑、跑了几轮、耗时、结果摘要
- 状态点三态：进行中（墨色）· 已完成（灰）· 失败（朱砂）
- 空态写清"**怎么才会有**"——比一句"暂无数据"有用

---

**资源管理器写操作 + 右键菜单**（用户意见：「要全功能的，支持拖拽上传、右键多功能」）

### 先补的地基：统一写入服务

改版前的检查点快照挂在 `file-tools` **那个工具**上 —— 界面若自己写文件就绕过检查点：
用户用文件树删掉一个文件，回滚面板里什么都没记，**退不回来**。
所以先把「写前快照 + 落盘」收进 `src/main/workspace-write.ts`，界面与 Agent 共用。

### 现在界面能写了，而且**写的东西也退得回**

- 六个 IPC 通道（写 / 新建目录 / 重命名 / 删除 / 导入 / 定位）**全部走写入服务**，
  **每个操作各开一个检查点轮次** —— 界面里删掉的东西同样出现在「文件变更记录」里
- 删除走**回收站**（`shell.trashItem`），不是硬删
- 右键菜单三套：文件行 / 目录行 / 空白处
- Electron 禁用 `window.prompt`，故新建与重命名走**内联输入**（Enter 提交 / Esc 取消）
- 菜单**贴边回收** —— 在窗口右下角右键也不会跑到窗口外

菜单项按本项目**真实具备的能力**筛，不照抄 VSCode：运行测试 / 调试 / 覆盖率 /
时间线 / Git 文件历史这些本项目没有，不做。

### 拖拽上传

- 系统里的文件**直接拖进文件树**即可导入工作区（拖到目录上 → 进那个目录；
  拖到空白处 → 进工作区根；拖到文件行上 → 进它所在的目录）
- 落点**高亮**提示
- **同名不覆盖**：自动另存为 `name (2).ext` —— 拖两次同一个文件很常见，
  静默覆盖会让人白白丢掉原有内容
- Electron 32+ 起 `File.path` 已移除，路径经 preload 的 `webUtils.getPathForFile` 获取；
  拿不到路径时如实提示

### 顺带修掉一个类型检查盲区

`src/preload/**` 此前**不在任何 tsconfig 的 include 里** —— 安全桥接层根本没被类型检查
覆盖（`ApiBridge` 改了而 preload 没跟上，typecheck 也抓不到）。现已纳入 renderer 那套。

### 后台任务（`run_command` 后台化）

此前 `run_command` 是 `exec` + **30 秒超时**的同步执行 —— 跑构建、起服务、下大文件都会撞超时，
模型只能干等。现在：

- `run_command` 加 `background=true`：立刻返回任务 id，输出持续累积（stdout + stderr 合流）
- 新增 `check_command`（看输出与状态，不传 id 则列全部）与 `kill_command`（终止）
- **杀进程树**：命令是经 shell 起的，只 kill shell 会留下孤儿进程（端口还占着）——
  Windows 用 `taskkill /T`，POSIX 杀进程组
- 右栏「任务」页签显示：命令原文、状态与耗时、退出码、输出回显；运行中的可一键终止

**三条边界**：

1. 窗口关闭时**统一终止**（`window-all-closed` → `killAll`，与危险确认桥同一口径）
2. 后台命令**仍需逐次确认** —— 安全不因为"后台"打折
3. 输出**内存累积 + 64KB 上限截断**（只留末尾），不落盘

---

## 0.11.0 · 2026-09-12

**设置页形态改造 + 尺度 token 收敛 + 待办清单**（用户意见三连）

> 本版本三个主题：
> ① **设置页**由单页纵向滚动改为**左导航分区**（形制对齐用户给的 DSH 截图）
> ② **R7 设计系统收口** —— 尺度 token 层立起 + **363 处裸值全部收敛**
> ③ **待办清单**（plan7 批 D 提前落地）：Agent 多步任务先列清单，界面显示在输入框上方

### 设置页：改为左导航分区

- 由「单页纵向滚动」变为**左分区导航 + 右内容**
- **四个分区**（按项目真实存在的能力划分，不放空条目）：
  - **通用设置**（新）：工作区（选择目录 / 打开目录）· 访问权限三档 · 恢复默认布局
  - **模型**：原「编辑模型」表单
  - **外观**：主题切换
  - **故障排查**：日志目录入口
- 图标为**手写内联 SVG** —— 只为几个图标引一个图标库不划算，且维持零 UI 框架依赖
- **访问权限档与输入框工具栏是同一份存储**，改哪边都生效（文案也共用一份，不写两遍）

### 底层：尺度 token 层 + 存量收敛（plan8 R7 的设计系统地基）

- 新增 `--fs-*`（字号 6 级）· `--sp-*`（间距，**名字即像素值**）· `--radius-*`（圆角 3 档）· `--lh-*`（行高 3 档）
- 收敛前实测：`font-size` **12 种**（含 11.5/12.5 半像素）· `border-radius` **12 种** ·
  `padding` **56 种** · `gap` 12 种 —— 不是"风格"，是每次就地拍一个数留下的痕迹
- **363 处裸值已全部归入阶梯**（脚本 `scripts/tokenize-scale.cjs`，可复核、支持 `--dry`）：
  - `font-size` **126 处 → 6 级** ｜ `border-radius` **71 处 → 3 档** ｜ `padding` / `gap` / `margin` **166 处 → 8 档**
  - 收敛后这些属性**只剩 5 处 `1px`**（标签的刻意微调，不是随手拍，保留）
  - 顺带简化多值重复：`8px 10px` 两值归同档后写成单值

### 细节（用户圈定）

- 权限三档**等宽一排**（实测 `cardW:[211,211,211]`、`sameRow:true`）
- 水墨主题**选中态加深**：`--accent-soft` `#efeee9` → `#e8e6df`，`--ok-soft` 同步
  （水墨体系里「正向 = 墨色」，这两个本就该同值）

### 待办清单（plan7 批 D 提前落地）

> 用户意见：「待办/任务栏做成跟 DSH 一样的输入框上方，会很舒服」

- **Agent 多了一个动作**：`update_todos` 工具 —— 多步任务先列清单，每完成一步更新状态。
  工具**只在有界面消费者时才注册**（没人看的话，它只是给模型的假承诺）
- **界面**：输入框上方的可折叠卡片，形制对齐 DSH ——
  标题「任务」+ 右侧统计「2 已完成 · 1 进行中 · 1 待处理」
  状态用符号而非色块：✓ 已完成（灰 + 删除线，退到背景）· ● 进行中（墨色，当前焦点）· ○ 待处理
- 清单为空时**整个面板不渲染**（没有活在建，就不占地方）
- **提示词**：「做事纪律」新增第 4 条 —— 三步以上的任务先列清单（单步小事不必列）
- 存储：**主进程内存态、不落盘**。它是"这一轮干到哪了"的即时视图，不是历史数据；
  放主进程是为了界面切视图重挂载后不丢
- 只读权限档**也下发** `update_todos` —— 它只改内存、不碰文件系统，属"进度可见"而非"改机器"

---

（下一步：批 A2 文件写操作 —— 前置是先抽「统一写入服务」，让界面与 Agent 共用同一条写入路径；
首屏水印待定）

---

## 0.10.0 · 2026-09-12

**主题系统 + 设置·外观**（用户需求：「在设置里加上自定义配色和风格的功能」）

> 里程碑版本：从此配色/风格是**可切换的设置**，不再写死在代码里。

### 新增

- **设置 → 外观**：主题切换，两个主题
  - **水墨**（新）：黑白灰 + 朱砂红 —— 按用户定的方向（参考 opencode 的克制感）
  - **经典**：原蓝白配色
- **切换即时生效**（写根元素 `data-theme`），**重启保持**（持久化到 `ui-prefs`）
- 主题下的整体观感：保存按钮变墨黑、选中态墨色系浅灰、全文无彩色

### 底层：颜色全面 token 化（这是主题系统的前提）

- `styles.css` 的裸色值 **34 处 → 0**（脚本 `scripts/tokenize-css.cjs` 批量替换，可复核）
- 新增 token：`--warn` 系（琥珀警告，此前散落 8 处裸值）、`--accent-hover`、`--on-accent`（accent 底上的白字）
- **水墨主题** = 一段 `html[data-theme="ink"]` 的变量覆盖——**换主题 = 换一组变量**，
  这正是"自定义配色"能成立的原因

### 水墨主题的用色逻辑

灰阶承担全部层次（**墨分五色**），红只做点睛（**朱砂逻辑**，面积必须极小）；
**语义色服从色彩体系**——正向/激活用墨色（去掉绿/蓝），负向用朱砂。
详见 `DIARY/专题-水墨风配色.md`。

### 修复

- 主题回读缺字段时的防御：若持久化返回的数据缺 `theme`，走 `sanitizeTheme` 保底，
  **不进入「两个主题都没选中」的死角**（真实渲染验证抓到）

### 测试与验证

- 265 例全过；lint / 类型检查干净
- **真点一次**：外观区两个主题项渲染正确 → 点「水墨」→ `data-theme=ink`、
  `--accent` 从 `#185fa5` 变 `#1c1c1a`、选中态正确迁移、水墨截图确认整体统一

---

## 0.9.10 · 2026-09-12

**对话页清空文案：进入对话后背景一个字都不留**（用户 UI 意见第 5 条）

### 变更

- **删除对话页空状态的文案**（原为「行百里者半九十 / 从何来？向何去？」）
- 进入对话后，消息区为**纯空白**，只有底部输入框

### 说明：与首屏的分工

用户看过水墨配色预览后定的方向 ——「进入对话的背景干净最好，字一个都不要」。

这正好合水墨的留白逻辑，也与首屏形成明确分工：

| 位置 | 内容 | 角色 |
|---|---|---|
| **首屏**（新建任务） | 文案「行百里者半九十 / 从何来？向何去？」（+ 规划中的淡墨水印） | **门面** |
| **对话页** | 无任何文案，纯留白 | **工作面**（专注内容） |

> 值得记一笔：**这是"先看效果再定"的直接收益** ——
> 上一轮用户刚要求把对话页文案改成新的两句，看过预览后才决定**整块去掉**。
> 如果当时直接落进代码，就会白改一轮。

### 顺带清理

`.chat-empty` 的三处样式全成死代码（JSX 引用为 0），按规范删除。

### 验证

`hasEmptyBlock=false`、`messagesText=""`（消息区文本为空）、`msgCount=0`；
截图确认对话区纯空白，仅底部输入框。

---

## 0.9.9 · 2026-09-12

**文案统一 + 顶栏去重**（用户 UI 意见第 3、4 条）

### 变更

- **对话页空状态**文案统一为「**行百里者半九十** / **从何来？向何去？**」（与新建任务页首屏一致）
  - 原为「九十里路 / 行百里者半九十。说说你想做什么。」
- **顶栏**在新建任务页时**不再显示「新建任务」**
  - 页面中间已有文案，顶栏再标一次是重复
  - 标题为空时**连分隔符一起不渲染**——否则会留下一个孤零零的「·」
- 两处排版统一（字号 21px、字距 0.06em / 0.04em，与首屏同一套）

### 真实渲染验证

- 新建任务页顶栏：`topbarText = "九十里路"`，`topbarHasSep = false`，`topbarHasTitle = false`
- 对话页空状态：`h2 = "行百里者半九十"`、`p = "从何来？向何去？"`
- 新建任务页仍完全居中（`offsetPx = 0`）

---

## 0.9.8 · 2026-09-12

**首屏文案定稿：「行百里者半九十 / 从何来？向何去？」**（承接 0.9.7）

### 变更

- 新建任务页首屏文案定为两行：
  - 主：**行百里者半九十**
  - 副：**从何来？向何去？**
- 文案区居中排版，整体仍**垂直完全居中**（实测偏移 0px）

### 说明

> 0.9.7 把这两句旧文案删掉了 —— 那是对用户意见的**过度执行**。
> 用户原话是「这两句字**也不要**」，但紧接着补充了「**把我选中文案改为**：行百里者半九十 / 从何来？向何去？」
> 即：**是替换，不是删除**。教训：用户说"不要 X"时，**先确认是不是"换成 Y"**——
> 删除与替换的差别，就是"空着"和"有内容"的差别。

出处（备注）：主文案「行百里者半于九十」语见《战国策·秦策五》引逸诗
（「诗云：『行百里者半于九十。』此言末路之难也」）—— 走一百里，走到九十里只算一半：
越接近终点越难。项目名「九十里路」即取此意；定稿省去「于」字（通用简写）。
副文案「从何来？向何去？」为用户原创设问。

样式细节：两行加 `letter-spacing`（0.06em / 0.04em），比普通标题略宽松，更像题辞而不像标签。

### 真实渲染验证

`offsetPx: 0`（容器中心 389.5 / 内容中心 388.5）；`newTitle` / `newSlogan` 均正确取到；
旧文案（「新建任务」「说清你想做的事」）确认不再出现。

---

## 0.9.7 · 2026-09-12

**新建任务页：内容完全居中 + 去掉两行文案**（用户美学意见，第一条）

### 变更

- 新建任务页内容**垂直完全居中**（此前偏上，顶部固定 56px 留白）
- **移除标题「新建任务」**
- **移除副文案「行百里者半九十。说清你想做的事。」**

### 说明

用户原话：「这是我的**美学偏好**，后续我还会**继续打磨细节和风格**」——
说明这是一条**持续意见流**，不属于某个批次。已在 `PLAN/plan7` 新开
**「用户 UI 意见台账（随到随做）」**专门收纳，避免意见散落在对话里、计划表里查不到。

处理原则：**随到随做，不等 R7 设计系统**（用户是产品主人，美学偏好就是需求；
R7 是技术债清理，两者不冲突）。

### 实现要点

居中用子元素的 **`margin: auto`**，**不用 `justify-content: center`**——
后者在内容高于容器时会把顶部裁掉且滚不上去（flex 已知行为），
而 `margin:auto` 居中的同时仍能正常滚动到顶部。

### 顺带清理

同区域的**死代码样式**（输入框控制台化之前的遗留，JSX 引用数为 0）：
`.new-task-hero` / `.new-task-console`（两处重复定义） / `.new-task-box` / `.new-task-controls`。

### 真实渲染验证

实测 `offsetPx: 0`（容器中心 389.5 / 内容中心 389）——**完美居中**；
旧标题与旧文案均确认不再出现；输入框正常。

---

## 0.9.6 · 2026-09-12

**资源管理器：工作区文件树**（plan7 批 A，只读部分）

### 新增

- **右抽屉「文件」页签**：工作区文件树
  - **懒加载**：展开哪个目录才查哪个（不做整树递归，工作区可能有几千个文件）
  - 目录在前、同类型按名称排（中文按拼音，不是内码）
  - 文件显示大小（B / KB / MB / GB）
  - **点击文件看内容**：等宽预览，超 256KB 明确告知"仅显示前 256 KB"（不假装读全了）
- 支持多级展开（点目录 → 缩进渲染子层）

### 说明：为什么本期只做只读

计划里批 A 含「新建/重命名/删除」，**我拆成两步，本期只做浏览**。理由是一个真实风险：

> R4 的检查点快照钩子挂在 **Agent 工具链**（`file-tools`）上。界面若直接写文件，就**绕过了检查点**——
> 用户用文件树删掉一个文件，回滚面板里什么都没记，**退不回来**。那正是 plan1 刚补上的承诺。
>
> 所以写操作要等「界面与 Agent 走同一条写入路径」之后再做，否则等于开了个绕过检查点的后门。
> 已拆为 **批 A2**（见 `PLAN/plan7`）。

### 两个安全设计（都有测试钉死）

- **`.env` 这类点开头的文件不列出**：文件树里能看见就等于摊在屏幕上，
  与 AGENTS.md「凭证不进 AI 可读路径」的红线同源。
  **但根目录例外**——用户有权看到自己工作区根下有什么
- **噪音目录不列**：`node_modules` / `.git` / `out` / `dist` 等（与 Agent 的 `search_files` 同一套名单）
- **路径门控复用 Agent 那一套**（`resolveInsideWorkspace`）：边界规则只该有一份，
  否则界面与 Agent 两套规则迟早分叉
- 二进制文件不给预览（按扩展名白名单判定），避免把乱码糊到界面上

### 测试

- 265 例全过（新增 27 例：跳过规则 / 排序 / 大小格式化 / 文本可预览判定 /
  真实目录列举 / 越界拒绝 / 截断保护）

### 真实渲染验证

真点一次：根目录列出 `归档`(目录) + 两个文件（目录在前、大小正确、显示工作区路径）→
点「归档」**懒加载出子项** `旧版说明.txt` → 点文件**出现预览**（327×120，内容含"氧化铈粉"）。

---

## 0.9.5 · 2026-09-12

**面板宽度可拖拽**（plan7 批 A0 · 布局基础设施，用户明示）

### 新增

- **左右抽屉的分隔处可拖动改宽度**，并**持久化**（重开应用保持上次宽度）
- **双击分隔条复位**为默认宽度
- 三档约束：最小 / 最大 / **主区域保底 320px**（拖到极限也不会把对话区挤没）
- 窄窗口下抽屉上限自动收窄，不出现横向滚动条

### 说明（为什么这一批排在功能批次之前）

用户配了 WorkBuddy 的分栏截图说「这种左右能滑动改页面大小的设计很需要」。原计划把它放在批 E（UI 精修），
**我改排到批 A 之前**，理由三条：

1. 它是**布局基础设施**，不是视觉打磨——三个区域的宽度是全局约定。
   若等批 A–D 的面板都按固定宽度做完再加拖拽，**要回头改一堆宽度假设**
2. **现在改最便宜**：左右抽屉都还是骨架（右抽屉只有浏览器、变更两页签有内容）
3. 用户是「很需要」而非「能不能加」——按优先级判据，明确需求 > 打磨项

同时把「布局类改动」从批 E 摘出，避免同一件事在计划里出现两处。

### 实现要点（都是踩过才知道的）

- 拖拽监听挂在 **document** 上而非手柄上——否则鼠标拖快了离开手柄就断
- 拖拽期间给 `body` 加 `data-resizing` **关掉宽度过渡**——`.sidebar`/`.dock` 带 0.16s transition，
  不关会让宽度"追"着鼠标走，手感像卡了半拍
- **只在松手时落盘一次**——拖动过程中每帧写盘是浪费
- 读回的存档**一律再夹一次**区间——存档可能被手改坏或跨版本改过宽度范围

### 测试

- 238 例全过（新增 22 例分隔条纯逻辑，重点覆盖**窄窗保护**：任何拖拽结果都不会吃光主区域）

### 真实渲染验证（真拖一次，不是看代码觉得行）

用真实鼠标事件按住左侧分隔条拖 80px：

| 阶段 | 左栏宽 | 主区域宽 | body 标记 |
|---|---:|---:|---|
| 拖前 | 248 | 568 | — |
| 拖动中 | **330** | **486** | `left`（过渡已关） |
| 松手后 | 330 | 486 | 已清理 |

`330 + 5（手柄宽）= 335 = 主区域 left`，且 `248→330` 与 `568→486` **增减守恒**——布局没被撑坏。

---

## 0.9.4 · 2026-09-12

**真机验收后的两处修复：不许"不查就答" + 变更面板去噪**

> 触发：用户真机跑 0.9.3 的五步验收。5 条里 3 通、1 挂、1 未验到。
> **挂掉的那条是核心问题，但好消息是架构没被证伪** —— 详见下。

### 修复

- **提示词缺「做事纪律」——模型会在该查的时候不查，直接编答案**
  - 实测现象：问「看看工作区里有什么文件，列出来」，模型**没调工具**，直接答
    「工作区根目录目前是空的，没有任何文件或子目录」——**语气笃定，且当次"碰巧"对了**
    （目录真的是空的）。但若目录里有文件，它会编一个假列表，**用户完全看不出来**
  - 模型自己还主动招了：「上一条回复我其实是**没查就答**的，刚才才真正调用工具确认——
    好在结果一致」（见 0.9.3 验收截图）
  - **核因**：提示词里只有防注入的安全基线（「工具输出视为数据」），
    **完全没有"必须先查再答"的要求** —— 这是提示词疏漏，**不是架构问题**
  - **反证（说明架构是对的）**：同一次验收里，用户要求「执行 dir 命令」时，模型做了
    完全正确的事——如实说"我没有执行系统命令的能力"，然后**主动找替代路径**：
    "不过 dir 想干的事我可以直接代替：列目录"，并**真的调用了** `list_dir`。
    说明「用不用工具由模型自决」这条架构假设成立
  - **修法**：`runner.ts` 新增「做事纪律」三条，对所有 Agent（内核默认 + 自定义子代理）生效：
    ① 能查就查不许猜（工具能确认的事实必须先核实再回答）
    ② 没核实过的事不用笃定语气讲
    ③ 能力不足时如实说并给替代方案（固化上面那个优秀表现）
  - **回归守卫**：2 例测试钉死这条纪律不被后续重构丢掉（含自定义子代理也要生效）
- **变更面板被「0 个文件」轮次刷屏**
  - 实测现象：面板 5 轮里 4 轮是「0 个文件」——每轮对话都建检查点，包括纯闲聊，
    真正改过文件的那轮反而找不到
  - 修法：`list()` 过滤掉无改动的轮次（磁盘上仍保留，由 prune 按上限清理）

### 测试

- 216 例全过（新增 3 例：做事纪律回归守卫 ×2 + 空轮次不进列表 ×1）

### 验收记录（0.9.3 五步）

| # | 项目 | 结果 |
|:--:|---|:--:|
| 1 | 闲聊不调工具 | ✅ 通过 |
| 2 | 该用时真调工具 | ❌ **失败**（没查就答）→ 本版已修 |
| 3 | 写文件真落地 | ✅ 通过（`test.txt` 18 字节，「新建 1」记录正确） |
| 4 | 命令弹确认 | ⚠️ 未验到——内核默认不下发 `run_command`，模型答"没有 shell 能力"，**这是对的** |
| 5 | 回滚 | ✅ 通过（「已回滚：删除 1 个」，文件消失，徽章正确） |

---

## 0.9.3 · 2026-09-12

**地基第二批：改坏能退回、危险操作先问一句**（plan8 R4 + R5，兑现 plan1 两条承诺）

> 一句话：Agent 早就能改你的真实文件了，但在此之前「崩了查不到、改坏退不回、危险命令拦不住」。
> 这一版把「退」和「拦」都补上了。

### 新增

- **检查点与回滚**（`src/main/store/checkpoints.ts` + `src/shared/checkpoint.ts`）
  - **一轮 Agent 运行 = 一个检查点**：写文件**之前**自动留存原始内容
  - 回滚粒度：**整轮**（退回这轮开始前）或**单个文件**
  - **崩溃/中止也能回滚**：清单增量落盘，且快照一律早于写入，故中断的轮次依然可退
  - 保留最近 50 轮，超出从最旧清理
  - 存储于 `userData/checkpoints/<runId>/`（清单 + 编号备份文件）
- **危险操作逐次确认**（`src/main/confirm.ts`）
  - `run_command` 每次执行前弹确认，**展示命令原文**再决定
  - **无人应答 = 拒绝**：超时（60 秒）、窗口关闭、渲染进程崩溃，一律按拒绝处理
  - 仅「可写」档确认；「完全访问」档不确认（那是用户明确选的「别拦我」）
- **右抽屉「文件变更记录」页签**：按轮列出改了哪些文件（新建/修改）、展开看清单、
  整轮或单文件回滚，**均需二次确认**
- **危险操作确认对话框**：命令原文 + 发起方 + 位置，「拒绝 / 允许这一次」

### 说明

- 权限档与逐次确认是**互补**关系：前者管「能碰什么」（事先设定的上限），后者管「这一次要不要」（当场决定）
- 被拒绝时给模型的是一句明确的错误文本（让它改道），不是含糊的失败

### 测试

- 206 例全过（新增 58 例：检查点纯逻辑 23 + 落盘集成 19 + 快照时序 4 + 确认桥 12）
- 落盘集成测试用**真实临时目录与真实文件**，直接回答「双击回滚能不能真退回去」

### 修复

- 单文件回滚的路径匹配只归一了一侧：若清单里含反斜杠路径会匹配不上，导致**回滚静默无事发生**
- 无效 Agent 名原本在建检查点之后才报错，会留下一个永远停在「未收尾」的空轮次

---

## 0.9.2 · 2026-09-12

**地基第一批：出问题查得到、崩了有痕迹**（plan8 R1 + R2 + R3）

### 新增

- **进程级异常兜底**（`src/main/crash-guard.ts`）
  - 主进程未捕获异常不再让应用**静默消失**：写日志 + 弹窗告知，**不退出**
    （此前一崩整个应用没了，用户无从知晓，也拿不到任何线索）
  - 未处理的 Promise 拒绝：记日志（不弹窗，避免骚扰）
  - 渲染进程异常退出：自动重载，**带限流**（30 秒内最多 2 次）；
    反复崩溃时停止重载并明确提示——无限制重载会变成死循环，比崩一次更糟
- **日志系统**（`src/main/log.ts`，零依赖）
  - 分级 `debug / info / warn / error`，带模块 `scope`（开发 `debug`、打包 `info`）
  - 落盘 `userData/logs/app.log`；**目录不可用时降级为仅控制台**，绝不影响主流程
  - **自动轮转**：单文件 1MB 上限，保留 4 个历史档案；按累计字节在运行期触发
  - **敏感信息脱敏**：写盘前一律过 `scrub()`，`sk-*` / `Bearer` / `apiKey` / `token` /
    `secret` / `password` 全部替换为 `***REDACTED***`（**API Key 绝不进日志**）
- **设置 → 故障排查**：显示日志目录、历史文件数，一键打开日志文件夹
- **CSP 安全基线**：按开发/生产两套策略注入（开发态需放行 HMR 的 WebSocket 与内联脚本）；
  生产态 `default/script/style/connect-src 'self'`、`img-src` 允 https（Markdown 外链图）、
  `object-src 'none'`、`base-uri 'self'`
  - 注：**只作用于主窗口文档**（meta 标签），不影响右抽屉的内置浏览器加载任意网站
- **导航守卫**（两条 Electron 必做项，此前都缺失）
  - 外部链接以前会弹出**没有地址栏的 Electron 窗口**（看着像应用内页面）→ 现在一律交给系统浏览器
  - 主窗口以前可被导航到任意网站（**整个应用被替换成外部网页**）→ 现在拦截
  - 只放行 http/https；`file:` / `javascript:` / `data:` 一律拒绝

### 测试

- 148 例全过（新增 11 例日志 + 10 例导航守卫）

### 修复

- 日志轮转的字节累计器在切换日志目录时未重置，会与真实文件大小**漂移**，
  导致「累计够了但文件还没到上限」→ 轮转被永久跳过。改为**仅轮转成功才清零**
- 内部地址判定原用字符串前缀匹配，会把 `http://localhost:51730` 误判为「自己人」；
  更危险的是 `http://localhost:5173.evil.com`（攻击者注册同前缀域名即可绕过）→ 改为 **origin 精确比对**

---

## 0.9.1 · 2026-09-12

**输入栏自适应修复**

### 修复

- 输入栏比上方消息区窄：`.chat-input` 沿用旧样式（为「输入框 + 发送按钮」横排设计的
  flex 布局），改成控制台结构后缺 `flex:1`，于是按内容宽度缩着
- 输入框多出一圈边框：旧规则 `.chat-input textarea` 优先级高于 `.console-input`，叠了一层

### 改进

- 输入框改用 `field-sizing: content` **随内容自动长高**（无需 JS）
- 工具栏改为**左右两组分簇**：窄窗口换行时整组一起换行，发送键不再独自掉到第二行

> 备注：本版安装包因宿主构建环境的安全删除配额耗尽未能产出，**双包未交付**，改动随 0.9.2 一并打包。

---

## 0.9.0 · 2026-09-12

**真浏览器 + 输入框修正**

### 新增

- 右抽屉「浏览器」做成**真浏览器**：改用 Electron 33 的 `WebContentsView`（移除 `<webview>`），
  真 Chromium 视图按矩形覆盖宿主窗口，显隐时挂载 / 摘除
- **Agent 可操控同一浏览器实例**：新增 `browser_navigate` / `read_page` / `click` / `type` 四个工具
  （通过 `browser-bridge` 依赖倒置，保持 Agent 层不依赖 Electron）

### 修复

- 输入框内元素重叠（chip 缺 `flex-shrink: 0`，文字换行后压叠）
- 工作区选择栏上移到输入框之上

---

## 0.8.0 · 2026-09-12

**输入框控制台化**

### 新增

- 新建 `InputConsole`（对话页 / 新建任务页共用）：附件 chips + 多行输入 + 工具栏
- **访问权限档**：只读 / 可写 / 完全访问——`allowedToolsFor()` 按档求工具上限，
  **权限是硬上限**，模型无法越档
- Git 分支读取、附件引用（限工作区 + 64KB 截断）、提示词优化

### 变更

- 「新建会话」文案改回「新建任务」

---

## 0.7.0 · 2026-09-12

**通道合并：取消「对话 / Agent 模式」切换**（架构级）

### 变更

- 两条物理隔离的通道（对话 / Agent）合并为**单一流式工具循环**：
  用不用工具、用哪个，**由模型自决**，界面移除模式开关
- Agent 循环改带完整对话历史（多轮有记忆）
- 右抽屉扩为**六页签**

### 新增

- `tool-accumulator.ts`：流式工具调用分片累积器（本批正确性核心，
  把 OpenAI / Anthropic 两家协议统一到「按 index 拼接」）
- Provider 新增 `streamWithTools*`
- 新增 `chat:tool` 事件 + **工具执行卡片**（◌ 执行中 → ✓ 成功 → ✗ 失败）

---

## 0.6.0 · 2026-09-12

**三段式抽屉布局**

### 变更

- 关键结构决定：**主区域只负责对话，能力一律挂两侧**
- 「新建任务」入口 → 新建会话
- 技能收进「＋」号拓展（`PlusMenu`）

### 新增

- `TopBar`（左右面板开关）+ `RightDock`（抽屉骨架）

---

## 0.5.1 · 2026-09-12

- 工作区选择统一进输入框：新增共用组件 `WorkspaceChip`，对话页撤掉顶部条，新建页复用

---

## 0.5.0 · 2026-09-12

**侧边栏与任务流**

### 新增

- 侧边栏「新建任务」→ 初始选择页（工作区 / 模型 / 技能勾选）
- 会话持久化 + 按工作区分组的历史（折叠 + 三点菜单：重命名 / 打开目录 / 删除）
- 设置入口改为左下角齿轮

### 安全

- 权限收紧：`set-known` 仅接受已知路径；`conv:create` 校验授权

---

## 0.4.0 · 2026-09-11

**P2 工作台 · 批 1：体验补齐**

### 新增

- **Markdown 渲染**（react-markdown + remark-gfm，渲染为 React 元素而非 `innerHTML`，免 XSS）
- **工作区选择器**：electron-store 持久化 + 对话框显式授权 + 惰性解析，**切换免重启**；
  目录失效自动回退
- **上下文用量指示器**（口径上收 `@shared/tokens`，主渲染共用；75% / 90% 变色）
- **模型快速切换**（只改 model 字段 + 最近使用记录）

### 修复

- P1 遗留：对话消息 Markdown 未渲染、工作区路径不可见 / 不可切换

---

## 0.3.0 · 2026-09-11

**P1 内核完成 + 实测验收通过**

### 新增

- Agent 主循环、工具框架、白名单门控（含路径越界防护）
- 工具六件套：`read_file` / `write_file` / `list_dir` / `search_files` / `run_command` / `fetch_url`
  - `run_command` 为高危工具，**默认不下发**，需显式声明才启用（cwd 锁定工作区、30s 超时、输出截断）
- Anthropic `tool_use` 适配（消息翻译器：同轮 `tool_result` 合并为单条 user 消息）
- 自定义 Agent 加载器（Markdown + YAML，两级覆盖，fail-soft 警告）
- 子代理调度器（并发上限工作池 + 异常隔离）
- 上下文管理：token 估算 + 三层裁剪（system 与末尾永不裁、中段折叠为历史摘要、tool 配对保护）
- 内置定义 `planner.md` / `reviewer.md`（随包分发）

### 实测验收

- 工具真的执行：`hello.md` 实际写入 `userData\agent-workspace\`
- **工作区隔离生效**：用户要求写桌面，Agent 拒绝并解释边界，改在工作区内创建

### 安全（交叉验证后加固）

- 修致命项：runner 未下发工具 schema，工具层形同虚设
- 修 symlink 逃逸、SSRF；加 Agent 并发闸、Anthropic thinking × tools 互斥处理

---

## 0.2.0 · 2026-09-09

- 版本号升级后重打包（澄清：桌面版**不会**自动更新，自动更新属 P5）

---

## 0.1.0 · 2026-09-09

**首个可安装版本（P0 完成）**

### 新增

- Electron 桌面应用骨架（主进程 / 预加载 / 渲染进程 / 共享层四层分离）
- 模型接入：OpenAI 兼容协议（DeepSeek / 通义 / 智谱 / Ollama / vLLM）+ Anthropic 原生
  - **不打包任何模型、不硬编码模型名、不内置任何 API Key**
- API Key 经 Electron `safeStorage` 加密落盘（禁止明文）
- 流式对话、模型细粒度字段（上下文窗口 / 思考强度）
- 应用图标（贝雷帽插画）+ Windows 安装包（NSIS Setup + Portable 双产物）

### 工程

- 测试 18 例、lint / 类型检查 / 构建全绿
- GitHub Actions CI 首次跑绿
- 仓库公开：`github.com/Gazer-887/jiushililu`
