feat: 右抽屉浏览器做成真浏览器（Agent 可操控）+ 修输入框重叠 + 工作区上移

按用户三点要求：

1. 浏览器＝**真浏览器**（不是占位/iframe），且 **Agent 可直接操控**
   - 主进程 src/main/browser.ts：Electron 33 的 WebContentsView（<webview> 标签已移除）
     真 Chromium 视图挂主窗口，按渲染进程给的矩形定位；显隐时才挂载（避免遮挡界面）
     导航/后退/前进/刷新/读取页面/点击/输入 全实现
   - **用户与 Agent 共用同一实例**：右抽屉看到的就是 Agent 在操作的那个页面
   - Agent 工具 browser_navigate / browser_read_page / browser_click / browser_type
     · 与 fetch_url 分工：后者抓静态 HTML，前者拿 JS 渲染后的结果、能点击输入
   - **依赖倒置**：agent 层不得 import electron（CI 无 Electron 二进制），
     故新增 agent/browser-bridge.ts 声明接缝，真实实现由 main/index.ts 启动时注入
     （架构守卫测试覆盖此约束）
   - 前端 BrowserPanel：地址栏/前进后退/刷新 + ResizeObserver 同步原生视图区域

2. 修输入框工具栏元素重叠
   - 病根：.chip-label / .chip-caret / 图标未设 flex-shrink:0，窄容器里被压缩后
     "工作区"三字换行成两行，与相邻元素视觉重叠
   - 修：上述元素 flex-shrink:0 + nowrap；工具栏允许 flex-wrap 并加 row-gap

3. 工作区选择栏移到输入框**上方**（独立一行，不参与工具栏挤压）

测试 127 例全过；门禁四道全绿 + 启动冒烟通过

Co-Authored-By: Anan (WorkBuddy) <noreply@local>
