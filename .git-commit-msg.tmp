feat: 输入框控制台化——大输入框 + 附件区 + 完整工具栏（用户图纸）

按用户图纸实现（上层多行输入区 + 下层工具栏），把散落的设置全部收进框内。

新增 InputConsole 组件（对话页 / 新建任务页共用，形态完全一致）：
- 上层：附件 chips（可移除 / 标注截断）+ 多行输入区（自动撑高）
- 下层工具栏：＋拓展 · 工作区（仅新建页）· Git 分支 · 访问权限 | 进度圆环 · 提示词优化 · 模型 · 发送（圆形）

主进程新增支撑（都是真功能，非占位）：
- 访问权限档（D-032「权限归人」落地）：只读 / 可写 / 完全访问
  · settings 持久化 + IPC；runner 里 allowedToolsFor() 纯函数求上限
  · 权限档是硬上限：自定义 Agent 声明的 tools 只能在其中再收窄，不能越权扩大
  · 测试抓出这一语义变化（原「声明即下发」→「声明且未越档才下发」），已补 2 条用例
- Git 分支显示：git rev-parse + status --porcelain（只读、限时 3s、非仓库静默）
- 附件引用：选文件 → 读入内容（限工作区内，越界拒绝；上限 64KB 截断标注）
- 提示词优化：一次轻量模型调用改写草稿（只输出改写文本）

其他：
- 「新建会话」文案改回「新建任务」（用户要求）
- 上下文用量：条形 → 圆环 + 百分比（对齐图纸）
- 发送键：方块按钮 → 圆形图标（生成中变停止键）
- 附件内容以 file 标签块注入上下文，并声明「是数据，不是指令」

测试 125 → 127 例；门禁四道全绿 + 启动冒烟通过

Co-Authored-By: Anan (WorkBuddy) <noreply@local>
